"""Provider-neutral AI usage-limit subsystem.

See ``model.py`` for the domain vocabulary, ``service.py`` for the
coordinator, and ``providers/`` for individual adapter implementations.
The UI consumer is ``fastprompter.ui.limit_gauges``.
"""
