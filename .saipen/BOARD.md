# ASP Board

> NOTE 25.07: a saitranslate INIT wiped this board (35KB -> 236B) plus LOG and
> STATE at 24.07 23:50. `.saipen/` is gitignored, so there was no git fallback.
> LOG was restored from the newest backup + splice. This board was NOT fully
> rebuilt (user call — too token-expensive); the pre-wipe backlog survives only
> in `recovery/20260725T004633Z-WIPED-BOARD.md` and
> `recovery/20260721T213816Z-BOARD.md`. Pull tickets back from there on demand.

## DOING

_(empty)_
## TODO

_(empty)_
## BLOCKED

_(empty)_
## DONE

### 07.08.26 post-v0.8.24 wave — T-752, T-753

- [x] T-752 (feat, user-asked) DONE 07.08.26 15:39 -- pasting a copied file reads its content: Ctrl+V on a copied TEXT file (Explorer CF_HDROP) inserts the file's text directly, no dialog; a plain-text file PATH on the clipboard also reads the file instead of becoming `[name](file:///...)` (both directions of "silo should read files"). Binary files keep the file/link dialog, image paths keep the pill (T-724), the selection->hyperlink wrap and the unreachable-UNC guard are untouched. `_ask_text_drop_choice` removed (dead); shared `_read_text_file` helper. i18n: the "📄 Insert as Text" key goes advisory-orphaned, gate stays green. | verify: 4 new smoke tests + existing paste/drop/image-pill tests green | owner: opencode
- [x] T-753 (fix, user-reported) DONE 07.08.26 15:39 -- the file manager's 📁 toggle grew the SILO SIDEBAR ~60px per open/close cycle. Measured with a probe: splitter `[files_dock|centre|silo_sidebar]`; the toggle's inline close (setVisible(False)) let Qt hand the hidden child's space to whoever had stretch — the silo sidebar — while reopen took the dock's 180 from the centre only, so the sidebar grew 330→823 over 6 cycles and the centre starved. T-721's centre-rebalance covered only the auto-hide path (`_show_files_dock(False)`); the 📁 toggle missed it. Fix: `toggle_file_container` routes the close through `_show_files_dock(False)` (saves width + hands freed space to centre + plays chest_close); dead `_save_files_dock_width` removed. Probe re-run: every cycle lands exactly [180,761,329] open / [0,943,330] closed. | verify: 1 regression test (3 toggle cycles, splitter sizes stable ±2px) + 3 dock-neighbor tests green | owner: opencode

### v0.8.24 — 07.08.26 Hide on Click-Out removed

- [x] T-751 (fix, user's own removal, ccc-confirmed) DONE 07.08.26 18:08 -- the working tree carried an unlogged removal of the whole Hide on Click-Out feature (settings checkbox, Alt+A hotkey, changeEvent hide path, `close_on_focus_loss` key, ~130 lines + tests). It was the root cause of three P0/P1 "window vanished" reports (T-732 Ctrl+Z hides, T-750 Ctrl+Z closes, startup self-hide), so removal is the definitive close of that defect family. Completed it: dropped the obsolete focus-lock-hides test (test_ctrlz_hide_regression had one test asserting deactivate-without-lock hides the window — now FAILING, removed; the other 4 undo guards stay), dead keys (`close_on_focus_loss` in state.py, `hide_on_clickout_hotkey` in default_profile.py, `_ever_activated`/`_shown_at` in main.py), and reframed the lie comments (file_container refresh docstring, snippet_ops, editor rename-dialog, undo lock-release). Also fixed a PRE-EXISTING gate break found by the full smoke: `tools/sync_saitranslate.py:87` Cyrillic (ded-voice `Эх,` prefix) tripped test_no_cyrillic_in_codebase — T-749 promoted tools/ but only ever ran 7 targeted smoke tests, so the gate was never exercised; the tool is input data (same class as i18n dicts), allowlisted with comment. i18n: 5 clickout keys left orphaned in en.json+modules (advisory, validator splits unused keys as data-driven; regenerating all 33 locales = out of scope). FOLLow-UP flagged on the ticket: the ~30 focus-lock call sites + `ignore_focus_loss` are now write-only (their only reader was the removed changeEvent) — kept for symmetric save/restore, a future HUNT candidate, deliberately NOT refactored here. | verify: `ruff check src tests tests_smoke tools` clean; `pytest tests -q` 943 pass + 1 known pre-existing winsound order-leak (T-730 class); `pytest tests_smoke -q` 737 pass; `python tools/validate_saitranslate.py` PASS (63 known warnings); `git ls-files` has no `close_on_focus_loss`/`hide_on_clickout_hotkey` in src | owner: opencode

### GOAL repair — 07.08 ledger + board normalization

- [x] T-749 (repo hygiene, markhunt) DONE 07.08.26 -- scratch/ was 68 tracked files, 64 of them one-shot migration/fix/patch scripts sitting next to live ship gates. Live tools promoted to tools/ (validate_saitranslate.py, inject_translations.py, sync_saitranslate.py, check_qt_enums.py); tools/fix_remaining_bugs.py (v0.8.1b one-shot) removed too; the 64 one-shots git rm'd (history preserves). scratch/ now tracked-empty. Test-file convention: `test_t743_toast_click.py` renamed to feature name `test_toast_click.py` (ticket ref kept in docstring), rule written into KNOWLEDGE/architecture.md: smoke files named by FEATURE, ticket number never in the filename. TRANSLATION_BUNDLE.md live commands updated scratch/->tools/. All promoted tools ruff-clean. Verify: `git ls-files scratch` = 0; `python tools/validate_saitranslate.py` = PASS; smoke 7 pass.

- [x] T-748 (stale audit claim, markhunt) DONE 07.08.26 -- E-1298 recorded "all 20 except-passes audited"; the real number at its own commit a679f9c was 67 (checked via git worktree + the same AST walk), and it is 70 now (+3 added since, incl. the T-750 sound/zoom/type guards). All 70 read: every one is defensive -- theme-cache fallback to a hardcoded color, watcher/file/connection cleanup, best-effort sound and AOT, QSignal disconnect guards, stylesheet propagation. Zero silent swallows in control flow. Lint-rule decision: NOT enabled (S110/BLE001 would flag all 70 legit defensive sites as noise); instead tools/audit_except_pass.py is the reproducible single source of the number so it cannot drift silently again. Verify: `python tools/audit_except_pass.py` = 70, matches the manual AST walk; E-1298's 20 refuted on evidence.

- [x] T-747 (i18n pipeline, markhunt) DONE 07.08.26 -- silent data loss closed both ways. Root cause: the 26 sound-event labels ('Archive panel', 'Backup DB', 'Bold', 'Copy (Ctrl+C)'...) were added to en/ru/est/ded MODULES by hand in 4a9a2f4 and never reached the JSON source of truth, so a regeneration would DELETE them from the shipped app, and the other 28 locales could never translate them; 9 keys drifted the other way (in en.json, never in a module). Fix: backported the 26 into en/ru/est/ded JSON (values from the modules), regenerated all .py from JSON via scratch/inject_translations.py (now 1019 keys, 0 drift both directions for all 33 locales), and added a json-vs-module gate to scratch/validate_saitranslate.py that FAILs on any key present on one side and absent on the other. Red-tested: fake module key -> FAIL, revert -> PASS. 28 non-EN locales still at 976/1019 (fall back to EN) -- honest WARN, a translate pass can now pick the 26 up. Verify: `python scratch/validate_saitranslate.py` = PASS (no structural errors, gate armed); key-set diff en.json vs en.py = 0/0.

- [x] T-746 (release integrity, markhunt) DONE 07.08.26 -- v0.8.23 was a ghost: CHANGELOG:3 carried the section since 880f701, but pyproject stayed 0.8.22 and no tag was created. Fix: bump pyproject + uv.lock to 0.8.23, tag v0.8.23 (cf6f04c), pushed. Changelog gained the T-750 line. `v0.8.2` re-checked: NOT a truncated typo -- it is a legitimate 30.07 release (3472712, pushed), so markhunt's guess was wrong; it is a historical instance of the SAME disease (tag names 0.8.2, tree's pyproject says 0.8.1b -- bump lag). Left in place: deleting a pushed tag rewrites history for no gain. Also absorbed the orphaned T-750 fix (code was DONE on the board but never committed): main.py + regression test now in e2073e5/59f617b. Verify: pyproject.toml == uv.lock == CHANGELOG head == newest tag == 0.8.23.

- [x] T-745 (quality gate, markhunt) DONE 07.08.26 -- tree was LINT-RED (24 ruff errors, all in the 3-4 test files the last wave added). Auto-fix cleared 22; two F841 kept as the interesting ones: `state_path` in test_write_guard.py was dead code (deleted), `in_open_before` in test_t743_toast_click.py was a recorded-but-never-asserted precondition (now asserted, so the test proves what its author meant). F811 QKeyEvent redefinition resolved to the top-level import. Smoke 739 pass, unit 943 pass (+1 known pre-existing winsound order-leak, T-730 class, proven not-mine by stash). Commit e0f29ec.

- [x] SAIPEN ledger repaired: corrupt active LOG sealed as logs/LOG-002.md, fresh chain E-1319+. Atomic write guard (tools/write_guard.py, 6 tests). Board normalized.
- [x] T-743 (fix, user-reported) CLOSED as NOT REPRODUCIBLE. 7 regression tests prove toast body click + close button + modal dialog all work correctly. WA_DeleteOnClose deletes C++ object, _open list clean. Modality does NOT block mouse input. T-736 fix sufficient.
- [x] T-744 (fix) del_silo undo-order corrected: snapshot moved BEFORE presets[idx] flush. Order: snapshot -> flush -> mutate -> refresh. 897 tests pass (39 undo + 7 T-743).
- [x] GOAL 8/8 complete: 943 unit + 735 smoke PASS, ruff clean, translation validator PASS, SAIPEN validator PASS, EXE built 46.1 MB, shipped 880f701. Bug hunt fixed double-sound (new/save/help), Ctrl+T double-action, btn tooltips.
- [x] T-750 (fix, user-reported, REGRESSION) "Ctrl+Z closes the program" — data undo queued a transient WindowDeactivate (async delivery), changeEvent -> close_on_focus_loss -> hide_and_save mid-undo. Fixed: _smart_undo/_smart_redo hold the counted focus lock, released DEFERRED via QTimer.singleShot(300ms) so it covers the queued deactivation event. 4 regression tests (data undo + text undo + real Ctrl+Z + editor path). 39 undo/T-732/shortcut smoke tests + 943 unit pass.

### AUDIT — 07.08 settings UI + undo/redo (consolidated)

- [x] hk_undo label "Undo Text Change" -> "Undo" (smart undo, not just text)
- [x] hk_italic + hk_underline added to HotkeySettingsDialog (were missing)
- [x] app_binds all 14 labels tr()-wrapped (were hardcoded EN)
- [x] ColorConfigDialog: "(RGB)" -> "(HEX)", Load from Theme confirmation added
- [x] Action checkboxes -> QPushButton, btn_drop_zones.setChecked removed
- [x] _smart_undo/_smart_redo reentrance guard prevents double-fire
- [x] _smart_redo kind="data" failure no longer falls through to text redo
- [x] "Nothing to undo/redo" statusBar messages when stacks empty
- [x] Fallback data-undo path plays sound
- [x] Ctrl+Z/Ctrl+Y in editor call _smart_undo/_smart_redo (not native)
- [x] batch_delete_selected_silos single undo snapshot
- [x] All verified: 891 tests (39 undo-specific, 38 settings) green

### Wave 5 — 07.08 sound asks (user, repeated)

- [x] T-741 (feat, user-asked) DONE 07.08 -- the timer alarm picker listed EIGHT event names out of 412 shipped files, so an alarm could only ever be one of eight sounds and wanting any other meant re-mapping a global event, which changes it everywhere else too. The combo now carries the named events first (existing timers keep resolving) then the whole library; a file is stored as `file:<name>` in itemData, and `_play_timer_sound` routes that straight to `play_file` at the timer's own volume so no settings change can move it under the timer's feet. A file that stops shipping falls back to `tick` instead of going blank. | verify: 2 smoke tests -- the combo offers exactly as many `file:` entries as `discover_sound_files` finds, and a `file:` timer reaches play_file while a named one still reaches play
- [x] T-742 (feat, user-asked, REVERSES MY OWN CALL) DONE 07.08 -- "sound on all possible hotkeys" was asked twice and still looked broken, for two reasons. (1) I shipped the generic `hotkey` event DISABLED in T-735 on my own judgement that a sound on every shortcut would be obnoxious; the user asked for exactly that, twice, so `_DEFAULT_OFF` is now empty and a one-shot `_heal_hotkey_default` flips profiles that stored the old shipped `False` -- migration cannot tell "the app shipped it off" from "the user turned it off", and a marker makes the heal run once so it never fights someone who switches it off afterwards. (2) Qt's OWN editing shortcuts never pass through add_shortcut, so the T-735 wrapper could not reach them and Ctrl+A was silent while every registered hotkey had a sound; the editor now sounds Ctrl+A/C/V/X and falls through without accepting the event. | verify: 2 smoke tests -- a fresh profile ships `hotkey` enabled, an old one is healed once and stays off if switched off after, and Ctrl+A through the real keyPressEvent asks for `select_all`

### Wave 4 — 05.08 evening `/goal` (user's own order)

- [x] T-739 (fix/UI) DONE 06.08 02:10 -- the cause was ordering INSIDE update_data, not the cache and not an external pass: the method ends with setStyleSheet(), which makes Qt re-resolve the font from the QSS cascade and discard a setFont() made before it. The bold was set and thrown away in the same call. New _apply_title_font() runs on the cache-hit path AND after the sheet. E-1293. Original: A snippet whose text starts with `#` does not get the bold sidebar title the `Bold # Titles` setting promises. The SILO half of the setting works; only snippets are affected, so the setting looks half-broken rather than off. Measured, so the next agent starts where this stopped: `refresh_snippets_panel` computes `title_bold` correctly (main.py:7753-7756) and `SnippetWidget.update_data` forwards it to `main_btn.update_data` unconditionally (snippet_panel.py:291) — yet after a refresh the button's font is not bold, while its own `_last_state` tuple already RECORDS `title_bold=True`. Calling `DraggableButton.update_data(..., title_bold=True)` directly DOES bold it (probed). So something either resets the font after update_data ran, or the refresh is not reaching this widget at all on that pass. Already done: the font is now applied BEFORE the `_last_state` early return, so a cached state can no longer claim a bold that is not on screen — necessary, not sufficient. | verify: `test_bold_hash_titles_toggle` green in isolation AND in a full run
- [x] T-740 (fix/UI) DONE 06.08 02:25 -- the scroll wiring existed (editor.py:264); rehover_from_pointer disqualified itself by asking `underMouse()`, which is False for an unfocused window, a warped cursor or a widget scrolled under a stationary pointer -- the exact cases it exists for. Now maps the global cursor into the viewport and lets the existing rect().contains() decide. E-1294. Original: `test_hover_line_follows_the_pointer_when_the_text_scrolls` fails in ISOLATION, not only in a full run — so it is a real defect and not the order pollution it has been filed as all session. The wash stays on the block the pointer was over before the scroll (`_hover_block` 8 where the pointer is over 11). `rehover_from_pointer` exists for exactly this (editor.py:1912) and its docstring describes the very symptom, so the question is who fails to call it on a scroll, not whether the idea is missing. | verify: the test passes standalone and in a full run

- [x] T-735 (feat, user-asked) DONE 05.08.26 -- shipped v0.8.18 (3aaa4b0); BUILT 19:10 -- wrapped at the single registration point (add_shortcut/add_fixed -> _with_hotkey_sound), so future shortcuts are covered automatically; named events for undo/redo/select_all/settings/help/new/save, generic `hotkey` for the rest, never None. 6 events added to the map + labels so they show in the Sound Settings dialog. undo/redo are a two-pitch pair like tick_on/tick_off. `hotkey` ships DISABLED via the new _DEFAULT_OFF. Existing mappings survive, pinned by a test. 5 smoke tests. E-1269. Original: Sound on every hotkey and UI action, not just the handful that have one today. Named cases: Ctrl+Z undo, Ctrl+Y / Ctrl+Shift+Z redo, the settings button, Ctrl+A select-all, and the help window — "all possible hotkeys in the software and help". Work: a GENERIC hook so every registered shortcut and toolbar action can request a sound, plus named events for the ones worth their own file, all appearing in the Sound Settings dialog so they are re-mappable. The user will re-pick the actual .wav files later, so what must be right is the coverage and the mechanism, not the choice: give every new event a sensible default from the shipped library and make none of them silent by accident. Existing events must keep their current mapping — a user who already customised one must not have it reset. | verify: a test that walks every registered hotkey and asserts each resolves to a sound event; the Sound Settings table lists them all; an existing custom mapping survives the migration
- [x] T-736 (fix, user-asked) DONE 05.08.26 -- shipped v0.8.18 (3aaa4b0); BUILT 18:20 -- the X/OK/auto-close existed; what was missing is clicking the toast ITSELF, which is what everyone tries first. mousePressEvent dismisses; plus two ways a toast became unreachable: stale entries in TimerToast._open inflating the stack offset (now pruned by _live_toasts) and no screen clamp in _place (now clamped). 2 smoke tests. E-1267. Original: The Test notification (Timers → Test) is neither clickable nor removable — it appears and there is no way to dismiss it or act on it. Work: clicking the toast dismisses it, give it an explicit close affordance, and make clicking the body do the useful thing (focus the app / open the timer it came from). | verify: fire a test notification, click it, it goes away; the close control removes it; it does not linger after its timeout either
- [x] T-737 (fix/UI, user-asked, 3 screenshots) DONE 05.08.26 -- shipped v0.8.18 (3aaa4b0); BUILT 18:05 -- measured: zero `QHeaderView` and zero `gridline` in themes.py, so Qt painted its own near-white default. New header_view_qss() injected once in theme_mixin; the calendar popup needed its own copy because a widget-level sheet REPLACES inherited rules, which is why T-725 left that one strip white. 4 unit tests. E-1266. Original: WHITE lines across dark dialogs. Seen in Sound Settings (the `Event \| On \| Sound \| Volume` header) and in Timers (the `Name \| Time \| Remaining` header), and the calendar popup's weekday strip — the one part T-725's theming did not reach, which is the clue: it is the same widget class. Cause to confirm, not assume: `QHeaderView::section` carries no background/border in the app sheet and the views' `gridline-color` is unset, so Qt paints both with its light default. Fix it ONCE in the shared stylesheet rather than three times per dialog. | verify: per dialog, no pixel of the header row or grid is near-white under any theme; the calendar weekday strip matches the calendar body
- [x] T-738 (feat, user-asked) DONE 05.08.26 -- shipped v0.8.18 (3aaa4b0); BUILT 18:40 -- new core/silo_export.py names the file from the CONTENT (header, else first three words) plus a timestamp, sanitises Windows-illegal characters, de-duplicates a same-minute second drag and sweeps the scratch folder daily. The drag now carries text/uri-list ALONGSIDE `silo:<idx>`, so internal reorder is untouched; the open silo exports its LIVE editor text, not the stale slot copy. 7 unit + 2 smoke tests. E-1268. Original: Drag a silo OUT of FastPrompter into Explorer / Total Commander / any file manager and have it land as a real `.md` file. The drag already carries `silo:<idx>` as text for the internal reorder; it must also offer `text/uri-list` pointing at a written file. Name it from the content — the silo's header if it has one, else the first three words — plus a timestamp, e.g. `Fix the parser_20260805_1730.md`. Work out where the temp file lives so a cancelled drag does not litter. | verify: a test that builds the drag's mime data and asserts both the internal `silo:` text AND a `text/uri-list` whose target exists, is named from the content and holds the silo's exact text


### Wave 3 — night 04.08 `/goal` (user's own order)

- [x] T-728 (feat, user-asked, extends T-700/T-701) DONE -- code IS in HEAD and shipped, the board line just never got flipped: `window_presets_capture_state` (fancy_zones.py:95), the `cb_capture_state` checkbox (window_presets_dialog.py:66-71), and 16 font_size/ui_scale/theme references in fancy_zones. Landed in 60e3c20, whose commit message named only T-732/T-733. Closed on that evidence by the other seat's neighbour, not re-verified against the ticket's own verify line -- and it is MISSING from the v0.8.18 changelog, which lists T-734..T-738 only. Worth one line there next release. Original: Ctrl+Q presets must be a REAL snapshot | verify: code in HEAD + shipped; closed on commit evidence, not re-verified (documented)


- [x] T-731 (translate, from T-724 + T-718) DONE 04.08.26 23:03 — the 11 new `tr()` keys (T-724 ×5: `Pasted image:`, `Pill (clickable)`, `Markdown link`, `Plain path`, paste-style tooltip; T-718 ×5: `Silos:`, `Sidebar`, `Horizontal tabs`, `↳ Children`, layout tooltip; plus `▤ Fill from preset` from T-715) added to all 32 non-EN locales (943 -> 971). RU/EST/DED hand-translated per the Core split; 29 via GoogleTranslator + 72 manual fixes for pass-through short labels. Verified per locale: exactly +11, 0 changed, 0 removed vs HEAD. Validator PASSED 33/33 @ 971 keys, 0 missing; docs mirrors re-synced (5 wiki pages). E-1239. Original: The 10 new `tr()` keys for the pasted-image setting and the silo tab-mode setting (`Silos:`, `Sidebar`, `Horizontal tabs`, `↳ Children`, its tooltip, plus the 5 from T-724) (`Pasted image:`, `Pill (clickable)`, `Markdown link`, `Plain path`, and the combo tooltip) are in `en.json` but untranslated in the other 32 locales, so they fall back to English there. Validator PASSES (zero structural errors) — this is the coverage warning, not a hard error. Pick it up on the next `ee` (saipen prepare saitranslate) run rather than hand-translating 32 files here. | verify: validator reports 33/33 at 100% with no untranslated-key warnings for these five

- [x] T-729 (fix/UI, user-asked, P1, screenshot) DONE 04.08.26 07:10 -- measured: four 21px rows at y = 0, 2, 4, 6 inside a 42px `archive_widget`. `refresh_archive_panel` ended with `adjustSize()`, which copies a layout hint computed before the rows were shown, and the section is a hand-placed overlay so nothing re-lays it out. Now pins the widget's minimum height to what the visible rows need, then `activate()` AFTER the resize (before it is what caused the stack) and re-places the overlay. Rows land at y = 0, 23, 46, 69. 1 regression test. E-1208. | verify: 1 regression test green
- [x] T-730 (fix, test-only) NOT A DEFECT — CLOSED 04.08.26 07:55. It was the suite command's ARGUMENT ORDER: `pytest tests_smoke tests` reports 56 failures (nearly all of test_sound_manager.py), `pytest tests tests_smoke` — the order T-661 established — is green at 4 failed / 1590 passed, and those 4 are the known pre-existing ones. Nothing to fix in the product or the test; the canonical command is unit-first. E-1213. Original note kept: `tests/test_sound_manager.py::TestVolumeOnTheWinsoundPath::test_play_uses_the_scaled_copy_and_stays_async` passes with its own module (44 pass) and fails in a full `tests` run with `played == []`. NOT the product and NOT the `_mute_sound` fixture (that fixture's import raises in unit runs, so it never patches anything — measured). Some earlier unit module leaves a stale `sys.modules["winsound"]`, the T-661 class of leak. | verify: `pytest tests -q` green, and the test still fails when the leak is reintroduced
  > original T-729 text: The Archive panel is broken on screen: the section paints as a wide empty dark box while its silo buttons collapse to ~8px vertical slivers pinned to the left edge, so nothing in the archive is readable or clickable. Facts already gathered, so the next agent does not re-derive them: `archive_widget` is a `SiloDropWidget` holding 50 pre-built `DraggableSiloButton`s in a QVBoxLayout (main.py:4167-4175); `refresh_archive_panel` ends with `archive_widget.adjustSize()` + `archive_section.adjustSize()` (main.py:7569); the section is an OVERLAY child of `left_panel`, sized by `_position_archive_overlay` with `setFixedWidth(left_panel.width())` and moved by hand (main.py:6634-6642) — so the section's own layout pass is the one thing that can fail to re-expand the buttons after an adjustSize. Suspect the interaction of the manual overlay sizing with T-695's new defaults (ui_scale 0.5, font 18) since `refresh_archive_panel` reads `ui_scale` default "0.5" at main.py:7534. MUST be reproduced live before changing anything — the same panel renders correctly in the silo list, which shares the widget. | verify: launch, open the Archive, screenshot: every visible archive button is at least as wide as the panel minus its margins, and its label is legible

- [x] T-716 (fix, user-asked, P1, DATA LOSS) DONE 04.08.26 05:10 -- five defects, all measured: stale snapshots (live editor text never folded in), silo_gaps absent from the snapshot AND from both gap mutators, a no-op guard comparing 6 of 18 keys that silently DISCARDED snapshots, the "keep data undo fresh" latch that made every later Ctrl+Z overwrite newer text, and no smart redo. Routing now keys off the document's own availableUndoSteps() per snapshot; Ctrl+Y bound to _smart_redo. 6 smoke tests incl. the full interleave round trip. E-1201. | verify: 6 smoke tests green incl. interleave round trip
  > T-716 as asked, kept for the record: Ctrl+Z is not reliably reversible once text edits and silo/gap moves are interleaved. Reported: moved gaps + edited text, pressed Ctrl+Z to bring the gaps back, got deleted TEXT back instead, and the text just typed disappeared with no way to return. The stated contract for this ticket: undo must reverse the last action of ANY kind, redo must put it back, in any order, always — losing what the user wrote is never acceptable, and "the user did not save" is a user error while "the software dropped it" never is. Load-bearing suspicion to check FIRST, not assume: there are two undo systems (QTextDocument's per-editor stack, and the app's own snapshot undo for slot-keyed state — `_snapshot_current`, the T-658 delete/restore path), and nothing orders them against each other, so one Ctrl+Z can pop the wrong stack. Work: one ordered undo timeline both kinds of change push onto. | verify: a test that interleaves type / move gap / type / reorder silo / delete text, then Ctrl+Z all the way back and Ctrl+Y all the way forward, asserting BOTH document text and silo+gap layout at every single step; plus a test asserting no undo step can produce text the user never typed
- [x] T-717 (fix, user-asked) DONE 04.08.26 05:35 -- both hotkeys already called ensureCursorVisible(), but INSIDE the edit block, i.e. before the endEditBlock reflow that resets the scrollbar. New `edit_guard.keep_view()` restores the scroll after the block closes and only then re-shows the caret; Ctrl+E moved onto it too (its old version only restored when the reflow landed on exactly 0). 1 smoke test over a 300-line silo. E-1203. | verify: 1 smoke test green
- [x] T-718 (feat, user-asked) DONE 04.08.26 12:40 -- built by MOVING the existing widgets, not by a second implementation: SiloDropWidget.set_horizontal() swaps the layout axis in place, _axis() makes the T-702 drop rule serve both orientations (indicator line included), apply_silo_tabs_mode() re-parents silos_section between the left panel and the top of the centre panel and re-labels the page arrows, and show_temp_menu gained a Children submenu -- the only route to a child once the bar has no room for one. Setting `silo_tabs_mode` + combo, applied at startup. 3 smoke tests. E-1226. Original: Sidebar must be toggleable into a horizontal tab bar. In tab mode a silo's children have no room on the bar, so they are reachable through the context menu on the parent silo; tabs must drag/reorder the same way sidebar entries do. | verify: flip the setting and silos render as a horizontal bar; right-click a parent shows its children; dragging tab 2 before tab 0 changes the SAVED order, not just the painted one
- [x] T-719 (feat, user-asked) DONE 04.08.26 14:10 -- apply_toolbar_position() moves header_widget within the central QVBoxLayout (index 0 or last), so nothing is rebuilt and the toolbar's saved order and drag-reorder wiring are untouched. Setting `toolbar_position` + checkbox in the Layout group, applied at startup. 2 smoke tests. E-1231. Original: Setting to move the top toolbar to the BOTTOM of the window, toggleable. | verify: flip the setting, assert the toolbar sits below the editor in the layout and the choice survives a restart
- [x] T-721 (fix, user-asked) DONE 04.08.26 09:20 -- both halves were one line. A DOCKED pane is hidden, never closed, so file_container.closeEvent (where T-710 put chest_close) never ran for it; and setVisible(False) let QSplitter hand the freed width to whoever had stretch, i.e. the silo sidebar. Close path now reads sizes before hiding, hands the width to the CENTRE pane and plays chest_close. 1 smoke test. E-1217. Original: Two defects on the file sidebar. (1) Opening it plays the open sound, closing it plays NOTHING — `chest_close` is wired in `file_container` (T-710) but the sidebar path misses it. (2) Closing the file sidebar lets the SILO sidebar grow into the freed width, which the user never asked for. | verify: the close sound fires on the sidebar path; the silo sidebar's width measured before opening the file sidebar equals its width after closing it
- [x] T-722 (fix, user-asked) DONE 04.08.26 09:10 -- `untick` -> tick_off.wav was mapped from day one and `play_tick()` hardcoded "tick", so nothing ever asked for it. play_tick(on) now picks the direction; wired at the genuine two-state sites (silo tick, footer checkboxes, snippets panel, hide-on-clickout) and the editor's own checkbox, which made no sound at all. One-shot confirmations stay on "tick". 3 unit tests. E-1216. Original: Un-tick plays the tick sound — both directions request the same event. `tick_on`/`tick_off` both exist in the library. | verify: a test asserting the un-tick path requests `tick_off` and the tick path requests `tick_on`
- [x] T-723 (fix, user-asked, P1) DONE 04.08.26 11:05 -- it was the whole shifted symbol row, not one key: `~!@#$%^&*()_+{}|:\"<>?` are in _LAYOUT_DEPENDENT and in no fallback map, so wherever VkKeyScanW cannot produce the character _resolve_vk returned VK 0 and the hotkey was unregistrable. New _SHIFTED_TO_BASE normalises each to its unshifted partner (same physical key), so Alt+~ resolves as Alt+`. 4 unit tests. E-1222. Original: Some hotkeys are dead on the Estonian layout — named case Alt+~. Hotkeys must fire on ANY keyboard layout. `hotkeys.py` already routes part of the set through `VkKeyScanW` (`_LAYOUT_DEPENDENT`, see T-689) and backtick/tilde is not in it. | verify: a test resolving EVERY registered hotkey under both the US and the Estonian layout and asserting a non-zero VK for each; live check of Alt+~ on the Estonian layout
- [x] T-724 (fix, user-asked, P1, screenshot) DONE 04.08.26 11:20 -- `![](...)` is the only shape MD_IMAGE_RE matches and so the only one that becomes the clickable chip; a pasted image PATH went in as `[name](...)`, plain link text. New editor.image_paste_markup() owns both paste routes and honours a new `image_paste_style` setting (pill/link/path), exposed as a combo in the settings panel. 3 smoke tests. E-1223. Original: Pasted images regressed from the compact clickable pill to raw markdown link text. Was: a golden pill with a file icon (`clipboard_20260804_025002_fb3b3db2.png`). Now: `[name](file:///...)` in blue, and NOT clickable — the clickability is the critical half. Restore the pill and the click-to-open, then add a setting for paste behaviour (pill / plain link / raw path). | verify: paste an image and assert the pill rect is produced and a click opens the file; each setting value produces its own documented rendering
- [x] T-725 (fix, user-asked, screenshot) DONE 04.08.26 09:50 -- the calendar popup is its OWN top-level window, so the sheet the dialog copies from the main window never reached it. New _style_calendar_popup() builds a sheet from the active theme's raw_colors and applies it to the field AND calendarWidget(). 1 smoke test. E-1219. Original: The timer's date picker popup renders stock white (calendar grid, month header, arrows) inside the dark golden app. Apply the app stylesheet to the `QDateTimeEdit` calendar popup — same fix as the sound dialog in E-1196. | verify: no stock-white widget anywhere in the timer dialog under any theme
- [x] T-727 (fix, user-asked, P1, screenshot) DONE 04.08.26 09:35 -- parse_when only understood HH:MM, while "Use Picker" writes "2026-08-10 11:00". It now takes a leading ISO date (bare date = midnight) and returns a dated moment as-is, no tomorrow-bump. ISO only on purpose: "12.03" is already a time in this parser. 6 unit tests incl. a table driven through the dialog's own format string. E-1218. Original: The timer parser rejects the format the timer's own picker writes: "Use Picker" fills `2026-08-10 11:00` and the dialog answers "Ma ei saa aru". Fix both sides — teach the parser the `YYYY-MM-DD HH:MM` form, and make the picker emit only forms the parser accepts. | verify: a table test feeding every string the picker can produce into the parser and asserting a datetime, never a failure

### Wave 2 — sound system + UI asks (03.08 evening `/goal`)

- [x] T-706 (feat, user-asked) DONE 03.08.26 19:13 -- discover_sound_files(), get_sound_file_for_event(), is_event_enabled(), get_event_volume() added. _DEFAULT_SOUND_MAP expanded with backspace, chest_open/close, hover, button_click/release, notify, error, success, timer. Migration on init creates sound_events dict with default mappings. Tests 43 pass. | verify: suite green, 43 pass
- [x] T-707 (feat, user-asked) DONE 03.08.26 19:16 -- SoundSettingsDialog added with per-event controls (enabled/file/volume/preview), table UI, reset to defaults, immediate save on change. Button added to Sound settings group. Tests 43 pass. | verify: suite green, 43 pass
- [x] T-708 (feat, user-asked) DONE 03.08.26 19:20 -- cb_cs_style checkbox added, on_cs_style_toggled() saves/restores mappings, hover/click/release sounds wired in snippet_panel, cs_style added to default_profile.py. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-709 (feat, user-asked) DONE 03.08.26 19:22 -- backspace event wired to Backspace/Delete keys in editor.py keyPressEvent. backspace already in _DEFAULT_SOUND_MAP. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-710 (feat, user-asked) DONE 03.08.26 19:25 -- chest_open/chest_close wired to file_container.py open_for/closeEvent. timer event already in _DEFAULT_SOUND_MAP from T-706, configurable per timer via sound panel. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-711 (feat, user-asked) DONE 03.08.26 19:27 -- QDateTimeEdit added to timer_dialog.py with calendar popup, Now/Use Picker buttons, _use_picker_value() fills text field. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-712 (fix, user-asked, screenshot) DONE 03.08.26 19:29 -- italic regex fixed in markdown_highlighter.py from `(?<![\w_])` to `(?<![a-zA-Z0-9])` to block intra-word underscores. snake_case stays plain, _real italics_ still italic. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-713 (feat, user-asked) DONE 03.08.26 19:30 -- already implemented in main.py capture_silo_session/restore_silo_session (lines 6991-7025). snippets_hidden saved/restored per project. Tests 45 pass. | verify: suite green, 45 pass
- [x] T-714 (feat, user-asked) DONE 03.08.26 19:31 -- already implemented in snippet_panel.py mousePressEvent (lines 933-945). Alt+click calls toggle_silo_collapse. Tests 45 pass. | verify: suite green, 45 pass
- T-715 moved up to Wave 3 — promoted and extended by the 04.08 `/goal` pivot (E-1200).

### Wave 1 — 03.08 `/goal` wave; T-696..T-704 all SHIPPED in v0.8.9 (205a18f) 04.08.26 (E-1257). T-688 closed 05.08.26 22:10 (E-1287).

- [x] T-688 (translate, infra) DONE 05.08.26 22:10 -- amend branch taken, and the SCOUT changed which branch was right: the premise was stale. `spawn_agents`/`opus-agent`/`gpt-5-agent` do not exist here, but an agent-spawn tool DOES under another name with a `model` parameter for the cheap seat, and Grep is ripgrep-backed and working -- a RENAMED surface, not a lost capability. phases/translate.md § 2 now carries a four-step fallback: look for the tool under its real name first; spawn exists -> split unchanged; no spawn at all -> Core MAY go in-role but MUST log a DEC naming what it looked for, what the host answered and which languages it took; verification duty unchanged. That step 3 is what was missing when JA/DE ran in-role twice with no legal route. NOT verified by a live spawn: that is the other branch's verify line and this session does not spawn agents unprompted (E-1287). Original: This host cannot spawn dedicated sub-agents (`opus-agent`/`gpt-5-agent` refused by spawn_agents; the code-searcher's ripgrep binary is also missing from the SDK vendor dir). The hard-split rule in translate.md § 2 says Core must NOT grind subSaipen languages — yet JA/DE translation has now run in-role twice (T-686 docs, then the guides). Formalize the deviation or fix the spawn path: ticket decision — restore spawnable instances on this host (CODEBUFF_RG_PATH / reinstall ripgrep + agent availability) OR amend the split with a documented fallback for hosts without one. | verify: attempt a dedicated-instance spawn and confirm behavior matches the chosen option

- [x] T-696 (feat, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): font 18 + UI scale 50% baked into the shipped defaults profile (folded into T-695's build, E-1169); existing DBs that never stored the key keep their own values. | verify: fresh profile launches at font 18 / scale 50%; existing profile keeps its own font+scale | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "Defaults are the shipped profile (T-695, T-696)"; E-1169
- [x] T-697 (fix, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): Ctrl+E on an already-bulleted line builds the header with cfg["bullet"] forced False, so the bullet becomes the title and no fresh "• " spawns (E-1170). | verify: bullet line + Ctrl+E gives a header block with zero bullet glyph left | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "Ctrl+E on a bullet builds the header"; E-1170
- [x] T-698 (feat/UX, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): delete is unconditional in the silo context menu with a confirmation only when the slot holds text; routes through trash_silo/del_silo so undo stays one path (E-1169). | verify: right-click a silo -> Delete -> confirm removes it; Ctrl+Z restores text AND colour/type/project path | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "deleting a silo is discoverable and confirmed (T-698)"; E-1169
- [x] T-699 (fix, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): the shipped build has no QtMultimedia, so it always took the winsound path which has no volume; levels now applied by rescaling the WAV samples into a per-level cached copy (E-1170). | verify: measured -- volume 1 vs 10 produces different output on the path the app really uses | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "Volume control actually does something (T-699)"; E-1170
- [x] T-700 (feat, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): window presets now carry zen + sidebar tri-state (True/False/None); apply_ui_state toggles only what differs; a preset saved before this exists says None and leaves the window alone (E-1171). Extended by T-728. | verify: hide sidebar + zen on -> save preset -> restore defaults -> apply preset -> sidebar hidden and zen on | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "window presets remember zen mode and the sidebar (T-700, T-701)"; E-1171
- [x] T-701 (feat, user-asked, follows T-700) DONE 04.08.26 -- shipped v0.8.9 (205a18f): the dialog's _capture() records the same zen+sidebar as the overlay S=save; snippets/silo presets audited as text stores with no window state to carry, out of scope by nature (E-1172). Extended by T-728. | verify: per preset kind, a save/apply round trip with sidebar+zen flipped; a pre-existing preset applies without changing sidebar/zen | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9; E-1172
- [x] T-702 (fix/UX, user-asked, P1) DONE 04.08.26 -- shipped v0.8.9 (205a18f): drop maths is ONE rule per surface -- silo bands now 40/20/40 via _NEST_BAND, _visible_buttons uses isVisibleTo (isVisible() is False for every hidden-window child), and the toolbar edits the SAVED order and reads only the drop position from the layout (E-1177, verify triage E-1195). | verify: per surface, drag item 2 onto the top/bottom half of item 0 and assert the resulting order; the toolbar case repeated twice in a row | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "Drag-and-drop lands where you dropped it (T-702)"; E-1177
- [x] T-703 (fix/UX, user-asked) DONE 04.08.26 -- shipped v0.8.9 (205a18f): the tick column is held open for any row where a tick is reachable; hover changes only the painted glyph (E-1176). | verify: the label's x/geometry is identical at rest and during a synthetic hover over the tick | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "hovering a silo's tick no longer shifts its title (T-703)"; E-1176
- [x] T-704 (fix/UX, user-asked, related to T-702) DONE 04.08.26 -- shipped v0.8.9 (205a18f): a gap belongs to the SILO it was placed under (user decision, E-1178) -- silo_gaps is int_list in _SILO_INDEX_STATE and drop_silo_state removes the gap of the silo being deleted. | verify: insert a gap below silo 1, then add/delete/reorder silos, and the gap stays attached to the position the user chose | verify: shipped v0.8.9 (205a18f) -- CHANGELOG v0.8.9 "gaps stay with the silo you parked them under (T-704)"; E-1178
- [x] T-726 (feat/UX, user-asked) Timer creation is typing-heavy. Give it the normal modern picker flow: click a date and a time, common presets (in 10m / in 1h / tonight / tomorrow), repeat options, with the free-text field kept as the POWER path rather than the only path. Refactor the dialog so the primary flow is the visible one. | verify: a timer can be created end to end without typing into the text field | verify: 2 smoke tests green (quick_presets_write_parseable_moments, created_end_to_end_without_typing); canonical suite 4f/1628p same four pre-existing; shipped v0.8.17 7ea7dbb E-1255
- [x] T-720 (fix, user-asked, NEEDS LIVE REPRO) At startup the caret lands somewhere in the middle of the silo's text instead of a predictable place. SCOUTED 04.08, E-1205: the obvious cause is ruled OUT — `silo_view_state_all` is already remapped on every insert/delete/reorder (`_remap_silo_view_state`, main.py:5202), and it keeps the archive and active halves apart. What is left is restore-time: `restore_silo_state` clamps the saved `pos` to the CURRENT document length (main.py:805-814), so any capture/restore text mismatch lands the caret mid-word instead of falling back to the caller's Start/End rule. Needs a live launch to tell which before touching it. | verify: fresh profile and existing profile both launch with the caret where the user left it, or at the end for a never-visited silo — never mid-word | verify: 2 regression tests green (stale_cursor_restore_falls_back_to_start_or_end, matching_text_still_restores); canonical suite 4f/1626p same four pre-existing; shipped v0.8.16 7ee5148 E-1251

- [x] T-734 (fix, user-reported, P0) DONE 05.08.26 -- shipped v0.8.18 (3aaa4b0); Ctrl+Z after switching silos and typing restored a PRE-SWITCH snapshot and wiped the text typed since — "half the text is gone". The "Switch silo" snapshot was stamped with the document being LEFT (add_data_undo_state ran before the swap), so `_undo_prefers_data` saw a doc mismatch and unconditionally fired a data undo; restoring it rewrote the new silo's doc from stale `temp_presets` and destroyed the newer typing + its undo history. Fix: `_switch_to_slot` re-stamps the switch snapshot to the TARGET doc + its step count after the swap, so routing sees "did the user type after this action?" honestly; the doc-mismatch branch of `_undo_prefers_data` now prefers text undo whenever the active doc has undo steps (also the safe choice for stacks reloaded from disk, where `_doc_id` ids are garbage). 2 regression tests (switch+type undo order with redo round trip; bare switch undo); undo subset 44 passed; ruff clean. Same pass fixed T-733's leftover test fallout: 2 timer tests still called `dlg.list.count()`/`setCurrentRow()` on the new QTreeWidget. | verify: `pytest tests_smoke/test_app_smoke.py -k "undo or silo_switch or line_blocking_drag" -q` green; canonical suite back to the 4 known pre-existing failures | owner: claude | claim_time: 2026-08-05T13:52:00Z
- [x] T-732 (fix, user-reported, P1, REGRESSION SUSPECT) Ctrl+Z straight after pasting an image link HIDES the whole window, exactly as if the user had clicked away. Reported 04.08 during the T-715 build. Evidence already gathered, so the next agent does not re-derive it: the only path that hides on its own is `changeEvent` (main.py:6907) reacting to `ActivationChange`/`WindowDeactivate` while `close_on_focus_loss` is on, guarded by `ignore_focus_loss`, `is_locked`, a help dialog, `_ever_activated` and a 2s post-show grace -- so SOMETHING in the undo path is briefly activating another top-level window, or the main window is losing activation. Prime suspect chain, to be confirmed live before changing anything: the image paste calls `fc.refresh()` on the file container (editor.py, insertFromMimeData), which is a floating Tool window in the undocked case; the undo then runs `_apply_data_state` -> `_switch_to_slot(initial=True)` -> `cancel_editing` -> `refresh_temp_presets`, any of which touching that window would deactivate the main one. Second suspect, cheaper to test: this may be fallout of T-724 shipping `![](...)` for pasted image PATHS in v0.8.11 where it used to insert `[name](...)` -- check whether the hide reproduces with `image_paste_style` set to `link`. That single setting flip is the fastest bisect available and needs no code change. | verify: paste an image link, press Ctrl+Z, the window is still on screen; a regression test that fires the undo and asserts the window stayed visible | owner: claude | verify: 3 smoke tests green (file_panel_refresh_holds_the_focus_lock, paste_image_fc_refresh, fc_refresh_guard_in_code) + canonical suite 4f/1624p same four pre-existing; shipped v0.8.15 16cf45d E-1248

- [x] T-694 (HUNT, bug) `test_line_blocking_drag_swaps_whole_lines` failed. `_move_lines` in `editor.py` failed to delete block 0 because `atStart()` is False when the cursor has a selection. Text insertion offset was also miscalculated. Replaced block deletion logic with robust `NextBlock`/`PreviousBlock` bound selection. Removed diagnostic `test_zz_probe_move` probe. | verify: `test_app_smoke.py::test_line_blocking_drag_swaps_whole_lines` passes.

- [x] T-693 (ADD, i18n gap surfaced by T-691 integration) `main.py` transform menu hardcodes four UI strings (`✨ Transform to…`, `📄 Text`, `📊 Table`, `📋 Kanban Board`) with ZERO `tr()` call sites — they were never served by the engine, and the bundle (939 keys) lacks them, so they render as hardcoded English in every locale. Work: wrap them in `tr()` + add the 4 keys to the bundle (TRANSLATE phase) + re-inject. | verify: app in non-EN locale shows translated transform-menu items; validator 0 missing

- [x] T-691 (ADD, integration) The 33-locale translation bundle integrated into the running app: all `src/fastprompter/core/i18n/*.py` modules regenerated from `.saipen/saitranslate/locales/*.json` (939 keys each, was 874 stale — the 63 multi-line tooltip keys from the 01.08 repair now actually ship); hardcoded `🤍 Support developer` button in help_dialog.py wrapped in `tr()`; `GUIDE_EST/JA/DE.md` copied to repo root. Engine smoke: 33 langs, 100% coverage, multi-line + support keys translate. Shipped v0.8.7 (E-1150/E-1151). | verify: engine smoke (33 langs 100%, `tr('🤍 Support developer','EST')` = '🤍 Toeta arendajat'); suite 886 pass / 1 skip; saitranslate validator 0 missing (E-1150)

- [x] T-692 (tool, user-requested) AST-based `tr()` extractor + source-vs-en gate built into `scratch/validate_saitranslate.py`. Closes the 01.08 multi-line-key hole structurally: ast.Call walk captures multi-line/implicitly-concatenated keys WHOLE (688 static + 34 dynamic), any static key missing from en.json is now a HARD error (synthetic multi-line probe caught as 1 MISSING). Advisory splits unused keys into data-driven vs docs/wiki. Reviewer 2 passes. | verify: `python scratch/validate_saitranslate.py` green (688 keys, 0 missing, 33/33); synthetic multi-line tr() probe flagged as hard error (E-1146)

- [x] T-690 (translate) `GUIDE_EN.md` ("FastPrompter for dummies", 66 lines) translated into `.saipen/saitranslate/kitchen/guides/` — GUIDE_EST.md (Core-owned), GUIDE_JA.md + GUIDE_DE.md (in-role, no spawnable sub-agent on host). Fidelity verified: 64/64 backtick code spans preserved, headings/hotkeys/README anchor intact, no verbatim copies, no BOM. `GUIDE_RU.md` at repo root is hand-maintained — untouched per translate.md § 2 carve-out. Shipped in v0.8.5 (E-1137). | verify: kitchen/guides/ has 3 files, backtick count == 64 each

- [x] T-689 (HUNT, test) `tests/test_hotkeys.py::test_special_character_handling` hardcoded the US-layout VK (0xBD) for `Ctrl+Shift+-`, but `-` routes through `_LAYOUT_DEPENDENT`/`VkKeyScanW` (deliberately layout-aware — module docstring: "hotkeys work on any layout"). On the machine's active Estonian layout (0x0425) VkKeyScanW correctly returns VK 0xDD, so the test failed the moment the layout changed from US. Fix: gate the exact-VK assertion on `GetKeyboardLayout(0) & 0xFFFF == 0x0409`, else assert `vk > 0` (mirrors sibling `test_oem_fallback`). Code unchanged — it was behaving as designed. | verify: 21 tests in test_hotkeys.py pass on both US and Estonian layouts; full suite 886 pass

- [x] T-687 (cleanup, ccc ship preflight) State-repair work from the 10:05 checkpoint (E-1134): replaced the previous session's non-conformant `WAIT: user decision` next_action with a conformant DONE/PHASE HUNT/transition_from TRANSLATE landing, merged duplicate ## DONE headings, re-added T-295 under single ## BLOCKED. | verify: validator STATE check + single ## DONE / ## BLOCKED each once

- [x] T-686 (translate, subSaipen scope) `kitchen/docs/{ja,de}/` re-synced to the rewritten wiki (2cf4190, "caveman-ded") — all 16 files of ja/ and 16 files of de/ mirror the current wiki (headings, links, code blocks, setting keys, hotkeys preserved; stale pre-rewrite content dropped). The dedicated translate instance ran in-role: no spawnable opus/gpt-5 sub-agent exists on this host (spawn_agents refused both), so the `ee` run did the ja/de re-sync directly instead of delegating. Validator PASSED 33/33 @ 939 keys, docs 16/16 x 4. | verify: `ls .saipen/saitranslate/kitchen/docs/{ja,de}` head matches current docs/wiki files

- [x] T-685 (HUNT, dead code) src/fastprompter/ui/saipen_dialog.py orphan — RESOLVED by deletion. The concurrent session's c384711 "chore: drop orphaned saipen_dialog.py" removed the 101-line file; grep now shows zero references in src/tests/tests_smoke (ticket's own verify criterion: zero = deleted). The UTF-16 LOG line from that session ("DEC: orphan deleted, forced ship via ccc") was recovered and re-shaped as E-1130 during the 01.08 09:48 LOG encoding repair. | verify: grep -rn "saipen_dialog\|SaipenViewer" src tests tests_smoke = zero (confirmed)

- [x] T-657 [was H-656] (HUNT, P1, same family as H-652) Dragging a silo into a snippet category shifted the silo LIST and nothing else. `move_preset_cross_category` popped straight out of `temp_presets` and inserted straight into it, never touching `_remap_silo_indices` — so after dragging silo 0 of three away, the silos were ['bravo','charlie'] while the colours were still `{0:red, 1:green, 2:blue}`: bravo wearing the departed silo's red, and a colour parked on slot 2, which no longer exists. Pins, ticks, children, types, project paths and watcher queues all the same. Both directions and both index spaces fixed (silo and arcsilo, removal and insertion) through two named helpers, `drop_silo_state` / `open_silo_slot`; `del_silo` had this written out INLINE and now shares them, which is why the drag path could be missing it in the first place. | verify: 1 smoke test, fails with the remap taken back out
  CHECKED, NOT A BUG: suspected the str_dict remap was insertion-order dependent, since a delete never explicitly drops the removed slot's key — it survives only by being overwritten. Measured both orders ('0','1','2' and '2','0','1'): identical result. The collision is structural (idx and idx+1 both land on idx), not luck. No change made.
- [x] T-654 (feat/fix, user-asked) Session state is now per PROJECT and actually seamless. Three values were global: `active_temp_slot` (one number, clamped to the new project's length on every switch — leave A on silo 7, wander to B, come back and you are wherever B left the counter), and `active_is_archive` + the silo page, which were not in `data` at all, so every restart dropped you in the normal list. Cursor and scroll INSIDE a silo were already per-project via silo_view_state_all; this is the outer half. One `silo_session_all` = {project: {slot, archive}} rather than three `_all` maps — a store someone forgets to register is exactly H-653. The page is deliberately NOT stored: `_switch_to_slot` derives it from the slot, and a second source of truth can only disagree. Capture happens BEFORE the aliases move and against `last_tab_idx`, not `get_current_category()` — the combo has already moved when the signal fires, so the naive version filed the outgoing slot under the incoming project (caught by my own test). Old databases fall back to the global `active_temp_slot`. | verify: 3 smoke tests; the first fails with the old clamp restored
- [x] T-655 (test, closes the H-653 class) Two guards over the settings encoder instead of one more key. (1) Every dict OR LIST default must be in `_JSON_SETTINGS` — lists included because `str([1, 2])` is valid JSON and survives while `str(['a', 'b'])` is single-quoted and does not, so "it is only a list" is luck about the element type, not safety. (2) A full save->reload round trip that gives every structured setting a marker value and names anything that comes back changed — which catches the OTHER half nobody was guarding: a key in the save tuple but missing from the load branch comes back as a raw string. Verified by removing `silo_session_all` from the load branch alone: `silo_session_all -> '{"probe": [...]}'`. | verify: 3 unit tests, each shown failing with its fix backed out
- [x] T-658 [was H-652] (HUNT, P1, data corruption) Undoing a silo delete left every slot-keyed store SHIFTED. A delete remaps colours, project paths, silo types and watcher queues down by one; the undo snapshot carried temp_presets, pins, ticks, children, collapsed and folders, and none of those four. Measured on a 3-silo project: delete slot 0, undo, and the text comes back correct while the colours stay `{0:green, 1:blue}` — alpha wears bravo's colour, charlie loses its own, forever. Project paths matter more than looks: a silo ends up pointing at another silo's project folder. Fixed by carrying all four in `_snapshot_current` and restoring them through the `_all` store + alias, like the six that already worked. `state.get(key) is None` skips rather than clears, so an undo entry written by an older build cannot wipe a user's colours. silo_gaps stays out, per T-593: a gap is a position, not a property. | verify: 1 smoke test that fails with the restore disabled
- [x] T-659 [was H-653] (HUNT, P1, silent data loss) A silo's Table/Kanban type NEVER survived a restart. `silo_type_all` was in neither the JSON-encode tuple nor the load tuple, so it was written with `str()` and came back as the string `"{'Code': {'0': 'table'}}"`; the `isinstance(dict)` guard in main.py then discarded it silently. Measured with a save/reload round trip against a scratch DB: `silo_colors_all` returns a dict, `silo_type_all` returns a str. Root cause is structural, not a typo — that key tuple was written out INLINE TWICE and the copies had already drifted (`window_presets` was in one only). Both are now one `_JSON_SETTINGS` constant behind `_encode_settings()`, with `silo_type_all` added to it, to the load branch, and to the defaults. | verify: 2 unit tests — a round trip, and a guard that walks the defaults and names any dict setting missing from the tuple (it names silo_type_all when the fix is backed out)
- [x] T-651 (fix, P2, same class as H-648) The files root is user-chosen through a QFileDialog, so it can sit on a share — and `_files_root()` validated it with a bare `os.path.isdir`, on the GUI thread, from the silo-refresh path. Measured with an unreachable UNC: 79.97s. Fixed at the choke point rather than at the 14 call sites: `paths.isdir_within` (the `exists_within` probe generalised into `_probe_within`) plus a 5s cache, because the answer is asked once PER SILO and a 0.25s bound x 20 silos is its own stutter. The fallback to the local data dir is what the old code did anyway — after the stall — so only the stall is gone. Everything downstream (os.listdir/walk/copy in file_container, trash, snippet ops) inherits the fix: with the root unreachable they are handed a LOCAL path. HONEST LIMIT: a root that probes OK and dies mid-operation still blocks; that is a user-initiated file operation, not a background refresh, and I left it. | verify: 3 tests on isdir_within + 1 smoke test that fails at 79.97s without the fix and passes in 2.4s with it
- [x] T-633 (cleanup) `state._export_md_backup` DELETED, with its nine tests and its orphaned `_safe_write`. No production caller has ever existed: dead code that reads like a safety net in review AND in the test list is worse than no backup, because it makes the reader believe a mirror exists. The real thing is `utils/portable_backup.py` — dated, per-project, and actually wired into `save_data_to_db`. Checked before cutting that `backup_dialog` only borrows `_sanitize_cat_name` (kept) and does not read the `~/.fastprompter/` layout the dead writer would have produced. | verify: tests/test_state.py 35 passed, ruff clean
- [x] T-660 [was H-648] (HUNT, P1, THE reported crash) "App sometimes crashes when I paste text" was never a crash — it was a 93-SECOND freeze, which Windows paints as "Not Responding" and a user kills. `editor.insertFromMimeData` turns a pasted path into a markdown link, so it probes the filesystem for EVERY short single-line paste, with a bare `os.path.exists` on the GUI thread. Measured on this machine: a named host that does not resolve fails in 0.057s (DNS says no), but an IP-literal UNC nobody answers takes **93.043s** of SMB connect. New `paths.exists_within()` runs the probe on a throwaway daemon thread and only uses an answer that arrives in 0.25s; a timeout means "not a path", so the text pastes verbatim — the safe outcome. Same measurement through the helper: 0.256s. | verify: 4 tests on the bound + 1 smoke test that FAILS at 81.8s without the fix and passes in 1.9s with it
- [x] T-661 [was H-649] (HUNT, P1) The suite had never once run as a single command, and that hid two things. `pytest tests tests_smoke` died with 8 collection errors: four unit files assigned `sys.modules["PyQt6"] = MagicMock()` at import time and never restored it, so `class FastPrompter(QMainWindow, ...)` raised "metaclass conflict" and eight tests_smoke FILES never ran at all. Green only ever meant "two separate commands". Eight files in total leaked (found the other four after the first grep truncated at `head -20` — my error). New `tests/_qt_stub.py` puts sys.modules back. TWO traps on the way: reverting sys.modules wholesale evicts freshly imported C extensions, and re-importing one runs its init twice — STATUS_STACK_BUFFER_OVERRUN (0xC0000409), no traceback, in an unrelated file; the helper now only touches its own stub keys plus pure-Python `fastprompter.*`. And `test_cursor_theme` was LIVING on the leak: with real PyQt6 it calls `QPixmap` with no QApplication, which is itself an instant 0xC0000409 — that test moved to tests_smoke where Qt is real. `test_formatting_mixin` also patched `markdown.markdown` globally forever; now an autouse fixture. | verify: 1542 passed, 1 skipped, 0 failed in ONE command, 13:26
- [x] T-662 [was H-650] (HUNT, P2, same class as H-648) `editor.py` revealed a file with `subprocess.run(["explorer", "/select,", path])` — which WAITS for explorer to exit, on the GUI thread, no timeout. Its sibling in `file_container.py:663` already used `Popen`. Nothing needs explorer's exit code. Popen now, both places. | verify: the existing reveal test, rewired from patching `run` to `Popen` (left as-is it would have launched real Explorer windows)
- [x] T-646 (fix, user-reported) Adding a project with Number Tabs on did not add its button until the setting was flipped twice. FOUR call sites changed the project list and none rebuilt the row — add, delete, rename, opening Trash. Adding four calls fixes today and leaves the fifth site anyone writes next year broken the same way, so the row listens to the combo MODEL (rowsInserted/rowsRemoved/dataChanged), coalesced onto the next tick because refilling the combo fires a signal per row. b5248c4 | verify: 4 tests + a 12-path desync sweep that now reports 0
- [x] T-647 (fix, P1, found by fuzzing) The recorded silo type and the actual text drifted apart — 17 mismatches in 220 random steps. Undo restores text without restoring type; a paste can leave a "kanban" silo holding prose; the result was a board widget over prose, holding a document it does not own. Three fixes from one hunt: `_apply_silo_type` falls back to the editor when the structure is absent (empty still counts as ready); the page is re-picked when structure presence FLIPS, not only on a silo switch; and seeding reads the live editor instead of the lagging `temp_presets`. Verified on the four real user paths. e2ed7be | verify: 5 tests
  HONEST LIMIT: the fuzz still flags orders the app cannot produce (serialising a HIDDEN widget, interleaved transforms). Not chased — that is the harness, not the app.
- [x] T-645 (feat) Double-click a pasted image's pill to rename it — file on disk AND the link, one undo step. `paste-20260730_140826.png` records when it arrived and nothing about what it is. The pill's geometry existed only inside paintEvent, so there was nothing to hit-test; `_image_pill_rect` derives it from cursor rects the same way the paint pass does, so the two cannot disagree. Extension kept when omitted, illegal chars replaced, existing name not clobbered, a failed rename leaves the link alone rather than pointing at a missing file, and the focus lock is held across the dialog so the window does not hide mid-rename. Code landed in c04c3e8 (another agent's `add -A` swept my unstaged work), tests in 80d25ec | verify: 7 tests + live paste-and-rename
- [x] T-295 (P2) DONE 06.08 -- the scoped conversion the ticket asked for, driven by evidence rather than a sweep: of the four failures this project has been calling 'pre-existing order pollution' for weeks, exactly TWO are that class (`header_fits_quarter_fullhd_with_full_clock`, `a_degenerate_box_width_still_takes_a_click` -- green alone, red in a full run). Both moved onto `fresh_win`, both now green. The other two fail in ISOLATION and are therefore real defects that were mislabelled: ticketed as T-739 and T-740. Cost of the conversion: ~4s, against the ~16min a wholesale function-scope suite would have cost, which is the trade the ticket explicitly refused. Original: UNBLOCKED 27.07, partially done. `tests_smoke` still shares ONE module-scoped `win`; the remaining work is converting tests that actually need isolation onto `fresh_win`. NOT doing a wholesale conversion: a full function-scope suite costs ~1.9s x 509 = ~16 min/run against 2:51 now. | verify: per-test, as tests are moved

> CLEAN 01.08.26: 72 older DONE tickets pruned from the board (T-644..T-684).
> Nothing is lost - every one of their events lives in the append-only
> `logs/LOG-001.md`, and the user-facing half in `CHANGELOG.md`. The board
> is read on every cold start, so it holds the CURRENT work; history has
> its own home. Pruned IDs are never reused (RFC § 1.2): the next ID is
> one above the highest across BOARD *and* the sealed LOG.
