# ASP Board

> NOTE 25.07: a saitranslate INIT wiped this board (35KB -> 236B) plus LOG and
> STATE at 24.07 23:50. `.saipen/` is gitignored, so there was no git fallback.
> LOG was restored from the newest backup + splice. This board was NOT fully
> rebuilt (user call — too token-expensive); the pre-wipe backlog survives only
> in `recovery/20260725T004633Z-WIPED-BOARD.md` and
> `recovery/20260721T213816Z-BOARD.md`. Pull tickets back from there on demand.

## DOING

## TODO

- [ ] T-633 (P3) `state._export_md_backup()` has nine unit tests and NO production caller — dead code that reads like a safety net. Either wire it up (a flat, always-current mirror under `~/.fastprompter/` complements the dated snapshots) or delete it with its tests. Do not document it until then.

- [ ] T-617 (P1) Paste crash NOT reproduced. User reports the app dying on paste, no traceback (so: access violation, not an exception). Probed offscreen AND on the real windows platform: plain/multiline/3k-line markdown/200k one-liner/HTML-flavoured mime/image mime, Ctrl+V through the real clipboard, with the event loop running and silo switches + apply_theme interleaved — 0 crashes. crash.log holds only a stale ImportError from 22.07; fastprompter.log has no traceback. T-611 fixed a real leak on that path (see DONE) but it is NOT proven to be the reported crash. USER DETAIL 28.07: it was silo #7 of the SAIPEN project, a silo that HAS user gaps set (silo_gaps). User cannot reproduce it either — big pastes are eaten fine now — so this is parked, not chased. Do not re-hunt it blind; wait for a second occurrence and grab the state at that moment. NOTE the gap angle is untested: T-593 moved silo_gaps out of _SILO_INDEX_STATE, and SiloGapBar frames are pooled — a paste that triggers refresh_temp_presets while a pooled gap frame is being reused is the one path I never probed.

## BLOCKED

- [ ] T-555 (P1) Document jumps to top on wheel. Mechanism confirmed: a scroll-RANGE collapse clamps value to 0. 3-hypothesis cap reached. DEAD: wheel-scrolls-to-0, WheelPager bubbling, stale fold range. UNTESTED: document replaced mid-wheel by autosave/silo switch; viewport resize re-wrap. | verify: reproduce a plain wheel that collapses the range
- [ ] T-295 (P2) UNBLOCKED 27.07, partially done. `tests_smoke` still shares ONE module-scoped `win`; the remaining work is converting tests that actually need isolation onto `fresh_win`. NOT doing a wholesale conversion: a full function-scope suite costs ~1.9s x 509 = ~16 min/run against 2:51 now. | verify: per-test, as tests are moved

## DONE

- [x] T-632 (fix, P1) The daily Markdown snapshot exported only the ACTIVE project's silos. `_do_export` read `data["temp_presets"]`, which is an alias for the current category, so a user with five projects had four missing from `Documents/.fastprompter/<date>/` — and the folder looked full, so nothing indicated the gap. Now per project (`silos/<project>/`, `archive/<project>/`) like snippets already were, manifest counts all of them. NOT data loss and not a regression: the DB, its .bak and the undo file always held every project — one backup layer of several was quietly thinner. Found by WRITING THE README claim down and checking it; the claim did not survive. 0b5ecc2 | verify: 4 tests + the real EXE wrote silos/Code, /Misc, /Text, /Trash
- [x] DOC-01 README gained a "Reliability & data safety" section: one-line defence-in-depth framing, the layers under a <details>, and a second <details> with the honest limits (synchronous=NORMAL loses at most the last transaction, .bak is one generation, snapshots keep 7 days, T-617 still open). Deliberately left OUT `_export_md_backup` to `~/.fastprompter/` — it has tests but no production caller, so advertising it would have been a lie. Fixed stale facts while there: 5 projects -> 100, 22 language names under a 32 heading, test counts 461/104 -> 884/589.
- [x] REL-0.8.1a Released. https://github.com/vacterro/FastPrompter/releases/tag/v0.8.1a
- [x] T-631 (fix, found in review) Every SiloKanban op rewrote the WHOLE silo, and replacing text destroys the QTextBlocks it spans — with them the margin marks of lines that have nothing to do with the board. Measured: two marked notes above a board lost their marks the moment a card moved. Each op now rewrites only what it changes (board span for a move, one line for a tick, span+1 for a new card) and `_replace_lines` remembers the marks it is about to destroy BY CONTENT, so a moved line keeps its own and a rewritten line does not inherit a stranger's. Also fuzzed both models 400 trials x 12/15 random ops: no card or column lost, render idempotent, 0 failures. 2cbbf96 | verify: 2 tests
- [x] REL-0.8.1 Released. tests_smoke 589, tests 880+1 skipped, ruff clean on the new modules, EXE built (28.3 MB) and LAUNCHED before publishing. https://github.com/vacterro/FastPrompter/releases/tag/v0.8.1
- [x] T-630 (feat) SiloTable + SiloKanban. What existed was two text dumps: Insert Table wrote "Row 1" into every cell and aligned nothing, Insert Kanban wrote a markdown TABLE with checkboxes — looked like a board, no card could move, because a table cell is not a card. Both are markdown BY DESIGN: a silo is plain text (DB, disk mirror, paste into an agent), so a QTextTable survives until the first save and no longer. New `silo_table.py` and `silo_kanban.py` are pure text in/text out, no Qt, so they test without a window. Editor gets Tab/Shift+Tab cell walking (Tab off the last cell grows a row), Enter = new row not a split row, Alt+arrows to move a card, context submenus, all through the edit_block guard as single undo steps. 93d9cfb | verify: 26 unit + 13 editor tests, driven end-to-end on a real window
- [x] T-629 (fix, P1, user-reported) Normal Window showed its title bar only from the third click. WS_CAPTION was set correctly on the FIRST one — measured — but Windows does not recompute the non-client area for a style change, so the window rect and client rect stayed identical (both top=120) until something else forced it. SetWindowPos SWP_FRAMECHANGED after the re-show. The toggle also walked the window: setGeometry pins the CLIENT (gaining a caption pushes it down by 23px), move() pins the FRAME (losing one pulls it up 4px), so the frame is anchored and the client offset derived from the margins the new frame actually has. Three cycles now leave the rect bit-identical. 5d7a8da | verify: 3 tests + caption screenshotted on the real platform
- [x] T-628 (fix, P1, long-standing) The cropped-icon bug, properly. Swept 9 themes x 5 scales first: 5-63 clipped buttons per combination (Vintage Classic 58 at 100%), i.e. T-627 had only fixed the header. `enforce_button_fit()` measures every button after theme+scale+panels settle and guarantees the label fits the STYLE's content rect; font gives way while the box is fixed, only a fixed box is grown, and a word in a free box is left to Qt's eliding (forcing those wider wraps the settings panel into rows — T-605). Marks are tagged centrally BY LABEL wherever the button came from: half the app's buttons never touch apply_button_size. The pass restores the intended font before re-measuring so it is not a ratchet, and repolishes a tagged button that still does not fit (a property set during construction is never re-evaluated against a stylesheet that did not exist yet). ac8396b | verify: sweep 0 clipped across 45 combinations, held over the whole suite
  Also found doing it: the settings footer FlowLayout kept its previous-width height on the FIRST fit — 194px against a 163px hint, ~100px of dead panel, exactly the T-605 complaint. Window tab 414 -> 287. The test's flat 320px cap became a content-relative check, since a flat number goes stale the moment a tab gains a control and then reads as the bug returning.
- [x] T-627 (fix, P1, user-reported, long-standing) Toolbar icons "white/broken on Vintage Classic since the first build". NOT colour — the glyphs were CLIPPED to a narrow vertical slice by the theme's TEXT padding. Measured via QStyle SE_PushButtonContents (contentsRect() ignores stylesheet padding and reports the full 20x20, which is why size checks never caught it): Vintage Classic border 2px + padding 3px/6px leaves a 4x10 content rect, Golden Default's 1px + 2px/4px leaves 10x14, and the glyph needs 15px — one theme looked broken, the other nearly right, same bug. apply_button_size tags "fp_icon_button" by LABEL (📁 is an icon at any size; NEW/Save are words and keep their padding) and the app sheet drops padding for that property; apply_theme repolishes, since a property set after polish does not re-evaluate the sheet. After: 16x16 / 18x18, whole glyphs. c1438df | verify: 4 smoke + 2 unit tests, header rendered before/after on both themes
  NOTE: I first built a contrast fix (colour emoji are bitmaps that ignore the pen, and the pale ones really do score 1.6-2.4 against #c0c0c0) and binned it when the zoomed screenshot showed clipping, not washout. The contrast numbers are real if it ever comes up: 📄 1.59, 📁 2.05, 🗂 2.08, 🖱 2.11, ⚙ 2.23, 📋 2.29, 💾 2.35 on Vintage Classic.
- [x] T-620 (fix, P1, mine in 9b612f7) Dead gap at the top-right, right cluster collapsed leftwards. The order-healing anchor search skipped "<stretch>", so every token defined after one was inserted IN FRONT of it; on a fresh profile both stretches trailed. Stretches are anchors now and an empty saved order short-circuits to the default. dfe7b18 | verify: 2 tests + measured btn_help ending at the header edge
- [x] T-621 (fix, P1, mine in T-615) Hamburger grew the sidebar instead of hiding it with the sidebar on the right: toggle_sidebar_visibility indexed it as "pane 1 when right", which the files dock made the CENTRE. Panes by identity; toggle_focus_mode had the same 2-element assumption and left a pane open in Zen. dfe7b18 | verify: 2 tests, both sides
- [x] T-622 (feat) 📁 mirrors the sidebar side — dock opens opposite, so the button sits next to the settings gear. dfe7b18 | verify: 1 test
- [x] T-623 (feat) Vision button cycles Source View / Live Preview / Reading; preview_combo stays the data layer. In _DENSE_HIDDEN — the same modes are one click away in the footer. dfe7b18 | verify: 1 test
- [x] T-624 (feat) Projects reorderable in the manager (▲ ▼). cats_order rebound in place: it is aliased elsewhere and reassigning would orphan those readers. dfe7b18 | verify: 1 test
- [x] T-625 (feat) Three-stage Ctrl+D: Zen, Solo (all other desktop windows minimised), back. Every exit route restores the desktop. cycle_focus_mode is SEPARATE from toggle_focus_mode — folding the stages in made "toggle twice" mean "sweep the desktop" for a dozen callers, and the fuzz test does exactly that, so the suite minimised the developer's real windows mid-run. Found by bisecting a watcher-test failure (40 runs of shrinking node lists) down to test_fuzz_ui_surfaces. zen_desktop also refuses to act in an offscreen Qt session. dfe7b18 | verify: 4 tests incl. one pinning that the plain toggle never sweeps
- [x] T-626 (feat) Session-state audit by real restart (mutate, tear down, rebuild on the same DB). Lost: sidebar collapsed with the hamburger (sizes were only written by splitterMoved) and an open files sidebar. Both persist now; the other eight states already survived. dfe7b18 | verify: 3 restart tests
- [x] REL-0.8.0 Released. tests_smoke 563, tests 852+1 skipped, EXE built (28.2 MB) and LAUNCHED before publishing — window up, header hugging the right edge. https://github.com/vacterro/FastPrompter/releases/tag/v0.8.0
- [x] T-619 (fix, P1, user-reported) Number Tabs made the project combo vanish with nothing in its place and ate the sidebar hamburger. `cat_numbox` was never added to DEFAULT_TOOLBAR_ORDER, and `apply_toolbar_order` detaches every header child then re-adds ONLY listed tokens -- so the widget was orphaned at (0,0), invisible AND painting over the corner. T-607's tests checked the button list but never that the widget is in the header layout; that assertion exists now. Also stopped `_toolbar_order_list` appending new default tokens at the very end: a token defined next to cat_combo/lbl_line_count landed after the help button for anyone with a saved toolbar_order. 9b612f7 | verify: 4 tests + real window (boxes present, hamburger intact)
- [x] T-618 (feat) Drop system matched to the docked panel. An open files sidebar re-points at the silo you switch to -- floating, a stale panel was just stale; docked, it is what a drop lands in, so it would file the drop under the previous silo. Floating panels deliberately do NOT follow. Panel shows a drop target (outline + hint): as a window the title bar said where the drop would go, docked it is just another strip. 9b612f7 | verify: 4 tests
- [x] T-615 (feat) Files panel docks as a third splitter pane (Settings > Window > Layout > Files Sidebar), always on the side the silo sidebar is not. `set_docked()` flips the window flags — a Qt.Tool widget cannot go in a layout — and `open_for` stops raising/activating when docked (it would steal focus from the editor). 📁 became a real toggle in that mode. `apply_sidebar_position` no longer indexes panes as 0/1 by hand: with a third pane that was wrong on BOTH sides, so panes are read by identity and a short saved size list is padded. Flipping the sidebar left the dock at 0 width; fixed. fbaf9a7 | verify: 6 tests + both sides checked on a real window
- [x] T-616 (feat) Fast Ctrl+Q. No picker: each press steps to the next zone of the chosen page and cycles. `open_for`'s setup split into `_prepare()` so `apply_fast` never builds the overlay. Page picker beside the toggle, refilled after the presets dialog (the Presets page only exists once a preset is saved). fbaf9a7 | verify: 4 tests
- [x] T-610 (fix) Line numbers overlapped. The gutter drew every block's number at its top in a box a full font-height tall, whatever the block's REAL height was — and the highlighter collapses `---` rules, image refs and concealed markup to 1pt (~2px), so those digits landed on the next line's. `gutter_rows()` yields (block, top, row_height) and skips any block inside the band the previous number occupies; marks and the queue stripe use the real row height. 66dd44b | verify: 2 tests + observed on a real window
- [x] T-611 (fix, P1) `set_active_document` connected `lambda *_a: self.refresh_extra_selections()` to every document and disconnected NOTHING. Documents outlive the swap (one per silo), so every switch BACK to a silo stacked another copy on the same document — A/B measured 4 -> 14 receivers over ten round trips at HEAD, flat at 4 after — and the connection outlived the editor, which is the H-406 access-violation class. Named method, disconnected on swap, guarded; `_stamp_edited_blocks` got the same guard. Found while hunting the paste crash; NOT proven to be it (T-617). 66dd44b | verify: 2 tests incl. the receiver count
- [x] T-612 (feat) Number boxes wrap into a grid — 100 boxes in one QHBoxLayout is ~2200px of header that runs off the window. `numbox_per_row` + `numbox_btn_size` in Settings > Window > Layout. 66dd44b | verify: 3 tests
- [x] T-613 (feat) Timer Minutes toggle: `format_remaining(minutes=True)` keeps the minute field at any magnitude ("4d 11h 05m", not "4d"), overriding `short`. 66dd44b | verify: 1 test
- [x] T-614 (feat) Token estimate beside the line count, toggleable, chars-per-token or tokens-per-word weighting, click the label to flip mode. Registered in DEFAULT_TOOLBAR_ORDER — apply_toolbar_order re-adds ONLY listed tokens, so an unlisted header widget disappears at the first rebuild. 66dd44b | verify: 4 tests
- [x] T-604 / T-608 re-verified against the user's list: the timer toast already shows name + description verbatim + "Time's up" (no estimate boilerplate), and Ctrl+Q presets already have the on/off toggle, reorder/rename/re-capture dialog and window state. Both were stale unchecked items, no code needed.
- [x] H-411 (HUNT, P1) T-295's recorded blocker was WRONG and it cost that ticket months of BLOCKED. The board blamed `QApplication.setStyleSheet()`; measured, the app stylesheet is CONSTANT at 2458 bytes across every cycle. Real cause: `QApplication.processEvents()` does not deliver DeferredDelete, so `deleteLater()` never lands and each window leaked its entire tree — +1397 widgets and +11 top-levels per window, construction climbing 1.2s -> 15.0s over six. A `gc.collect()` across that pile of half-dead PyQt wrappers is what produced the "access violation": SIGSEGV at the third window. Isolated by A/B on identical probes with and without gc.collect (139 vs 0). One missing line: `sendPostedEvents(None, QEvent.Type.DeferredDelete)`. With it, widgets flat at 1, cost flat ~1.9s, gc safe. Added `_teardown_window()` (the module fixture also closed AFTER scheduling the delete) and a `fresh_win` function-scoped fixture. | verify: 3 tests; suite 512 passed EXIT=0 in 2:51, down from 3:29
- [x] T-609 (feat) Hotkey summon puts the caret in the text silo. `show_window` already ended in `text_area.setFocus()`, but `activateWindow()` is ASYNC on Windows — the window is not active yet when that focus is applied, so the caret often did not land and the first keystroke went nowhere. Re-applied on a 0ms singleShot on the by_hotkey path only, through a guarded `_focus_text_silo()` (deferred call into a destroyed widget = access violation, H-406 class). Also hoisted `QTimer` to a module-level import in window_mixin — the new call site had no local import and would have NameError'd on every summon. | verify: 4 tests
- [x] H-409 (HUNT, P1) `_rebuild_cat_numbox` called `deleteLater()` on the old number buttons without unparenting them. `deleteLater` only SCHEDULES the destructor; the widget stays in `self.findChildren(QWidget)` until the event loop flushes, and theme_mixin.py:229 walks exactly that list calling styleSheet()/unpolish/polish. Any theme or font apply landing in that window touched a C++-dead object — access violation, no traceback, process gone. In the app that is a random hard crash on switch-project-then-theme. Measured: theme subset crashed 2/5 in-tree vs 0/5 at HEAD; `setParent(None)` before `deleteLater` -> 8/8 clean. | verify: 3 tests (source order, child-tree behavioural, rebuild->apply_theme->apply_font end to end)
- [x] H-410 (HUNT, P1) `test_add_category_capped_at_five` pinned the OLD cap. At cap 100 the guard no longer fires, so the test fell through to `QInputDialog.getText` — modal, and only QMessageBox was patched — which hung tests_smoke indefinitely offscreen. This is what ate several full-suite runs (one sat blocked for hours). Retargeted to the real cap plus a 99-case asserting the guard does NOT fire below the boundary. | verify: 2 tests, 1.36s, no hang

- [x] T-568 (fix) Buttons washed out on Vintage Classic. My first pass measured SIZES and closed it "not reproduced" — wrong axis. The user's screenshots showed the real fault: contrast. A sweep of every theme's style keys against its own background found exactly two below 3.0, both here: `btn_new` #5e7a7a on #c0c0c0 = 2.54, `lbl_help` #808080 on #c0c0c0 = 2.17. Now #3a5e5e (4.1) and #505050 (5.1). No other theme has a key under threshold. | verify: 2 tests
- [x] T-604 (feat) Timer toast drops the "Estimated reset window" substitution — a description containing "assumed" was silently swapped for boilerplate plus a two-line tooltip. The timer's own description is shown verbatim now. | verify: 1 test
- [x] T-605 (fix) First settings open left an empty strip at the bottom. `_fit_settings_tabs` measures the visible page at `tabs.width()`, which is still 0 the first time the frame is shown, so `avail` fell back to the 120px floor and the wrapping layout reported a much taller height than it needed. Fitted twice now: once inline, once on a 0ms singleShot after layout resolves the real width. | verify: suites green
- [x] T-606 (feat) Cursor blink speed is configurable — Editor > Typing, 0..2000ms, 0 reads "No blink" (Qt treats it as a solid caret). Applied through `QApplication.setCursorFlashTime` and re-applied at boot. | verify: 2 tests
- [x] T-607 (feat) Number-box project switcher — opt-in (Window > Layout) row of numbered buttons replacing the project dropdown, checked button marks the active one, right-click gets the same add/rename/delete menu, wheel still switches. The combo stays as the data layer, so `_cat_at`/`get_current_category` are untouched. Project cap raised 5 -> 100. | verify: 8 tests
- [x] T-608 (feat, COMPLETED 27.07) Ctrl+Q window presets. First pass shipped only save/delete/apply; the user pointed out the ask also had reorder, edit, an on/off setting and window STATE. All four added: WindowPresetsDialog (up/down, rename, re-capture, delete, add-current) reached from Window > Window presets; `window_presets_enabled` toggle; presets carry `state`, and a maximized preset is applied via showMaximized() because setGeometry on a maximized window is ignored and a rect alone does not restore "maximized". Names now render in the picker instead of bare digits. Re-capture keeps the entry's name AND slot -- the picker alone could only delete+re-add, moving it to the end and losing the name. Storage moved to dicts; both _load_presets and _save_presets accept the legacy [x,y,w,h] (save previously raised TypeError on it -- a real asymmetry, not just a test artifact). | verify: 13 preset tests
- [x] T-608 (feat, first pass) Ctrl+Q window presets — a third picker page, "Presets", holding up to 10 user-saved window rectangles. `S` saves the current geometry, `Delete` removes the hovered one, `1`..`0` applies. Stored as screen FRACTIONS like the builtin zones, so a preset survives a monitor change; the page only appears once something is saved. `window_presets` added to the JSON-serialised keys both ways. | verify: 5 tests incl. a real DB round trip
- [x] H-407 (HUNT, P1) `show_cat_context_menu` ended in `self.cat_combo.mapToGlobal(pos)`. In number-box mode the combo is HIDDEN, and mapToGlobal on a hidden widget returns a stale origin — the right-click menu would open away from the cursor. Takes an `anchor` now; the number button that was clicked passes itself. Found by hunting T-607's own code. | verify: 2 tests
- [x] H-408 (HUNT, P2) `build_categories` rebuilt the number row AFTER `setCurrentIndex`, and that setter fires `on_tab_changed` -> `_update_cat_numbox_active`, so one highlight pass ran against the row that was about to be thrown away. Rebuild moved ahead of the index change. `rebuild_cat_combo` was already correct (it blocks signals across the switch). | verify: 2 tests, one behavioural
- [x] SYS-02 (rest) `.saipen/` untracked in git was the reason the 24.07 wipe had no fallback. User's call: track it ("пусть будет + реклама SAIPEN"). The ignore line is gone.

- [x] H-406 (HUNT, P1) `_refresh_checkbox_flag` and `_apply_header_density` both fire from QTimer.singleShot and both called a Qt method (self.document() / self.width()) before any deletion check — a dead C++ object is an access violation, not an exception, so it kills the app with no traceback. Same class as T-295's teardown crashes and C-13. Found by sweeping all 20 singleShot sites for guards. 9031108 | verify: test asserts the guard exists AND precedes the first Qt call; 480/851
- [x] T-566 (refactor) Deleted dead `toggle_header_line()` — 102 lines nothing in the app called (Ctrl+E and the H button both go to apply_header_timestamp). The blocker was the fuzz test: random.choice draws a variable number of bits by sequence length, so shrinking the 6-item op list reshuffles every later fuzz op. List kept at SIX (toggle_quote_conversion took the slot) and the comment says the length is load-bearing. 1a2bb4c | verify: 479/851, identical counts
- [x] SYS-02 (partial, feat) `tools/saipen_snapshot.py` — timestamped generations of STATE/BOARD/LOG under `.saipen/snapshots/`, hash-skipped when unchanged, `--keep` pruning (default 30), root found by walking up for `.saipen/`. Additive only: does NOT touch .gitignore. 3155d7b | verify: snapshot / no-op / change-detect / --force / --list / prune 4->2 all exercised. REMAINING HALF: whether to track `.saipen/` in git is still the user's call.
- [x] T-001 (moot) saitranslate init — closed. Its premise was the dead `.saipen/extensions/saitranslate/` scaffolding created by the 24.07 INIT, removed under SYS-01. Canonical `.saipen/saitranslate/` is phase DONE, 33 langs. Nothing to initialise.
- [x] SYS-03 (fix) Coverage data lied and the validator repeated it. TEN locales stored coverage_pct 100.0 while missing keys (tur 785/802, nine others 1 each). Wrote the real numbers, recorded _meta.missing_keys, and made validate_saitranslate.py COMPUTE coverage against en.json instead of echoing the stored field — it now warns per gap and no longer prints "(100% OK)" beside a warning list. Translations deliberately NOT fabricated: the app already falls back to EN for a missing key. | verify: validator prints TUR 97.9% + 10 warnings, "PASSED with 10 warning(s)"
- [x] T-603 (feat) Obsidian-style Hide Markup — opt-in toggle hides **, *, __, ~~ and ` markers so text reads as rendered; the caret's block keeps its markers so it stays editable. Transparent 1pt trick (same as the `---` rule); document text untouched; repaints only the 2 blocks involved on caret move. 24303b7 | verify: 6 tests
- [x] T-601 (fix) Gap render height floored at 6px — a 2px bar made the Ctrl+drag handle unusable, and a transparent layout-cell widget cannot have a grab zone bigger than itself. Also fixed _apply_header_density's comment, which claimed dense hides 2 widgets while _DENSE_HIDDEN hides 10. 903f2a6 | verify: suites green
- [x] T-599 (feat+fix) Projects manager — Settings button opens a checklist; unticking hides a project from the tab combo without deleting anything, refusing to hide the open project or the last one (24ed359). Follow-up 2fc2f54: build_categories() still iterated cats_order raw, so profile switch / undo / Trash toggle brought hidden projects back — now fills from visible_categories(). | verify: 7 tests total
- [x] T-602 (fix) Toolbar audit — all 55 buttons mechanically checked on a live window: none dead. Added the 6 missing tooltips (whole find/replace bar was unlabelled) and removed 3 dead names from DEFAULT_TOOLBAR_ORDER. 3 guards added. bbf58d3 | verify: 464 smoke
- [x] H-405 (HARDENING, P1) cat_combo row index was assumed equal to the cats_order index in get_current_category/on_tab_changed/del/rename. Since get_current_category feeds the temp_presets/colors/gaps aliases, a mismatch writes silos into ANOTHER project. Rows now carry their own name (_cat_at); rename updates row data too. b5ed728 | verify: 3 tests incl. row-data-disagrees-with-position
- [x] T-600 (fix) Gaps respect hierarchy — a gap on a parent no longer splits it from its children (clears the whole subtree, cycle-safe); Ctrl+drag now re-parks live on every row crossed. Probed first: gaps on a child and across collapse/expand already worked. a581b1b | verify: 4 tests + synthetic drag walks [1][2][3][4]
- [x] T-597 (fix) Colour swatch owns a fixed left column — was after the conditional collapse/tick buttons and inside the child indent, so swatches zig-zagged. Column reserved on colourless rows (click-dead placeholder) so titles align; feature toggle still reclaims it. 52d70eb | verify: 3 tests
- [x] T-598 (feat) Right-click NEW appends at the bottom (append_empty_silo existed, nothing was wired to it). 52d70eb | verify: 1 test
- [x] H-401 (HIDDEN, P1) select_empty_silo wrote children keys as str(np) while every reader uses int — the child was excluded from the top level as "somebody's kid" then never emitted under its parent, so the silo DISAPPEARED from the sidebar. 52d70eb | verify: live before/after + 2 tests
- [x] H-402 (HIDDEN, P2) select_empty_silo never shifted watcher_queues, so inserting a silo at the top handed every queue to its neighbour. Root cause of both: it was the only hand-rolled copy of the slot shift. 51 lines replaced by one _remap_silo_indices call. 52d70eb | verify: test asserts all 9 stores shift, gaps deliberately do not
- [x] H-403 (HIDDEN, P2) Three callers replaced a corrupt list with a fresh one and rebound data[key], orphaning the alias into <key>_all[category] — ticks and pins silently stopped being per-project. Added _slot_list(). 52d70eb | verify: 1 test
- [x] H-404 (HIDDEN, P1) change_profile skipped the int-key normalisation, so switching profiles flattened the hierarchy and killed recency colours until restart (JSON has no int keys). Added _normalise_int_keys(). e031abc | verify: 3 tests incl. one pinning that the DB really returns str keys

- [x] T-592 (feat) Editor `Ctrl+MiddleButton` deletes the line under the cursor with smart list reflow — ordered lists renumber, bullets close the gap, no blank line, one atomic undo. Plain MButton unchanged. b2c14c8 | verify: 5 smoke tests
- [x] T-589 (feat) Sidebar multi-select silos — Shift=range, Ctrl=toggle, plain=switch+clear; golden outline; context-menu batch Save/Delete/Clear; batch delete trashes high-index-first. 6c42d3a | verify: 6 smoke tests
- [x] T-590 (feat) User-defined sidebar gaps — per-category `silo_gaps` aliased out of `silo_gaps_all` like silo_colors, registered as int_list in `_SILO_INDEX_STATE` so reorder/delete remap for free; pooled spacer frames; height reuses `silo_gap_height`. b9c32aa | verify: 5 smoke tests
- [x] T-591 (feat) One-way silo sync to disk — Settings mode (Off/Silo/Hierarchy) + folder picker; writes `<root>/<category>/<NN_slug>.md` on save; never reads back, never deletes; skips unchanged text; handles str/int child keys and parent cycles. 8c93309 | verify: 7 smoke tests
- [x] T-593 (fix) Gaps made positional + draggable. Root cause: `silo_gaps` sat in `_SILO_INDEX_STATE`, so every reorder remapped the anchor and the divider chased its silo — the reported unpredictability. Removed from the remap table; added `SiloGapBar` with Ctrl+LMB drag to re-park a gap; refuses drops onto an existing gap / out of range; `prune_silo_gaps()` drops anchors past the end. efdb035 | verify: 6 new tests + live Ctrl+drag via synthetic events (row 1 -> row 4 = [4]); tests_smoke 436, tests 851
- [x] T-569 / T-570 `tests_smoke` teardown segfault fixed (adopted + finished an unlogged WIP). 80fde60 | verify: tests_smoke green, no crash
- [x] T-337 (P5) [from saihunt HUNT-005] `fix_tests.py` + `fix_tests_2.py` moved to `scratch/`.
- [x] SYS-01 Duplicate saitranslate root resolved. `.saipen/saitranslate/` kept (33 langs, 802 keys, 100% coverage, phase DONE); `.saipen/extensions/saitranslate/` was a dead half-migration from the 24.07 INIT — 3 scaffolding files, `phase: INIT`, `agent: none`, no locales — preserved to `recovery/20260725T010951Z-DEAD-extensions-saitranslate/` then removed. The 4 `scratch/*_saitranslate.py` scripts had already been repointed at that non-existent path (so they were broken) and are now back on the canonical root. | verify: `python scratch/validate_saitranslate.py` -> VALIDATION PASSED (100% OK); no src/ dependency on saitranslate

Suite baseline 27.07: **tests_smoke 519 passed (EXIT=0, 3:07), tests 851 passed, 1 skipped.**
Note: HEAD 9031108 measured 480 passed but EXIT=139 (SIGSEGV at teardown). After
H-409's setParent(None) fix the run exits 0 — the teardown segv did not recur in
3 consecutive full runs. Not asserting causation, just recording it.
Run unbuffered (`python -u`): stdout block-buffering made a hung run look like a
crash at a much earlier test and cost a lot of time chasing the wrong thing.

Older baseline at 24ed359: **tests_smoke 471 passed, tests 851 passed, 1 skipped.**
(Run them separately — the two suites cannot share one pytest process, see T-283/T-284.)
