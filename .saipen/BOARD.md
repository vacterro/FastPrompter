# ASP Board

> NOTE 25.07: a saitranslate INIT wiped this board (35KB -> 236B) plus LOG and
> STATE at 24.07 23:50. `.saipen/` is gitignored, so there was no git fallback.
> LOG was restored from the newest backup + splice. This board was NOT fully
> rebuilt (user call вЂ” too token-expensive); the pre-wipe backlog survives only
> in `recovery/20260725T004633Z-WIPED-BOARD.md` and
> `recovery/20260721T213816Z-BOARD.md`. Pull tickets back from there on demand.

## DOING

- [ ] T-1018 (P2, user-request, ui) DOING 19.08.26 -- UX fixes from user: (1) Ctrl+Z no longer drops FastPrompter behind other windows (_bring_to_front after undo/redo in main.py); (2) Shift+NEW inserts empty silo ABOVE selected, Ctrl+NEW BELOW selected (select_empty_silo modifier-aware, remap `i+1 if i>=pos else i`); (3) Shift+MiddleClick on a silo clears it (text only, files untouched, undoable) via clear_silo(); (4) Ctrl+Drag gap keeps its name -- move_silo_gap now moves the silo_gap_names entry with the anchor slot; (5) Shift/Ctrl+NEW no longer lands a new silo BEYOND a hugging gap -- gap under the selected (Ctrl/below) relocates to sit under the new silo, group stays intact. All py_compile clean, uncommitted (LOG E-771..E-775). | verify: py_compile clean; manual UI check pending (win fixture headless) | owner: opencode

- [ ] T-1008 (P2, bugfix, timer-wave) DONE 19.08.26 -- Live Preview header regression proven + duplicate rehighlight collapsed. New tests/test_markdown_highlighter_wave.py 13p (small/large degraded, headers top/middle/below viewport, Live->Source->Live no scroll, silo swaps, conceal toggle, heading edit; layout-format probe -- block.charFormat() hides per-layout formats on this Qt build). Call-count tests PROVED double rehighlight with conceal ON -> main.py _apply_conceal_mode returns whether it rehighlighted, theme_mixin sync skips its own when so (exactly one). 2 stale skip-tests re-cut to degraded contract in tests/test_markdown_highlighter.py (35p). Combined-suite hygiene: test_timer_fire main import lazy + test_sound_manager pops cached module before stub import (any file order green). | verify: wave 13p; highlighter 35p; full tests/ 1291p/1s -- single baseline red test_themes_headers.py::test_the_calendar_popup_carries_its_own_copy proven red at HEAD 67d182f (zero diff, theme-CSS, T-1010 scope, untouched); ruff clean on touched files; bandit -ll 0; compileall clean; smoke CI-only | owner: opencode

- [ ] T-1007 (P1, bugfix, timer-wave) DONE 19.08.26 -- fire-path truth: canonical `_play_timer_sound(timer, fired_at=None) -> bool` in main.py (choose_timer_sound + play_sound_ref), _notify_timer uses it; topbar_only=True regression fixed + companion test; tests/test_timer_fire.py exists + 8p (hidden-from-topbar still fires, missing-sound-ref scheduler survives); stale smoke spy updated. SECOND WAVE (19.08.26, uncommitted) snooze/lifetime/recovery: REPRO -- Snooze on a fired REPEATING timer re-armed the object collect_due had already advanced (series silently shifted); old profile's toast could mutate the new profile's data; delayed Test notifications were fire-and-forget QTimer.singleShot (survived profile switch, leaked past shutdown); one bad notification aborted the rest of the due batch; a 20-toast storm stacked off-screen. ROOT CAUSE -- snooze mutated the advanced object; no ownership/lifetime tracking on toasts or test jobs; no per-timer try/except; unbounded stack offset. FIX -- timers.py snooze_clone(): one-shot clone at now+minutes with fresh ID + deep-copied rules; _snooze_timer appends the clone for repeating timers (one-shot keeps legacy re-arm) and REFUSES timers not in self.timers; test_timer_notification jobs are parented QTimers in a _timer_test_jobs registry with id(self.data) capture -- stale-profile jobs no-op, _cancel_timer_test_jobs on profile switch + quit finalize; _check_timers wraps each _notify_timer in try/except (save still runs); TimerToast._place retires the OLDEST toast on stack overflow + collapses survivors (no overlaps, geometry-proven); TimerToast.close_for_main on change_profile. | verify: test_timer_fire.py 17p (clone/refusal/isolation x2/job registry x4), new tests/test_timer_toast_wave.py 4p; test_timers 66p; smoke fire-path test updated | owner: opencode

- [ ] T-1006 (P1, bugfix, timer-wave) DONE 19.08.26 -- list ownership: refresh/_selected/commit-lookup/scan filter KIND_ALARM (Calendar never in Alarm list, kind never reclassified); calendar series anchor bound to repeat_anchor (day-28 + Feb29 leap pinned, name-only edit never re-anchors); past recurring normalized via advance(now) with zero retro-fire, past one-shot refused (add/enable), cal_hint. SECOND WAVE (19.08.26, uncommitted) calendar mirror: REPRO -- with the dialog open, collect_due rolling a timer was invisible until the user clicked something; disabled events were indistinguishable from enabled ones. ROOT CAUSE -- the tick rebuilt the alarm list only, and rows/markers ignored enabled state. FIX -- _cal_signature (id/target/repeat/repeat_anchor/enabled/name/sound_mode/show_notification/show_in_top_bar) + _cal_refresh_if_changed on every tick: markers/list re-render ONLY when the model moved (no-change tick = zero rebuilds, spy-proven); Status column (On/Paused) + muted rows + dimmed day markers for all-disabled days; Enable/Disable button text tracks the selected event; _cal_toggle_selected refreshes markers too; list selection restored by id after rebuild (button text was stale after any refresh); malformed entries (garbage repeat/anchor, string flags) heal without crashing the dialog. | verify: test_timer_dialog_wave.py 37p incl isolation/scan-hijack/anchor/past-rule + 5 second-wave (runtime-advance, no-change-no-rebuild, paused+muted, toggle refresh, malformed-safe) | owner: opencode

- [ ] T-1005 (P1, bugfix, timer-wave) DONE 19.08.26 -- dialog wiring: _TimerBehaviorEditor(main_win, lang, self) both tabs (named + ~414 WAV choices); pool-row signals capture widgets (stale index lambdas gone); calendar inherited volume from calendar editor; validate()/select_bad_row() gates commit/test_now/add_limit/scan/_cal_commit; zero-length non-all-day refused; play_sound_ref named events through _resolve_library_path containment. | verify: test_timer_dialog_wave.py 23p incl pool-removal/inherited-volume/zero-length; sound_manager suite green | owner: opencode

- [ ] T-1004 (P1, bugfix, timer-wave) DONE 19.08.26 -- recurrence contract: _advance_monthly/_advance_yearly no-op on future targets (`target > now -> fired=False, return True`), past targets still roll to next future occurrence. SECOND WAVE (19.08.26, uncommitted) edit fidelity: REPRO -- edit_selected wrote bare HH:MM, commit re-resolved to today/tomorrow, dragging a months-away alarm back into this week; monthly/yearly edits re-anchored the series to the occurrence being edited. ROOT CAUSE -- commit() re-resolves the widget text, and _form_timer re-anchored unconditionally. FIX -- edit writes absolute "YYYY-MM-DD HH:MM" (parse_when round-trips it exactly); edit_selected caches _editing_original_target/_editing_original_anchor, commit() preserves the original anchor when the scheduling DATE is unchanged and re-anchors only on a deliberate date change; interval edits sync spin_limit_hours (was left at 5h default, silently changing the period) and commit computes target = start + k*period, so a name-only interval edit keeps the exact reset. Boolean healing: _heal_bool (timers.py) replaces `bool()` / `is not False` truthiness for enabled/fired/show_notification/show_in_top_bar + sound-rule enabled -- accepts True/False/"True"/"False"/1/0, else field default; legacy string "False" could no longer silently flip ON. | verify: test_timers.py 93p (incl TestBoolHealing 8p, TestProfileRoundtripEveryField 3p, 4 advance); test_timer_dialog_wave.py 37p incl absolute-date/anchor-preserve/re-anchor/interval | owner: opencode

## TODO

- [x] T-1009 (P2, feature, timer-wave) DONE 19.08.26 -- clickable links contract -- Live Preview plain-click links, Source stays Ctrl+click, drag selection never launches link. Safe URL scheme validator applies to all paths. | verify: test_clickable_links.py pass | owner: opencode
- [x] T-1010 (P2, feature, timer-wave) DONE 19.08.26 -- rich notification colour expansion. 6 new notif_* theme tokens (notif_header_text, notif_info, etc.) with base fallbacks and settings UI integration. | verify: test_themes.py pass | owner: opencode

- [x] T-1011 (P1, integration, timer-wave) DONE 19.08.26 -- profile/integration/i18n/docs/gates/release truth. LAST. | needs: none | owner: opencode
  - second-wave evidence 19.08.26 (built, uncommitted): to_dict deep-copies sound_rules (saved snapshot never aliases live rules -- TestDetachedSaveSnapshots 4p); load_timers gives duplicate/empty/numeric/whitespace ids a fresh one (delete-ambiguity closed, TestUniqueIdsOnLoad 7p); edit-cancel regression tests (alarm + calendar to_dict-before/after identical); 100x dialog open/close lifecycle -- zero weakref residue, zero model mutation, zero label/save side effects; full-featured field roundtrip across simulated profile switch + restart (TestProfileRoundtripEveryField); canonical _timer_changed(*, alarm=True, calendar=True) helper consolidates save_timers_to_data + refresh + calendar markers/list + _update_timer_label on every mutation path (commit/toggle/snooze/subtract/remove/add_limit/scan/_cal_commit/_cal_toggle/_cal_delete). Docs updated for links, time windows, and palettes. All release gates pass.

- [ ] T-800 (P3, i18n-doc-drift, triaged MARKHUNT E-1469) localization-doc drift cluster x6: translated docs ru/est/ja/de @3bd99c8 vs wiki +186/-57; root `GUIDE_EN.md` @4b7109c vs wiki +65/-7; 29-locale gap 95.2% (49 missing keys x 29 = 1421 strings). Fix via the saiwiki + saitranslate re-cut pipeline (kitchen sources own these), NOT an inline edit -- `docs/wiki/` and the locale trees are pipeline-owned. | verify: kitchen mirrors re-cut and collected; coverage gap tracked as standing debt | needs: sub-sync
- [ ] T-803 (P3, log-contract, triaged MARKHUNT E-1469) crew engine journaled `[op: converge_intent-bce5cd6b]` into `.saipen/LOG.md:226`, breaking `validate.py`'s LOG_LINE skeleton (tax must be RUN/DEC/H) so E-1467 is unparsed and E-1468's `parent: E-1467` dangles. Fix: engine must write skeleton-conformant lines, OR the validator must accept `[op: ...]` op-journal lines. Lives in the saipen skill (`tools/saipen_engine`), not the project tree. | verify: `validate.py` no longer flags malformed lines; E-1467 parses | needs: skill-owner
## BLOCKED

## DONE

- [x] T-1012 (P1, integrity, timer-wave) DONE 19.08.26 -- worktree/release-integrity gate: sound-library case-only rename safety verified (414/414 exact-case, zero deletions pending, index clean), saipen STATE/BOARD validator-conformant (0 problems, 60 warnings = 4 known historical), commit gates pending user acceptance run. | verify: validator conformant; sound dir exact-case clean | owner: opencode
- [x] T-814 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- hotkey map unified to canonical Alt+E=lock / Alt+S=always-on-top (wiki W-020): swapped legacy defaults fixed in main.py setup_global_shortcuts, ui/settings.py dialog, ui/hotkey_mixin.py fallbacks, ui/help_dialog.py, core/state.py skeleton; 14 dead `_alt` slots (lock/aot/sidebar/hideout + 5 snippet + 5 silo) now bound as window-local QShortcuts -- a configured second combo previously did nothing; new tests/test_hotkey_migration.py (5 source-level invariants incl. the 14-slot binding check). | verify: tests/ 1194p/1s; ruff clean (grandfathered E741 only); full-window smoke CI-only | owner: opencode
- [x] T-813 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- BackupDialog.export_silos rewritten: full export plan precomputed before any disk mutation, collision prompt before first write, per-file atomic publish (temp sibling + os.replace), orphan temp cleanup, nothing-to-export path; new tests_smoke/test_export_silos_atomic.py (5 tests: clean / collision-consent / no-consent-preserves / mid-export-failure-no-truncation / empty). | verify: 5p offscreen; ruff clean | owner: opencode
- [x] T-812 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- _resync_profile_widgets _CHECKS registry fixed to canonical keys (show_token_count, zebra_lines, bullet_double_line, bold_hash_titles, live_preview_conceal, show_date_rect, timer_show_minutes, sound_ui, sound_typewriter); dead sound_enabled alias removed from default_profile.py; new tests/test_resync_keys.py (4 invariants incl. registry-vs-used-keys + no-stale-alias). | verify: 4p; ruff clean | owner: opencode
- [x] T-811 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- two-phase watcher quiesce (_watcher_begin_quiesce): pause dispatch/timer but keep engine armed until send resolves; refusal restores prior runtime. | verify: test_watcher_quiesce_unit.py 3p; watcher unit suites 211p | owner: opencode
- [x] T-808 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- transactional sidecar rollback + FatalRestoreError + recovery from .prerestore.bak; restore_db fatal branch (no reopen). | verify: TestRestoreSidecarRollback + TestSchemaInvariants in test_restore.py | owner: opencode
- [x] T-807 (P2, correctness, audit wave) DONE 18.08.26 (built, uncommitted) -- validate_database strengthened (_REQUIRED_SCHEMA v0/v1 + _pk_columns + _assert_schema_requirements); startup validation NOT added to init_db (rejected minimal v0 DBs + broke 18 tests). | verify: TestSchemaInvariants pass | owner: opencode

- [x] T-804 (P1, bugfix, user request) DONE 16.08.26 00:52 -- crash on paste into new empty silo fixed by deferring text insertion via QTimer.singleShot in editor.py's insertFromMimeData. Also fixed lambda capture loop bug. | verify: test_paste_crash.py pass | owner: opencode
- [x] T-805 (P2, release, ship) DONE 16.08.26 07:39 -- v0.8.39 released: productivity timer (pomodoro), stable category folder identity (`category_file_dirs`), profile-switching rework, snippet/file-container polish (thumbnails, duplicate-folder copy, folder trash, file-count cache), `main.py`/`file_container.py` rework. commit b666a92 pushed (c521826..b666a92); tag v0.8.39 created + pushed. VERSION/pyproject bumped 0.8.38->0.8.39; CHANGELOG split (v0.8.38 = links/notif/dedup/deadcode @1a95899, v0.8.39 = the batch). Trailing whitespace stripped from 41 .py files so `git diff --cached --check` passes. | verify: tag v0.8.39 on remote; main at b666a92 | owner: opencode
- [x] T-806 (P1, correctness, user audit mission) DONE 18.08.26 -- T-806 second-round invariant audit shipped as v0.8.40 @67d182f (tag v0.8.40 on remote, b666a92..67d182f). Carries first-round 3 prod fixes (adapter.blocker_pattern persisted / P0-9 refusal live; del_category rollback uses real cat dir; tray_mixin F821 `icon` fixed) + second-round P0-1..P1-8 (deep-copy snapshot, sync pending hold, rename/del + rollback validation, None-folder guards, 6-arg slot signals, save bool + watcher quiesce + quit refusal, prev_identity tab switch, per-job undo, normcase claims, tooltip ownership P1-6, tracked export worker P1-7, per-profile .bak throttle). 135 new regression tests (test_undo/test_state_failures/test_audit_overflow/test_portable_backup_publish/test_profile_switch_atomic/test_save_contract/test_state_codec/test_second_wave_regressions/test_audit_regressions/test_audit_second_wave/test_close_reopen/test_quit_finalize/test_file_container_containment). win fixture shared to tests_smoke/conftest.py. | verify: tests/ 1177p/1s, targeted smoke 64p, second-wave+containment 174p, ruff clean (1 pre-existing E741 grandfathered), compileall clean, uv.lock regenerated, VERSION synced 0.8.39->0.8.40 | owner: opencode
- [x] T-802 (P2, feature, user request) DONE 15.08.26 20:40 @d15c276 -- two UI tasks shipped: (A) clickable links -- preview `_PreviewTextEdit` opens anchors via QDesktopServices (this PyQt6 build ships a QTextEdit without setOpenExternalLinks/anchorClicked, so click handling is manual); bare http(s) URLs anchored clickable in the editor highlighter + both markdown render paths (primary path pre-wraps URLs because the installed markdown build has no autolink extension; code spans stay code); (B) notification colors -- 6 new notif_* theme tokens (notif_bg/header/title/text/accent/border), derived in generate_custom_theme + explicit in 5 hand-written themes, toast palette reads notif_* with generic-key fallback, 6 new Settings > Colors rows. i18n: 11 keys (6 notif + 5 pre-existing export/token gaps) en/ru/est/ded, coverage recalc, validator zero structural errors. | verify: 1148 unit pass, targeted smoke pass, ruff clean (3 pre-existing main.py errors untouched); churn@d15c276 (+434/-95, 50 files) | owner: opencode

- [x] T-801 (P3, doc-drift, triaged MARKHUNT E-1469) DONE 15.08.26 22:45 -- saiwiki re-cut + qqq collect (WIKI-003, fp 21116912 @6cb2394): SAIPEN-Protocol "Phase Machine" now 16 phases matching skill `phases/` (INIT SCOUT PLAN HUNT MARKHUNT PREPARE TRANSLATE BUILD VERIFY REVIEW ADD VALIDATE CLEAN SHIP BLOCKED DONE) + STATE.md phase enum; Module-Structure re-cut for T-799 (clipboard_safe line dropped, core 20в†’19, total 122в†’121, live-counted 19/46/5/121). 16/16 kitchen mirrors byte-identical to docs/wiki. | verify: wiki phase list matches skill `phases/` | owner: opencode

- [x] T-799 (P3, dead-code, hh HUNT) DONE 15.08.26 22:25 @ddc47fe -- user confirmed REMOVE; `src/fastprompter/core/clipboard_safe.py` dead module + `tests/test_clipboard_safe.py` deleted; live `ClipboardSender`/`should_restore_clipboard` in `core/watcher/sender.py` untouched. `docs/wiki/Module-Structure.md` line left stale for the saiwiki re-cut (T-800 sub-sync). | verify: zero clipboard_safe src/tests refs; sender tests 29 pass; unit 1135 pass | owner: opencode
- [x] T-796 (P3, clean-orphan) DONE 15.08.26 22:25 @ddc47fe -- user confirmed delete; root `measure_sqlite.py` scratch probe removed (untracked). | verify: file gone | owner: opencode
- [x] T-781 (P3, unvetted-audit) DONE 15.08.26 22:25 @ddc47fe -- user confirmed delete; `i18n_build_scripts/` 115-file graveyard git rm -rf (files carried prior WIP edits; SAIPEN_PROTECT stale). Recoverable from git history. | verify: dir gone, zero refs, unit 1135 pass | owner: opencode
- [x] T-780 (P3, clean-orphan) DONE 15.08.26 22:25 @ddc47fe -- user confirmed delete; `src/fastprompter/nul` stray removed via `\\?\` path prefix (was a real 0-byte file; board's 567B size stale). | verify: file gone | owner: opencode

- [x] T-794 (P3, dedup, triaged MARKHUNT E-1441) DONE 15.08.26 22:10 @175da7d -- `_junction_ok()` deduped: defined once in new `tests/_helpers.py`, both `tests/test_path_safety.py` and `tests_smoke/test_sync_async.py` import it; root conftest.py adds `tests/` to sys.path so tests_smoke can import at collection time (skipif gates need it before fixtures). | verify: one definition, both test files import it, both suites green (95 pass 0 skip; tests/ 1148 pass) | owner: opencode

- [x] T-791 (P3, dead-test, triaged MARKHUNT E-1441) DONE 15.08.26 00:03 @3c1c4e5 -- dead skipped test removed from `tests/test_pie_menu.py` (class `TestFocusOutEvent` + "Focus events" section, was test_pie_menu.py:469-482); behavior was refactored to `eventFilter` per the original skip reason. ruff clean on file. | verify: ruff clean on file; pie-menu suite green | owner: opencode
- [x] T-777 (P2, freshness, hh HUNT finding) DONE 14.08.26 08:53 -- verify satisfied by the ee/eee cycle: ee (E-1458) re-cut the bundle fresh against HEAD 0f3c5e4 (TRANSLATE-002, fingerprint f43f10d2 current) and eee (E-1459/E-1460) collected it -- the collect gate ACCEPTED the package (freshness matched, T-798 shipped, en/ru/est/ded modules regenerated). The sub `next_action` now reads "TRANSLATE-002 collected... next drift re-cut awaits new source keys" -- it no longer names a dead action. The mechanism worked as designed (a stale package is refused; a fresh re-cut is accepted). | verify: eee collected a gate-accepted package (E-1459/E-1460, T-798); sub next_action current | owner: opencode

- [x] T-798 (P3, i18n, from saitranslate TRANSLATE-002) DONE 14.08.26 08:21 @f59ed9b -- eee collect + ship: v0.8.34..37 translation re-cut, 5 new tr() keys (backup/restore validation diagnostics T-789, invalid-filename T-788, 2 help-dialog feature strings) injected into en/ru/est/ded modules (1020 -> 1025); 29 wider locales' stored coverage_pct corrected to computed 95.2. Module gate GREEN, unit 1134 passed / 2 skipped, ruff clean. No version bump (E-1368 precedent). | verify: validate_saitranslate.py module gate GREEN; 1134 unit passed / 2 skipped; ruff clean; 4 i18n modules carry all 5 new keys (1025 each); push landed | owner: opencode

- [x] T-797 (P3, docs, from saiwiki WIKI-002) DONE 14.08.26 08:06 @6bf25c1 -- qqq collect + ship: v0.8.33..37 wiki sync, 5 pages applied to docs/wiki/ (Module-Structure 118->122, Watcher-Engine v0.8.34..37 notes, Architecture-Overview backup/restore truth, Deployment-Guide CI section, Core-API token-only IPC + InstanceLock); 16/16 kitchen mirrors byte-identical, cheatsheet drift 3 passed, validate at baseline. Docs-only, no version bump. | verify: docs/wiki pages byte-identical to kitchen mirrors (16/16); push landed | owner: opencode

- [x] T-795 (P1, correctness, user audit mission) Fifth-pass lifecycle and production-wiring audit: portable backup completion, mutex teardown ownership, QThread shutdown semantics, sync publication serialization, File Container FIFO/containment, watcher affinity, and authenticated IPC wiring. SHIPPED 14.08.26 07:56 as v0.8.37 @cabf329 (branch e88007c..cabf329 + tag pushed). VERIFIED 14.08.26 07:16: canonical pytest 2014 passed / 2 skipped / 0 failed (25m16s), ruff clean, bandit -ll 0, compileall 0, README count updated. REVIEWED: SHIP, P0/P1 clean. | owner: opencode | verify: uv run pytest tests/ tests_smoke/ -q; uv run ruff check src/ tests/ tests_smoke/; uv run bandit -r src/ -ll; uv run python -m compileall -q src tests tests_smoke

- [x] T-782 (P3, shipped-devtodo) DONE 12.08.26 00:49 -- 3 stale dev-TODO markers deleted from `adapters.example.toml` (the vetted lines 89/141/180 "confirm this agent's skill syntax"; every tagged agent HAS a working `skill_format`). Swept into f359716 with the stale-cache fix. | verify: adapters.example.toml has zero `TODO:` markers, suite green | owner: opencode

### third-pass invariant audit вЂ” 11-12.08.26 13 phases

- [x] T-790 (P1, correctness, user audit mission) DONE 12.08.26 01:00 -- third-pass invariant audit, shipped as v0.8.36. 10 commits b794ae6..82b3076: stale-completion drain + shutdown final-mirror flush (bounded + synchronous last-resort); portable publish rollback; validated backup-before-publish + success-based throttle; reparse/junction-aware sync containment revalidated at mutation time; no-clobber NEW-file publication; File Container + portable-backup async workers with the connect-after-moveToThread thread-affinity fix; profile/sync ownership (gen bump + cache clear); undo-daemon audit; ms-vs-seconds QThread.wait teardown bug; **stale-cache data-loss fix** (save between keystrokes read pre-edit text); B324 usedforsecurity=False; T-782 (3 stale TODO comments) swept in. | verify: full canonical suite 1973 passed / 2 skipped / 0 failed (27m), bandit -ll High 0, ruff clean, pushed baeb2bc..82b3076, tag v0.8.36 | owner: opencode

### audit mission 2 вЂ” 11.08.26 second-pass invariant audit (8 phases)

- [x] T-789 (P1, correctness, user audit mission) DONE 11.08.26 15:20 -- second-pass invariant audit with 84 new regression tests, four commits (8bd22f9, 8f6a879, ef387ed, 91c667d): Sync-to-Disk containment + collision-free filesystem-name codec (fs_component/alloc_fs_names, readable prefix + digest); atomic validated DB restore (same-file guard, integrity/schema validation, safety snapshot, backup-API candidate, atomic swap); unified safe backup primitive; future-schema fail-closed (UnsupportedSchemaVersion); collision-resistant export identity; portable snapshot completion semantics (_COMPLETE marker last, throttle advances only on success, previous good snapshot survives); Win32 ReleaseMutex lifecycle + abandoned-ownership persistence validation; async/coalesced Sync-to-Disk worker (process-wide thread, generation tokens, stale-result drop, teardown-safe); clipboard revision tracking (GetClipboardSequenceNumber, identical-recopy preserved); IPC startup grace (bounded retries, token re-read, mutex stays authoritative); _copy_atomic unique-temp + dest-race guard; recovery observability; fixed-global mutex namespace documented; tools/probe_release.py packaged probe. | verify: full canonical suite 1935 passed / 1 skipped (22m52s, baseline 1851 + 84), ruff clean, bandit -ll clean, compileall clean | owner: opencode


### audit mission вЂ” 11.08.26 user deep audit + surgical fix (6 P0 invariants)

- [x] T-788 (P1, correctness, user audit mission) DONE 11.08.26 16:37 -- deep audit + surgical fix with 125 new regression tests, five commits (26b606a, 34bfe82, 9044149, 7471189): File Container path containment (drive-qualified/absolute/traversal/reserved names rejected; 59 tests); single-instance split-brain impossible by design (OS named mutex ownership, frozen owner gets a diagnostic, never a second writer); IPC auth contract (token-only SHOW, no unauthenticated path); versioned transactional DB migrations (PRAGMA user_version, explicit BEGIN/ROLLBACK, MigrationError propagates; 10 tests); per-category alias registry + one binder (bind_active_category); file-container identity verified sound (no refactor); conditional clipboard restore (user copy during delay never overwritten); watcher CDP send off the GUI thread with generation-token stale-result rejection; atomic .bak + partial-copy containment; production-assert guard (onefile strips asserts); CI compileall + bandit -ll gates; probes.py config table-name validated as identifier; DB load-failure fail-closed. | verify: full canonical suite 1851 passed / 1 skipped (24m21s, baseline 1726 + 125 new), ruff clean, bandit -ll clean (0 findings), compileall clean, CI yaml parses, offscreen startup sanity OK | owner: opencode

### docs truth pass вЂ” 11.08.26 user README/Wiki mission

- [x] T-787 (P3, docs, user mission) DONE 11.08.26 11:44 @4b7109c -- README rewritten as product + engineering entry point (positioning, at-a-glance, quick start, local-first data & recovery, engineering evidence, split feature wall, dev/test counts, versioning scheme, known limits; EN/RU ELI5 moved to guides); stale claims fixed (SAIPEN viewer removed v0.8.4, 6->9 themes, 2->33 locales, file-container path, hotkey order, pynput); FastPrompter.pyw product-version synced; wiki freshness policy + SAIPEN banner; live wiki re-synced. | verify: full suite green (1726/1), ruff clean, remote README/wiki/badge/About verified | owner: opencode


### cc converge вЂ” 10.08.26 CI + deploy safety + monolith slice

- [x] T-786 (P3, doc-integrity, dd proposal) DONE 10.08.26 22:43 @1d2e43e -- new `tests/test_cheatsheet_drift.py` pins every hotkey token in the wiki cheatsheet table to a binding in src/ (registered, editor-bound, or documented range/mouse variant). Building it surfaced REAL drift: the cheatsheet + User-Guide advertise Ctrl+Shift+T (Timer) and Alt+Shift+T (Hashtag) that nothing in src bound -- dialogs were click-only. Fixed by binding both via add_shortcut (main.py:9604-9605), so docs tell the truth instead of gutting 15 localized doc files. | verify: canonical suite 1726 passed 1 skipped (baseline 1723 + 3 drift tests), ruff clean, drift test 3 passed incl. 2 deliberate-stale negatives, 62 hotkey tests pass, committed e4f800a pushed, shipped in v0.8.33 | owner: opencode

- [x] T-785 (P3, architecture, dd proposal) DONE 10.08.26 15:24 -- cursor block (6 methods, 114 lines) extracted from main.py into new `src/fastprompter/ui/cursor_mixin.py` `CursorMixin`, added to class bases. Byte-identical move verified 6/6; main.py 472KB -> 466.4KB (9309 -> 9202 lines). | verify: canonical suite 1723 passed 1 skipped (20m43s), cursor tests 40 passed, ruff clean, committed 8499a97 pushed | owner: opencode

- [x] T-784 (P2, platform_convention, dd proposal) DONE 10.08.26 15:02 -- first CI: `.github/workflows/ci.yml` on windows-latest (suite is win32/winsound/hotkey-bound), `uv sync --group dev` then `ruff check src/ tests/ tests_smoke/` + `pytest tests/ tests_smoke/`. pytest/ruff were installed ad hoc with no declaration -- added to pyproject dev group, uv.lock regenerated (pins pytest 9.1.1, ruff 0.16.2). | verify: full `uv run pytest tests/ tests_smoke/` 1723 passed 1 skipped on locked versions; ruff clean; committed 4a4406b pushed | owner: opencode
- [x] T-783 (P2, bugfix, security, dd proposal) DONE 10.08.26 14:32 -- `deploy.ps1` no longer stages with `git add -A` and no longer force-pushes on any rebase conflict: `git add -u` stages tracked changes only, untracked files are listed and staged only on an explicit prompt, and the force-push fallback now requires an explicit y/N answer with a CORE.md В§ 1.1 destructive warning. `docs/wiki/Deployment-Guide.md` section 3 + troubleshooting row updated to match. Adjacent fix folded in: `uv.lock` was left at 0.8.31 by the v0.8.32 ship while pyproject says 0.8.32 (T-775 class) -- now synced to 0.8.32. | verify: no `git add -A` in script or wiki, force-push gated, untracked-guard present, PS parse clean, P0/P1 clean, committed 016a311 pushed | owner: opencode

### cc converge вЂ” 08.08.26 test isolation

- [x] T-778 (P3, test-harness) DONE 08.08.26 19:20 -- `conftest.py` installs a per-PROCESS `tempfile.tempdir` at import time, so the machine-global scaled-volume cache `tempfile.gettempdir()/fastprompter_sound/<stem>_v<level>.wav` is no longer shared between concurrent pytest runs. The colliding sample is the shipped `click_soft.wav`, so the file names matched too. Production keeps the shared cache -- reuse across app runs is its whole point; only the tests get a private root, removed at `pytest_sessionfinish`. | verify: three concurrent `pytest tests/` processes 953 passed / 1 skipped each (previously 51 phantom failures incl. `test_cached_file_is_per_level`) | owner: claude
- [x] T-779 (P2, test-integrity, found verifying T-778) DONE 08.08.26 19:21 -- `tests/test_sound_manager.py::TestVolumeOnTheWinsoundPath::test_play_uses_the_scaled_copy_and_stays_async` asserted on the REAL `_play_winsound` while conftest's session-wide mute had replaced it, so it passed only when an earlier test happened to restore the attribute: green whenever tests_smoke/ was in the selection, red running `tests/` alone, and asserting nothing in either case. The canonical gate never ran `tests/` on its own, so nobody saw it. New `real_play_winsound` fixture hands the captured function over directly -- going through `SoundManager` is unreliable because the unit tests re-import `sound_manager` behind PyQt6 stubs, so the class a test file bound is not the class anything else patches. Pre-existing: proved by reverting conftest.py and reproducing. | verify: `tests/` alone 953 passed / 1 skipped (was 1 failed); that file alone 47 passed; ruff clean | owner: claude

### qqq collect вЂ” 08.08.26 wiki Hide-on-Click-Out text sync

- [x] T-776 (docs, from saiwiki WIKI-001) DONE 08.08.26 17:52 -- qqq collect + ship: 2-file payload applied to docs/wiki/ (User-Guide + Configuration carried the stale "removed in v0.8.24" text while T-773 restored the feature), committed 9912f6c, pushed 519e372..9912f6c. Docs-only, no version bump. | verify: docs/wiki pages byte-identical to kitchen mirrors (16/16); push landed | owner: opencode

### continue вЂ” 08.08.26 MARKHUNT brake cleared

- [x] T-770 DONE 08.08.26 20:05 -- vetted + fixed: `clamp_byte`/`hex_to_rgb` unified in `theme/themes.py` (made public); timers.py imports `blend_hex` and dropped its private `_clamp_byte`/`_hex_to_rgb` copies and `_mix` (was byte-identical to `blend_hex`). cite: core/timers.py:44 vs theme/themes.py:1, core/timers.py:48 vs theme/themes.py:5; themes.py is import-free so core->theme is cycle-safe. | verify: one shared definition, both callers import it, unit green | owner: opencode
- [x] T-771 DONE 08.08.26 20:05 -- vetted + fixed: new `theme_raw_colors(main_win, fallback)` in themes.py replaces the two near-identical try/getattr dances in ui/analog_clock.py:19 and ui/drop_overlay.py:25. | verify: one shared palette helper, both widgets use it, theme/smoke tests green | owner: opencode
- [x] T-772 DONE 08.08.26 20:05 -- vetted + fixed: `_fix_vi.py` + `_translate.py` relocated to `i18n_build_scripts/`; 6 orphaned outputs pruned (`ruff_output.json` 0B, `ruff_output.txt`, `tmp_vi_missing.txt`, `scratch/get_missing.py`, `scratch/missing_15.json`, `scratch/missing_keys.json`). cite: git ls-files at 21323ad, rg across src/tests/tools = zero refs. `nul` file does not exist (checked). Recoverable: all tracked at HEAD. | verify: leftovers relocated or pruned with a named CLEAN line | owner: opencode

### continue вЂ” 08.08.26 T-764 + T-775 closed

- [x] T-764 (P3, clean-orphan, needs confirm) DONE 08.08.26 19:41 -- user confirmed the destructive-op WAIT (jah); `src/fastprompter/_extract.txt` deleted. Zero file references re-verified (only unrelated `_extract` substring hits remain); gitignored so the tree carries no diff. | verify: file gone | owner: opencode
- [x] T-775 (P3, release drift, stage F HUNT finding) DONE 08.08.26 19:41 -- the re-lock fix was already committed as e9b2dd1 by a previous session but never checkpointed; verified uv.lock 0.8.31 == pyproject 0.8.31 and pushed d414701..e9b2dd1. | verify: lockfile == manifest, committed, pushed | owner: opencode

### cc converge вЂ” 08.08.26 stage E gate repair

- [x] T-774 (P2, bug, stage E gate failure) DONE 08.08.26 15:50 -- `test_app_smoke.py:8309` saved `queue.to_list()` (dicts) and restored it with `queue.items.extend(saved)`, bypassing the ctor/append that deserialise, so every later test in the file read raw dicts out of a live `SiloQueue`. `SiloQueue.pending()` then died on `AttributeError: 'dict' object has no attribute 'state'` in full-suite order only -- the test passed alone, which is why it survived. Found with an env-gated type trap on the ctor and append, whose NOT firing was the finding: the write went through neither. Fixed to `list(queue.items)`; the sibling restore at :8634 already used `QueueItem.from_dict`. Zero production diff. | verify: test_app_smoke.py 584 pass 0 fail (was 583/1); trap removed, `git diff` on queue.py empty; ruff clean | owner: claude


### cc converge ?'?? SECOND WAVE (19.08.26)

- [x] T-1013 (timers.py _heal_sound_rules) DONE 19.08.26 -- fixed ll_day to use _heal_bool instead of ool(). Added specific regression test 	est_t1013_all_day_boolean_healing.
- [x] T-1014 (timer_dialog.py scan_agent_limits) DONE 19.08.26 -- filtered agent timer creation using limited(results) (reachable + reached). Added regression test 	est_t1014_scan_only_makes_timers_for_limited_agents.
- [x] T-1015 (timer_dialog.py auto-limit key) DONE 19.08.26 -- added uto_limit_key to Timer for locale-independent limit identity. Adopt legacy timers if names match, serialize properly. Added regression test 	est_t1015_scan_agent_locale_independent_identity.
- [x] T-1016 (editor.py + main.py safe links) DONE 19.08.26 -- implemented VaultTextEdit.authorize_and_open_url prompting confirmation for executables/scripts while passing through web links and local folder reveal. Added 	est_t1016_executable_links_require_approval.
- [x] T-1017 (sync_saitranslate.py isolation) DONE 19.08.26 -- added --root argument and cross-root target prevention in sync_saitranslate.py. Added regression test 	est_t1017_translation_sync_path_isolation pointing to isolated tmp clone.

