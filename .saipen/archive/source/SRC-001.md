# FastPrompter — Audit Handoff

RUN_ID: acb-mtctw8at-f916ba5c89054686891b
GENERATED_AT: 2026-08-28T15:15:55.871600
PROJECT_NAME: FastPrompter
TARGET: FastPrompter_28.08.26-T07-33-57(2).zip, fully unpacked and audited at the archive project root; audit-command contract: :contentReference[oaicite:0]{index=0}
BASELINE: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive worktree is dirty and contains substantive post-HEAD state.py/main.py/test changes, including backup-coordinator fixes; current files were audited, not the clean HEAD snapshot
SAIPEN_CONTEXT: STALE - .saipen/STATE.md says DONE/board empty while BOARD.md still contains T-1121+ TODO entries and LOG.md records only a partially completed post-v0.8.60 audit rerun; used as orientation only
TOTAL_TICKETS: 21
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 8 tickets (baseline: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD)
- Wave 2 (Second Wave): 7 tickets (baseline: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD)
- Wave 3 (Performance): 6 tickets (baseline: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD)

---
## 01 — AUDIT CORE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-28T13:47+03:00
WAVE: AUDIT CORE
TARGET: FastPrompter_28.08.26-T07-33-57(2).zip, fully unpacked and audited at the archive project root; audit-command contract: :contentReference[oaicite:0]{index=0}
BASELINE: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive worktree is dirty and contains substantive post-HEAD state.py/main.py/test changes, including backup-coordinator fixes; current files were audited, not the clean HEAD snapshot
SAIPEN_CONTEXT: STALE - .saipen/STATE.md says DONE/board empty while BOARD.md still contains T-1121+ TODO entries and LOG.md records only a partially completed post-v0.8.60 audit rerun; used as orientation only
AUDIT_SCOPE: entrypoint/manifest; FastPrompterState schema migration, structured codecs, save/backup/recovery/profile transitions; backup publication/drain coordinator; restore/shutdown; nested-silo filesystem merge + undo journal; Sync-Project persisted mapping/allocation; interval defaults/presets; relevant tests, Git and SAIPEN context
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: PyQt6 is absent in this sandbox although correctly declared in pyproject.toml; `python -m pytest --collect-only -q` exits 4 while importing tests_smoke/test_app_smoke.py:20 with `ModuleNotFoundError: No module named 'PyQt6'`. markdown and pynput are also declared but absent.
VERIFIED_INSTEAD: `python -m compileall -q src FastPrompter.pyw tools tests tests_smoke` passed; focused non-GUI/state/project-sync/timer suite ran 263 passed, 1 skipped, 3 PyQt6-only import failures; deterministic local reproductions proved the backup revoke/swap race, overlapping-worker drain false-success, malformed migration commit, missed-ID codec loss and malformed Sync-Project key crash
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 8
HANDOFF: IMPLEMENTATION_AGENT

[P1] [CORE-001] src/fastprompter/core/state.py::_backup_atomically / _backup_publication_authorized / _schedule_periodic_backup
EVIDENCE: `_backup_atomically` lines 830-833 calls `authorize()`, then performs `os.replace(tmp, dest_path)` only after the authorization helper has released `_BACKUP_LOCK`; therefore authorization and publication are not atomic. A deterministic reproduction blocked the final `os.replace`, revoked the captured generation strictly after authorization returned True, then released the replace: `_backup_publication_authorized(...)` was False after revocation yet `destination_overwritten_after_revoke=True`. There is a second symptom of the same publication API defect: an authorization refusal makes `_backup_atomically` return normally with no explicit result, while `_schedule_periodic_backup` lines 1025-1027 unconditionally sets `published=True` after that return. A second reproduction revoked the generation during the copy and observed `on_published` firing even though the final swap was denied.
DEFECT: backup authorization is a check-then-act race rather than an atomic publication decision, and the helper does not report whether publication actually occurred. A retired/stale generation can therefore overwrite the recovery `.bak` after revocation, while a correctly denied publication can conversely be reported as success and advance the backup throttle despite publishing nothing.
REPAIR: keep copy/validation outside the coordinator lock, but make the final authorization + `os.replace` one indivisible coordinator operation under `_BACKUP_LOCK` (for example a dedicated `_publish_backup_if_authorized(tmp, dest, key, gen)`); make `_backup_atomically` return an explicit publication boolean and fire `on_published` / success throttling only when that boolean is True. Preserve the unrestricted/manual-backup path by treating a successful unguarded replace as True.
VERIFY: add a barrier immediately before the final swap; authorize generation N, revoke N while the publisher is blocked, then release it and assert the existing `.bak` remains byte-identical and the temp is retired. Assert an authorized generation does publish. Assert a denied swap returns False and neither invokes `on_published` nor advances `_last_backup_time_by_profile`.

[P1] [CORE-002] src/fastprompter/core/state.py::_BACKUP_INFLIGHT / _schedule_periodic_backup / _start_safety_snapshot_async / _drain_db_backup
EVIDENCE: both periodic and startup workers represent destination ownership with the same boolean `_BACKUP_INFLIGHT[dest_key] = True` (periodic lines 997-1003; startup lines 1747-1750), and each worker independently executes `_BACKUP_INFLIGHT.pop(dest_key, None)` in its `finally` block (lines 1043-1047 and 1773-1776). `switch_profile` only revokes an old profile destination at lines 1672-1678; it does not wait for the old worker, so immediate profile re-entry can start a new startup worker for that same destination while the revoked old periodic worker still exists. A deterministic reproduction created exactly that overlap: both workers were active, only the old worker was released, its `finally` removed the shared destination marker while the new worker remained blocked, and `_drain_db_backup(db, timeout=0.2)` then returned True in 0.0 seconds while the new worker was demonstrably still active.
DEFECT: a boolean destination marker cannot represent more than one worker generation. One worker can erase another worker's ownership record, turning the supposedly hard drain barrier into a false-success result. Restore or shutdown can consequently proceed while a real SQLite backup worker still owns/reads the destination.
REPAIR: separate profile-level coalescing from destination-level liveness. Track destination workers as unique tokens/generations in a set or reference-counted registry; each spawned worker registers its own token before launch and removes only that token in `finally`; `_drain_db_backup` must succeed only when the destination's worker set is empty. Do not let an old generation's cleanup mutate a newer generation's liveness record.
VERIFY: reproduce profile A periodic worker -> switch away/revoke A -> immediate re-entry/start A startup snapshot. Release either worker individually and assert drain remains False while the other token is alive; release both and assert drain becomes True. Repeat with reversed completion order and ensure no worker can remove another worker's registration.

[P1] [CORE-003] src/fastprompter/main.py::FastPrompter.restore_db
EVIDENCE: `restore_db` first closes `self.state.conn` and clears both connection references at lines 13095-13100, then calls `_drain_db_backup(db_path)` at line 13111 without inspecting its documented boolean result, and unconditionally proceeds into `restore_database(path, db_path)` at line 13113. `_drain_db_backup` explicitly returns False on timeout at state.py:935-958 and leaves the destination draining/revoked. Thus a timeout both fails the intended quiescence barrier and is silently treated as permission to restore.
DEFECT: backup quiescence is advisory instead of mandatory, and the live connection is destroyed before quiescence has even been proven. Restore can race an active backup worker. If the restore subsequently fails and the editable runtime is reopened, the timed-out destination can also remain stuck in `_BACKUP_DRAINING`, suppressing future backup scheduling for that session.
REPAIR: after watcher quiescence, require `_drain_db_backup(db_path)` to return True before closing the live SQLite connection or invoking `restore_database`. On False, abort the restore with the original connection/database untouched, resume the watcher, and release the drain gate in a way that keeps the timed-out generation permanently revoked/stale while allowing a later generation to schedule. Close the connection only after the backup barrier actually succeeds.
VERIFY: block a real backup worker beyond the drain timeout and invoke restore. Assert `restore_database` is never called, the live connection remains usable, live DB bytes/state are unchanged, watcher runtime resumes, and a subsequent backup generation can be scheduled. Then release the worker and verify the normal successful restore path still drains, closes and restores.

[P1] [CORE-004] src/fastprompter/main.py::make_silo_child/_write_undo_file and src/fastprompter/ui/snippet_ops_mixin.py::_merge_journal_* / _reconcile_merge_journal
EVIDENCE: `make_silo_child` mutates `silo_children`, physically moves child files via `_merge_child_files`, queues an undo snapshot with `merge_journal_root`, and only calls `mark_dirty()` at line 16812. Normal SQLite autosave runs every 10 seconds (main.py:909-911), while the undo writer is armed with a 1-second debounce (main.py:12405-12411). `_write_undo_file` clears `.merge_journal.json` immediately after the secondary undo JSON `os.replace` succeeds (main.py:12553-12567), regardless of whether the hierarchy change has committed to SQLite. Conversely `_reconcile_merge_journal(root, data)` lines 101-136 never consults `data` at all and assumes every surviving journal proves the logical DB half was uncommitted, reversing all moved files. Therefore crash after undo publication but before SQLite autosave leaves pre-nest DB ownership + already-moved files + no journal; the opposite ordering, SQLite commit followed by crash/journal-clear failure, leaves a surviving journal that startup wrongly reverses despite the durable DB already describing the nested ownership.
DEFECT: transaction authority for a primary filesystem ownership mutation is incorrectly bound to durable undo-history publication instead of durable SQLite hierarchy state. Recovery cannot distinguish committed from uncommitted file moves and can produce DB/filesystem ownership disagreement in either crash ordering.
REPAIR: retain merge-journal records until the corresponding `silo_children`/`silo_children_all` relationship is durably committed to SQLite; store enough journal identity (category, child, parent, expected post-merge ownership plus move pairs) to classify recovery. Do not clear the journal merely because undo JSON published. After a successful authoritative DB save, acknowledge/retire records whose expected post-state is now durable. On startup inspect the loaded durable hierarchy: pre-merge DB -> reverse moved files; post-merge DB -> keep moved files and retire the journal; ambiguous/unsafe state -> retain the journal rather than guessing.
VERIFY: (1) nest+move, persist undo but prevent SQLite commit, simulate restart -> files return to child and journal retires; (2) commit SQLite, leave journal artificially present, restart -> files stay under parent and journal retires; (3) make undo publication fail -> DB durability alone decides recovery; (4) crash after any prefix of N file moves remains idempotently recoverable.

[P1] [CORE-005] src/fastprompter/core/state.py::_merge_legacy_into_v2 / _migrate_v0_to_v1 / _migrate_schema / _assert_loader_rows
EVIDENCE: `_merge_legacy_into_v2` lines 645-688 inserts a legacy `slot` unchanged whenever it does not collide with an existing `_v2` key; it never establishes that the slot is an integer in 0..99. `_migrate_v0_to_v1` then drops the legacy tables at lines 711-719, records `PRAGMA user_version = 1` at line 733, and checks only schema shape before `_migrate_schema` commits at line 778. Loader-row validation runs only afterwards in `init_db` at line 1828. A real SQLite reproduction with legacy `temp_presets(slot=100)` completed migration, produced `user_version=1`, dropped `temp_presets`, retained `('Code', 100, 'orphan')` in `temp_presets_v2`, and was then rejected by `_assert_loader_rows`. A weak but v0-contract-compatible legacy table containing REAL slot `1.5` was even worse: migration committed it, `_assert_loader_rows` accepted it because its SQL predicate only checks `<0 OR >=100`, and the actual loader later skips it because it is not an `int`.
DEFECT: the migration commits a schema version whose rows do not satisfy the loader's own data invariant. Invalid legacy rows can turn a loadable v0 database into an unusable v1 database after the supposedly transactional migration, or become silently invisible when a non-integer numeric slot evades the SQL range check.
REPAIR: make row loadability part of the v0->v1 transaction postcondition before any legacy-table drop/version commit becomes durable. For legacy temp/archive rows, reject any non-integer or out-of-0..99 slot with `MigrationError` unless a complete, reference-safe remap is explicitly performed; failing closed is the smaller safe repair because silo slot identities have cross-references. Strengthen `_assert_loader_rows` to validate Python-level type as well as numeric range rather than relying only on the SQLite `<0 OR >=100` predicate.
VERIFY: migrate v0 fixtures carrying slots -1, 100, REAL 1.5 and a weak-schema textual/non-integer value. Each must either be transformed through an explicitly reference-safe legal mapping or raise `MigrationError`; on failure assert `user_version` remains 0, legacy tables/data remain intact and no v1 partial state commits. Assert no row accepted by the migration can later be skipped/rejected by `init_db`.

[P2] [CORE-006] src/fastprompter/core/state.py::_JSON_SETTINGS/_STRUCTURED_CODECS and src/fastprompter/main.py::_persist_missed_ids/_load_missed_ids
EVIDENCE: `_persist_missed_ids` lines 1355-1361 stores a Python list in `self.data["missed_timer_ids"]` and marks settings dirty, but `missed_timer_ids` is absent from both `_JSON_SETTINGS` and `_STRUCTURED_CODECS`. `_encode_setting_value` therefore falls through to `str(value)` at state.py:519-523; a reproduction encoded `['timer-1']` as the literal string `"['timer-1']"`. On reload the generic settings branch leaves that raw value as a `str`, while `_load_missed_ids` lines 1337-1353 accepts only list/tuple/set and therefore restores an empty missed-ID set. Existing `tests/test_w2_003_missed_ids.py` exercises in-memory list persistence but does not perform a real SQLite save/reopen roundtrip.
DEFECT: acknowledged/unacknowledged one-shot timer attention state is not serialized under the project's structured-settings contract, so a missed timer that should remain visibly outstanding disappears after restart/profile reload.
REPAIR: add `missed_timer_ids` to the canonical structured persistence contract as a list with default `[]`, enable legacy `ast.literal_eval` recovery for already-written `"['id']"` rows, and normalize members to non-empty strings. Keep `_JSON_SETTINGS` and `_STRUCTURED_CODECS` parity intact, preferably by reducing the duplicated registry if that can be done without widening scope.
VERIFY: use a real `FastPrompterState` SQLite roundtrip: persist one missed ID, commit, reopen, and assert the raw row is canonical JSON and `_load_missed_ids` receives the same ID. Verify legacy Python-list strings recover, wrong-top-level JSON falls back to `[]`, and non-string/empty members are discarded.

[P2] [CORE-007] src/fastprompter/core/state.py::_normalize_per_category_store and src/fastprompter/core/project_sync.py::free_slots
EVIDENCE: decode normalization for `project_sync_map_all` validates mapping VALUES as safe relative paths at state.py:381-393 but accepts slot KEYS unchanged. A reproduction decoded `{"Text":{"not-a-slot":"a.txt","100":"b.txt","01":"c.txt","1":"d.txt"}}` unchanged. `project_sync.free_slots` line 341 then executes `{int(k) for k in mapping}` with no validation and raised `ValueError: invalid literal for int() with base 10: 'not-a-slot'`. `_rescan_project_sync` retains an existing binding whose mapped file still exists before calling `free_slots`, so such a persisted key survives long enough to crash a real rescan. Numeric but invalid keys also distort `len(mapping)`/free-slot demand, while aliases such as `"01"` and `"1"` represent competing persisted identities for one logical slot.
DEFECT: persisted Sync-Project mappings are treated as untrusted for paths but trusted for slot identity. Malformed state can crash re-scan, consume nonexistent slot capacity, or create duplicate logical ownership.
REPAIR: canonicalize mapping keys during decode/bind: accept only integer slot identities 0..99, emit canonical `str(int(slot))`, drop invalid/negative/>99 keys with a warning, and handle aliases such as `"01"`/`"1"` deterministically without allowing two values to own the same slot. Make `free_slots` defensive as a second boundary so malformed callers cannot raise.
VERIFY: codec tests for `not-a-slot`, `-1`, `100`, `"01"` + `"1"`, boolean/non-string keys where representable, and valid 0/99. Re-scan with an existing file bound through malformed persisted keys must not raise or reduce legitimate capacity; duplicate aliases must resolve to one deterministic owner.

[P2] [CORE-008] src/fastprompter/ui/timer_dialog.py::DEFAULT_INTERVAL_RULES/_interval_reset_defaults/_interval_show_presets_menu
EVIDENCE: every canonical `DEFAULT_INTERVAL_RULES` entry has `"volume": 1.0` at timer_dialog.py:134-199, matching `core/default_profile.py` lines 607-663. `_interval_reset_defaults` copies those exact rules at lines 2943-2948, and selecting the default preset copies them again at lines 2957-2969. Nevertheless the Defaults tooltip says `Reset to default 24h chime schedule (0.05 volume)` at line 2591 and the preset action says `Default 24h Chime (Genie / NewDay / Owl @ 0.05)` at line 2957; corresponding i18n strings also encode 0.05.
DEFECT: the UI advertises a materially different sound volume from the state it installs. Choosing a preset labelled 0.05 actually installs 1.0, a 20x amplitude-setting discrepancy in the visible contract.
REPAIR: make the displayed default volume derive from the canonical default-rule value rather than embedding `0.05` independently in menu/tooltip translation strings. Preserve the separate Workday/Hourly/Pomodoro/Night presets whose actual configured volume is genuinely 0.05.
VERIFY: assert the volume shown in the Defaults tooltip/default-preset label equals the volume copied into every canonical default rule; exercise both Defaults and the default preset action and confirm 1.0 is displayed/installed. Assert explicit 0.05 presets remain labelled and configured as 0.05.

CORE_DONE_WHEN: all eight root causes are repaired with focused regressions for the exact backup interleavings, restore timeout, both merge crash orderings, migration-invalid rows, real missed-ID SQLite roundtrip, malformed Sync-Project slot keys and interval-label parity; existing non-GUI tests remain green, compileall remains clean, and the complete declared-dependency test/smoke suite passes in a PyQt6-capable environment.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-28T14:42:32+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: NOT_PROVIDED
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: FastPrompter_28.08.26-T07-33-57(2).zip, fully unpacked and audited at /mnt/data/_audit_fastprompter
BASELINE: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive worktree is heavily dirty with substantive post-HEAD implementation/test changes; current live files were audited
SAIPEN_CONTEXT: STALE - .saipen/STATE.md reports DONE/empty board while BOARD/LOG and sub-agent state still contain active/stale work; used only as orientation
AUDIT_SCOPE: startup/shutdown/finalization and worker retirement; File Container async ownership/publication and completion contracts; Sync-Project folder conversion, rescanning, eligibility changes and allocation; SQLite backup/restore temporary-candidate lifecycle; watcher two-phase quiesce contract; relevant tests and persistence/error boundaries
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: PyQt6 is unavailable in the sandbox although correctly declared by the project; `PYTHONPATH=src pytest -q tests/test_w2_003_rescan_binding.py` could not exercise its three GUI-linked tests because importing fastprompter.main raises `ModuleNotFoundError: No module named 'PyQt6'`. Qt signal/interleaving tests therefore require a PyQt6-capable environment.
VERIFIED_INSTEAD: `python -m compileall -q src FastPrompter.pyw tools tests tests_smoke` passed; `PYTHONPATH=src pytest -q tests/test_limit_scan.py tests/test_project_sync.py tests/test_portable_backup.py` = 60 passed; `PYTHONPATH=src pytest -q tests/test_restore.py tests/test_state_failures.py` = 42 passed, 1 project-behavior failure; isolated restore roundtrip test deterministically failed again because leaked SQLite temp sidecars entered the prerestore artifact namespace; pure deterministic reproductions also proved Sync-Project filter-retention and bounded-rescan discovery failures
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: src/fastprompter/main.py::_change_project_sync_folder/_rescan_project_sync/_apply_external_sync/_save_sync_include/_save_sync_exclude/_save_sync_recursive/save_data_to_db/_pre_quit_logical_finalize/_shutdown_application; src/fastprompter/core/project_sync.py::scan_folder/is_text_file/free_slots/read_text_file; src/fastprompter/core/state.py::_backup_atomically/validate_database/restore_database; src/fastprompter/ui/file_container.py::_ContainerOpWorker/_move_into_container/_copy_atomic/_make_publish_guard/detach_session/_on_container_done; src/fastprompter/ui/watcher_mixin.py::_watcher_begin_quiesce/_watcher_commit_quiesce/_watcher_rollback_quiesce/_watcher_shutdown; relevant restore, project-sync, File Container and watcher regression tests
COVERAGE_DEFERRED: unrelated rendering/layout/i18n breadth, audio/timer behavior, native hotkey/CDP integration and other surfaces outside the second-wave lifecycle/boundary objective; previously established CORE root causes were not re-audited as new tickets
CROSS_WAVE_REFERENCES: CORE-001/CORE-002/CORE-003 cover separate backup publication, overlapping-worker drain and restore-barrier defects; CORE-007 covers malformed persisted Sync-Project slot keys. W2-001..W2-007 below are distinct root causes and do not duplicate those CORE tickets.
RESIDUAL_UNCERTAINTY: PyQt6 absence prevented live Qt signal/interleaving execution, so W2-001/W2-005/W2-007 are established from exact cross-thread/callback contracts and source ordering rather than GUI runtime reproduction; Windows-specific rename timing was not dynamically exercised in this Linux sandbox. No campaign run-id or canonical previous-wave checksum was supplied, so neither was invented.

[P1] [W2-001] src/fastprompter/ui/file_container.py::_move_into_container/_copy_atomic/_make_publish_guard and src/fastprompter/main.py::_copy_folder_into_container
EVIDENCE: `_make_publish_guard` captures mutable panel ownership/session state and returns a plain predicate. `_copy_atomic` checks that predicate at lines 749-752 and only afterwards executes `os.rename(tmp, dest)` at line 753. Same-volume `_move_into_container` checks at lines 643-645 then renames at lines 647-648; its cross-volume path checks at lines 676-679 then publishes at line 680. `detach_session` independently revokes `_session_valid`/`_container_owner_id` without any gate shared with those final rename operations, and files-root reconfiguration invokes that revocation from main.py. The duplicate-silo custom guard at main.py:15625-15660 has the same check-before-publication shape. Existing W2-012 tests cover guard-already-false and guard-true cases only; they do not cover revocation after the check but before rename.
DEFECT: logical lease validation and physical publication are separate operations. An async worker can observe an authorized owner, the GUI thread can then revoke/rebind that owner, and the already-authorized worker can still execute the final rename into retired/old storage. The guard therefore narrows but does not close the ownership TOCTOU it claims to prevent.
REPAIR: introduce one publication/revocation gate or generation-owned lease primitive shared by every final File Container publication and every ownership revocation. Worker publication must acquire the gate, verify the captured immutable lease/generation, and perform the final no-clobber rename before releasing it. `detach_session`, files-root/category/silo retirement and duplicate-silo structural invalidation must revoke/bump that same lease under the same gate. Keep expensive copying outside the gate.
VERIFY: instrument a barrier immediately before final rename for `_copy_atomic` and both `_move_into_container` publication paths. Start with an authorized generation, pause the worker after its preliminary work, revoke/rebind the owner, release the barrier and assert no destination is published and temp/staging recovery is correct. Repeat with an unchanged generation and assert publication succeeds. Include duplicate-silo deletion/reparent and files-root reconfiguration cases.

[P1] [W2-002] src/fastprompter/main.py::_change_project_sync_folder
EVIDENCE: the function mutates `project_sync`, `project_sync_all`, the active mapping, presets, Sync leases and baseline/EOL/BOM caches through lines 6440-6497. It then calls `self.save_data_to_db(force=True)` at line 6498 but ignores the returned boolean, immediately starts the new watcher at line 6499, refreshes the UI and displays “This project is now a Sync-Project ... changes now sync both ways in real time” at lines 6509-6513. `save_data_to_db` explicitly returns the authoritative persistence result at main.py:4872-4911. Other lifecycle boundaries correctly treat False as a refusal, e.g. profile switching aborts at main.py:9386-9400.
DEFECT: the forced persistence operation that should separate preparation from activation is not a gate. On SQLite save failure, the new binding remains active in memory, old leases have already been invalidated, the new watcher is armed and success is reported even though authoritative SQLite still contains the pre-conversion state. Subsequent external/app edits can therefore operate under a binding that disappears or changes semantics after restart.
REPAIR: make the forced save a mandatory commit boundary. Snapshot/stage all affected configuration, mapping, presets and runtime cache/lease state before mutation; if save returns False, restore the previous in-memory ownership/configuration and previous watcher state and report conversion failure. Start the new watcher and show success only after the authoritative save succeeds. Prefer staging irreversible lease/watcher transition until persistence is known-good.
VERIFY: force `save_data_to_db(force=True)` to return False during initial conversion and folder repoint. Assert the new watcher/root is never activated, no success message is produced, prior project_sync config/mapping/preset text remains authoritative, old binding leases remain usable and restart sees the unchanged DB. Then verify the normal True path activates exactly once.

[P1] [W2-003] src/fastprompter/main.py::_rescan_project_sync/_save_sync_include/_save_sync_exclude/_save_sync_recursive/_apply_external_sync
EVIDENCE: project_sync.py defines the canonical contract: every file that passes include/exclude filters becomes a silo. `_save_sync_include`, `_save_sync_exclude` and `_save_sync_recursive` update those rules and immediately call `_rescan_project_sync`. However `_rescan_project_sync` lines 6545-6552 retains every existing mapping whenever its physical path is still a file, regardless of whether that path still passes the new include/exclude/recursive rules. `_apply_external_sync` lines 6285-6315 subsequently iterates retained mappings and calls `read_text_file` directly without rechecking filter eligibility. A pure reproduction created `a.txt`, changed effective exclusion to `*.txt`, and obtained `scan_folder(...) == []` while the rescan retention predicate left `{'0':'a.txt'}` intact and `read_text_file(a.txt)` remained readable.
DEFECT: rescan conflates two fundamentally different states: temporary unsyncability of an otherwise eligible binding (oversize, binary during a partial write, transient read/stat failure) and deliberate configuration ineligibility (excluded extension/path or nested path after recursive is disabled). The former should preserve identity; the latter must stop syncing. As implemented, changing the user's filters or recursive setting does not reliably stop existing mapped files from participating in two-way sync.
REPAIR: separate path eligibility from transient content readability. For every existing mapping, first test stable configuration eligibility using relative-path containment + recursive-depth + include/exclude matching. If configuration-ineligible, remove only the binding, invalidate its write lease/baselines and preserve silo text. If still configuration-eligible but temporarily unreadable/oversize/binary, retain the binding as current W2-003 tests require.
VERIFY: bind `a.txt`, then exclude `*.txt` and assert the mapping is removed while silo text remains. Remove `.txt` from include and assert the same. Bind `sub/a.txt`, disable recursive and assert the nested binding is removed. Conversely retain mappings for eligible files that are temporarily over max_bytes, binary or unreadable. Confirm removed bindings cannot receive later app->file pushes.

[P1] [W2-004] src/fastprompter/main.py::_rescan_project_sync and src/fastprompter/core/project_sync.py::scan_folder/free_slots
EVIDENCE: `_rescan_project_sync` computes `free_count = max(0, len(presets) - len(mapping))` at line 6531, then bounds `scan_folder` to `free_count` or 1 at lines 6532-6536. `scan_folder` knows nothing about the existing mapping and retains the lexicographically smallest K eligible paths; mapped files therefore consume the entire discovery budget before `new_files = [f for f in files if f not in mapped]` is applied at line 6561. The capacity formula is also not the allocator's capacity: `free_slots` starts allocation at `total=len(presets)` and permits slots through 99, so ten existing silos/mappings still have ninety appendable slots even though `free_count` is zero. Deterministic reproduction with files `00.txt`..`10.txt`, ten presets and mappings for `00.txt`..`09.txt` produced `free_count=0`, bounded `scan_result=['00.txt']`, `new_files=[]`, while `10.txt` physically existed and slot 10 was available.
DEFECT: the performance bound is applied to raw scan results instead of unmapped discovery results, and its K value is calculated from mapping density rather than actual append capacity. Manual “Re-scan folder” and settings-triggered rescans can therefore fail to discover legitimate new files indefinitely whenever lexicographically earlier mapped files fill the bounded result.
REPAIR: bound only UNKNOWN eligible paths. Either teach `scan_folder` to accept the mapped relative paths as an exclusion set before heap retention, or stream eligibility and discard mapped paths before consuming the K budget. Compute K from actual allowable new silo capacity, consistent with `free_slots` and `MAX_SILOS_PER_CATEGORY` (normally `100 - len(presets)` under the current append-from-total contract), not `len(presets) - len(mapping)`.
VERIFY: with ten mapped early-sorted files and a later eleventh file, explicit rescan must bind the eleventh to slot 10. Repeat where free capacity is 1 and dozens of mapped names sort before the new path. Verify lexicographically smallest UNMAPPED candidates are chosen, no slot >=100 is created, and a full 100-silo project performs no new allocation.

[P2] [W2-005] src/fastprompter/main.py::_on_duplicate_copy_done and src/fastprompter/ui/file_container.py::_ContainerOpWorker.done
EVIDENCE: `_ContainerOpWorker.done` is declared with five values at file_container.py:103-105: `(request_id, request, done, partial, errors)`, and `_run` emits all five at line 163. The normal panel receiver `_on_container_done` correctly accepts all five. `_copy_folder_into_container` connects that same shared signal to `self._on_duplicate_copy_done` at main.py:15708, but `_on_duplicate_copy_done` at line 15714 declares only four parameters `(request_id, request, done, errors)`. Thus the duplicate callback does not implement the worker's evolved result contract; with Qt's shorter-slot argument compatibility its `errors` argument receives the fourth `partial` value and the actual fifth error list is discarded.
DEFECT: one duplicated consumer retained the pre-`partial` callback shape after the shared worker result protocol grew to five fields. Async duplicate-silo copy failures therefore cannot be interpreted through the callback's intended `errors` channel, and partial/error semantics can drift independently from the canonical File Container completion handler.
REPAIR: change `_on_duplicate_copy_done` to accept `(request_id, request, done, partial, errors)` and handle/log the real error collection explicitly. Treat unexpected partial outcomes deliberately rather than aliasing them to errors. Add a protocol-level test so all consumers of `_ContainerOpWorker.done` remain signature-compatible when the result schema changes.
VERIFY: emit/dispatch a dup-copy completion with `done=[]`, `partial=[]`, `errors=[("src","boom")]` and assert `_on_duplicate_copy_done` receives the actual error tuple and executes its failure reporting while still refreshing the badge. Emit a synthetic partial result separately and assert it is not mistaken for the errors list. Run under real PyQt6.

[P2] [W2-006] src/fastprompter/core/state.py::_backup_atomically/validate_database/restore_database temporary SQLite candidate lifecycle
EVIDENCE: `_backup_atomically` creates a unique temp DB, closes its destination connection, calls `validate_database(tmp)` at lines 828-829, then either removes/replaces only the main `tmp` pathname at lines 830-836. `validate_database` opens the WAL-mode candidate read-only and closes it but can cause SQLite to create `tmp-wal` and `tmp-shm`; those siblings are never retired by `_backup_atomically`. A deterministic reproduction copied the supplied valid backup to `cand.db`: before validation the directory contained only `cand.db`; after `validate_database(cand.db)` it also contained `cand.db-shm` (32768 bytes) and `cand.db-wal`. The supplied archive itself contains 512 leaked backup-temp sidecars: 256 `*.tmp-wal` and 256 `*.tmp-shm`. This affects restore safety snapshots too because restore_database builds them through `_backup_atomically`. `PYTHONPATH=src pytest -q tests/test_restore.py::TestRestoreDatabase::test_successful_restore_replaces_and_round_trips` failed reproducibly because `glob(live.db.prerestore-*)` selected a leaked `...tmp-shm`/`...tmp-wal` as though it were the safety database, yielding `file is not a database`/`no such table`.
DEFECT: SQLite validation sidecars are outside the temporary candidate's ownership/cleanup model. Successful atomic publication removes the temp main filename from its temporary namespace but leaves its WAL/SHM siblings permanently behind; failure paths do likewise. Repeated backup/restore operations therefore leak filesystem artifacts and pollute namespaces used to identify recovery snapshots.
REPAIR: make every temporary SQLite candidate own its complete file family. After the validating connection closes, remove candidate `-wal` and `-shm` before publishing/retiring the main temp, and clean them on every exception/authorization-refusal path. Apply the same rule to restore's separately-created restore candidate. Do not indiscriminately delete WAL/SHM belonging to arbitrary user-selected/live source databases; cleanup must be limited to temp files created and owned by the operation.
VERIFY: successful and failed `_backup_atomically` calls must leave neither `<tmp>-wal` nor `<tmp>-shm`; restore must leave exactly the intended prerestore safety DB and no sidecars carrying its temporary prefix. Re-run the currently failing restore roundtrip until green. Verify validation of a real live/source DB with pre-existing WAL/SHM does not delete those source-owned files.

[P2] [W2-007] tests_smoke/test_quit_finalize.py and tests_smoke/test_watcher_quiesce_unit.py watcher quiesce contract
EVIDENCE: live `WatcherMixin._watcher_begin_quiesce` explicitly implements reversible phase 1: lines 790-804 state that it leaves the engine ARMED and `_watcher_quiescing=True`; irreversible disarm occurs only in `_watcher_commit_quiesce` lines 854-876. Newer tests_smoke/test_audit_regressions.py:687-709 correctly assert armed=True after begin and armed=False only after commit. In direct contradiction, test_quit_finalize.py:108-117 still expects `_watcher_begin_quiesce()` itself to call `disarm("application is quitting")` and clear `_watcher_quiescing`, while test_watcher_quiesce_unit.py:89-119 likewise expects the engine to be disarmed immediately after begin.
DEFECT: the test suite contains two mutually exclusive sources of truth for a lifecycle invariant. The stale tests protect the old irreversible phase-1 behavior that the current implementation deliberately replaced with reversible begin/commit/rollback semantics. In a PyQt6-capable full run they can fail correct code or pressure a later implementation agent to regress the repaired lifecycle.
REPAIR: update the stale tests to the canonical two-phase contract instead of changing production code: after begin assert paused/quiescing and still armed; after commit assert disarmed/quiescing cleared; on rollback assert the pre-quiesce armed runtime resumes. Remove duplicate obsolete expectations rather than maintaining parallel lifecycle specifications.
VERIFY: run all watcher quit/quiesce suites together under PyQt6 and require one invariant everywhere: BEGIN = reversible pause, COMMIT = irreversible disarm/persist, ROLLBACK = resume. Include successful quit, refused final save, in-flight timeout and profile-switch refusal.

SECOND_WAVE_DONE_WHEN: W2-001 through W2-007 are repaired with regressions covering lease revocation exactly at File Container publication, Sync-Project save refusal, deliberate eligibility changes, bounded discovery behind mapped paths, five-field duplicate-copy completion, zero leaked temp SQLite sidecars and one canonical watcher quiesce contract; the currently failing restore roundtrip is green, the 60 pure project-sync/portable/limit tests remain green, compileall stays clean, and the complete GUI/smoke suite passes in a PyQt6-capable Windows environment.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-28T15:13:47+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: NOT_PROVIDED
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: FastPrompter_28.08.26-T07-33-57(2).zip, fully unpacked and audited at /mnt/data/_audit_fastprompter
BASELINE: archive SHA256 a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab; pyproject version 0.8.60; live archive filesystem authoritative over Git HEAD
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive worktree is heavily dirty with substantive post-HEAD implementation/test changes; live files were audited
SAIPEN_CONTEXT: STALE - STATE.md reports phase DONE/board empty, while BOARD.md contains active T-1121..T-1138 work including performance backlog; treated only as orientation and independently verified against live code
AUDIT_SCOPE: Sync-Project discovery/filtering/allocation hot paths; repeating timer and ProductivityTimer catch-up; interval notification scheduler/canonicalization; editor fold/centering metadata restore; scaled-WAV cache retention; File Container listing/thumbnail/cache bounds; autosave/extraction/routing and existing performance regressions
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: PyQt6 is unavailable in the sandbox although correctly declared by the project. `PYTHONPATH=src pytest -q tests/test_perf004_hidden_date_gate.py` fails during collection importing fastprompter.main with `ModuleNotFoundError: No module named 'PyQt6'`; real Qt paint/event-loop and Windows winsound timings could not run.
VERIFIED_INSTEAD: `PYTHONPATH=src pytest -q tests/test_timers.py tests/test_pomodoro.py tests/test_project_sync.py tests/test_limit_scan.py tests/test_performance.py` = 170 passed; `python -m compileall -q src FastPrompter.pyw tools tests tests_smoke` passed; exact pure-code benchmarks/reproductions exercised scan_folder, Timer.advance, ProductivityTimer.tick, interval-rule methods and scaled-cache pruning
STATUS: PERFORMANCE: COMPLETE
TICKETS: 6
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: src/fastprompter/core/project_sync.py::is_text_file/scan_folder/free_slots; src/fastprompter/main.py::_change_project_sync_folder/_rescan_project_sync/_check_timers/_tick_productivity/_interval_notifs/_check_interval_notifs/restore_silo_state/_restore_centered_blocks and existing save/snapshot/cache worker paths; src/fastprompter/core/timers.py::Timer.advance/_advance_monthly/_advance_yearly/collect_due; src/fastprompter/core/pomodoro.py::ProductivityTimer.tick; src/fastprompter/core/sound_manager.py::_prune_scaled_cache_dir/_bounded_cache_insert/winsound fallback; src/fastprompter/ui/editor.py::collect_view_metadata/paint typo and visible-block paths; src/fastprompter/ui/file_container.py refresh coalescing, folder caches, thumbnail LRU/pending-generation bounds
COVERAGE_DEFERRED: native Qt paint/layout timings, Windows QFileSystemWatcher/filesystem-provider behavior, actual winsound playback latency, physical HDD/NAS scan timings, and unrelated correctness/UI/i18n surfaces outside this wave
CROSS_WAVE_REFERENCES: W2-004 covers the separate correctness defect where bounded Sync-Project discovery lets already-mapped early paths consume the K-result and computes capacity from the wrong quantity; PERF-001 below addresses the independent GUI-thread/full-tree CPU and memory cost and must be repaired without reintroducing W2-004. CORE-008 concerns interval preset label correctness only and is independent of PERF-005.
RESIDUAL_UNCERTAINTY: filesystem benchmarks ran on the sandbox filesystem and likely understate Windows/HDD/NAS latency; PyQt6 absence prevents end-to-end GUI frame-time measurement, so editor metadata impact is established from its exact list-membership algorithm plus equivalent Python benchmarks rather than live QTextDocument timings. No campaign run-id or canonical previous-wave checksum was supplied, so neither was invented.

[P1] [PERF-001] PROVEN BOTTLENECK src/fastprompter/core/project_sync.py::is_text_file/scan_folder + src/fastprompter/main.py::_change_project_sync_folder/_rescan_project_sync
EVIDENCE: `_change_project_sync_folder` calls `ps.scan_folder(...)` synchronously from the GUI handler with `limit=None`, then uses at most `MAX_SILOS_PER_CATEGORY` (100) results. `_rescan_project_sync` also invokes `scan_folder` synchronously. `scan_folder` traverses the complete tree, and `is_text_file` performs the full exclude-pattern pass before the cheap extension/include rejection. The bounded path keeps only K results but still scans all N and uses a Python `_NegStr` heap wrapper for every eligible winner comparison. Exact sandbox benchmarks: 20,000 irrelevant `.bin` files with `limit=100` took 1.097185 s; a semantically equivalent pass that rejected the trivial extension before file-level exclude matching took 0.291470 s. With 20,000 eligible `.txt` files, current `limit=None` took 2.527080 s and retained all 20,000 results (1.47 MiB traced Python peak), while `limit=100` retained 100 but took 5.711416 s (0.044 MiB peak). All of this work occurs before control returns to the GUI.
ISSUE: the existing bounded-result repair fixed result memory but not the dominant full-tree GUI-thread latency, and its Python max-heap can increase CPU substantially for eligible trees. Initial conversion additionally collects/sorts every eligible file even though only 100 can ever be bound. Large repositories therefore freeze folder conversion/rescan for seconds, with worse expected latency on Windows HDD/NAS storage.
OPTIMIZE: move Sync-Project discovery to a dedicated generation-owned/coalesced worker using one in-flight + newest-pending semantics; reject obviously non-included files before expensive file-level exclude-pattern evaluation while preserving excluded-directory pruning; initial conversion must request only actual capacity. Keep result memory bounded, but do not assume the current `_NegStr` heap is the fastest bounded selection algorithm. Coordinate capacity semantics with W2-004 so mapped paths are excluded before consuming new-file capacity and actual free slot count is used.
GUARDRAIL: preserve the exact include/exclude/special-basename contract, max_bytes checks, recursive behavior and deterministic lexicographic winner order. A stale worker result from an old root/category/profile/filter generation must never apply. Existing bindings and W2-003 temporary-unsyncability semantics must remain intact.
VERIFY: create 20k+ irrelevant files and assert the GUI path only schedules work, with no synchronous tree traversal; worker output must equal the legacy eligible sorted prefix exactly. Verify cheap extension rejection avoids file-level exclude matching for obvious non-candidates. Create 20k eligible files with capacity K<=100 and assert retained discovery state is O(K), newest pending request wins, stale results are discarded, and W2-004 cases with mapped early-sorted files still discover the correct unmapped winners.

[P1] [PERF-002] PROVEN BOTTLENECK src/fastprompter/core/pomodoro.py::ProductivityTimer.tick + src/fastprompter/main.py::_tick_productivity
EVIDENCE: `ProductivityTimer.tick` consumes elapsed time by looping over phase transitions with a hard `for _ in range(1000)` cap, appending every completed phase to `ended`. `_tick_productivity` then synchronously calls `_notify_productivity` once for every entry, including sound/tray work. Exact model reproduction with 60 s work / 30 s break: a 3,600 s stall returns 80 historical phase completions; 43,200 s returns 960. At 46,860 s the hard cap loses elapsed state: one-shot catch-up returned 1000 completions, phase=work, remaining=60, completed_cycles=500, while a one-second-step reference produced 1041 completions, phase=break, remaining=30, completed_cycles=521.
ISSUE: elapsed-time correctness is only valid below 1000 crossed phases. Before that boundary, resume from a long event-loop stall can synchronously replay tens or hundreds of obsolete sounds/tray notifications; beyond it, the guard silently drops residual elapsed time and leaves the productivity timer in the wrong phase/cycle state. The protection against an unbounded loop therefore trades one stability failure for another.
OPTIMIZE: fast-forward complete work/break cycles arithmetically, then resolve only the residual partial phase. Preserve exact state/cycle accounting for arbitrarily large finite elapsed values without walking every historical transition. Bound user-visible catch-up notification work so resume surfaces the relevant latest completion rather than replaying an unbounded history.
GUARDRAIL: preserve current exact behavior for ordinary short ticks and short multi-phase stalls, including work-only mode, completed-cycle semantics, pause state, `alarm_pending`, `alarm_phase`, repeat-alarm behavior and exact remaining seconds. Never convert a long stall into a notification storm.
VERIFY: retain existing 10 s/70 s/150 s tests; compare one large `tick(elapsed)` against repeated 1-second reference ticks for final phase, remaining and completed_cycles at 1 h, 12 h, 13 h 1 m and much larger elapsed values. Assert catch-up notification dispatch is explicitly bounded and the final pending alarm describes the latest relevant completed phase.

[P2] [PERF-003] PROVEN BOTTLENECK src/fastprompter/core/timers.py::Timer.advance/_advance_monthly/_advance_yearly/collect_due
EVIDENCE: interval/daily/weekly `advance` executes `while self.target <= now: self.target += step`; monthly repeatedly calls `_next_monthly_after` until future; yearly similarly repeats a helper that itself searches from the recurrence anchor. `collect_due`, called from the 1 Hz GUI timer, invokes `t.advance(now)` synchronously for every due timer. Exact benchmark for a one-minute repeating timer stale relative to `now`: 1 year = 525,601 iterations / 0.033267 s; 5 years = 2,628,001 / 0.165651 s; 10 years = 5,256,001 / 0.371944 s; 20 years = 10,512,001 / 0.650633 s. All cases eventually produced a future target, but only after linear catch-up on the GUI thread.
ISSUE: recurrence advancement cost scales with the number of missed occurrences rather than remaining constant. Fine-grained interval timers restored after a long stale period can freeze the scheduler/UI despite only one result being required: the next occurrence strictly after `now`.
OPTIMIZE: compute skipped interval/daily/weekly periods arithmetically (`floor((now-target)/step)+1`). For monthly/yearly recurrence, calculate the first anchor-valid occurrence directly after `now` rather than walking every missed occurrence; yearly helper should derive the candidate year directly instead of rescanning thousands of anchor years.
GUARDRAIL: preserve immutable recurrence-anchor semantics, end-of-month/leap-day clamping, target hour/minute/second, the rule that a future target is a no-op, and exactly one due notification rather than replaying missed occurrences.
VERIFY: compare optimized results against the current iterative algorithm over dense short reference ranges for interval/daily/weekly/monthly/yearly, including Jan-31 and Feb-29 anchors. Add decade-scale one-minute interval cases and assert runtime is independent of number of missed periods within a small constant factor while target is exactly the first valid occurrence `> now`.

[P2] [PERF-004] STRONGLY EVIDENCED WASTE src/fastprompter/main.py::restore_silo_state/_restore_centered_blocks
EVIDENCE: fold restoration walks every QTextBlock and tests `b.text().strip() in folded`, where `folded` is a list. Center-alignment restoration likewise walks every block and tests `block.text() in centered`, another persisted list. This is O(blocks × tracked_entries) membership work on silo/document restoration. Equivalent Python membership benchmarks using the exact list-vs-set access pattern: 2,000 blocks / 1,000 tracked = 0.012816 s list vs 0.000084 s set; 5,000/2,500 = 0.073465 vs 0.000195; 10,000/5,000 = 0.358419 vs 0.000455; 20,000/10,000 = 1.223682 vs 0.001063. Actual Qt formatting/toggle work is additional.
ISSUE: persisted block metadata is a membership set semantically but is consumed as a linear-search list. Large documents with many folded or historically centered headings therefore add quadratic Python work to silo/document restoration on the GUI thread.
OPTIMIZE: normalize the loaded membership collections once per restore (`folded_set = set(folded)`, `centered_set = set(centered)`) and use O(1)-average membership while traversing document blocks. Keep persisted JSON shape unchanged unless another verified requirement warrants migration.
GUARDRAIL: exact text matching must remain unchanged: folded uses stripped block text, centered uses raw block text. Duplicate persisted entries currently have no semantic effect and must continue to have none. Preserve fold-bit checks and alignment behavior.
VERIFY: regression with duplicate and non-duplicate tracked strings must restore exactly the same blocks before/after optimization. Add a large-document performance test that demonstrates near O(B+M) membership behavior rather than O(B×M), without changing persisted `folded`/`centered_blocks` representation.

[P2] [PERF-005] STRONGLY EVIDENCED WASTE src/fastprompter/main.py::_interval_notifs/_check_interval_notifs/_interval_top_bar_candidate
EVIDENCE: `_interval_notifs` runs `_heal_interval_rule` over every stored rule and `_heal_interval_rule` begins with `r = dict(rule)`, so the 1 Hz scheduler allocates a full copy of every rule even when already canonical. When the interval top-bar candidate is evaluated, the same canonicalization can occur again in the same second. More seriously, whenever `_check_interval_notifs` changes any timestamp, it loops every healed rule and then linearly scans the entire raw list by ID to copy timestamps back, yielding O(N²) writeback. Exact execution of the live extracted methods with canonical rules: `_interval_notifs` averaged ~0.001874 s/call at 1,000 rules, ~0.014231 s at 5,000 and ~0.019486 s at 10,000. A first elapsed-mode tick, which seeds timestamps and enters the nested writeback, took 0.024906 s at 1,000 rules, 0.134613 s at 2,000 and 0.592984 s at 5,000. The UI exposes unrestricted rule creation; there is no canonical rule-count cap.
ISSUE: validation/canonicalization is being paid as recurring scheduler work rather than mutation/load-boundary work, and copy semantics forced a second nested reconciliation implementation merely to persist scheduler timestamps. This creates continuous allocation churn plus an avoidable quadratic GUI-tick path whenever rules become dirty.
OPTIMIZE: canonicalize interval rules at load/profile-change and rule mutation boundaries, then let the scheduler consume canonical live rule objects directly or through a revision-keyed cache. Remove the nested ID reconciliation by updating the authoritative canonical objects, or build a single ID index once if copies must remain for another verified invariant.
GUARDRAIL: malformed-rule healing, duplicate-ID first-wins behavior, canonical JSON writeback, clock/elapsed alignment, topmost collision priority and `last_fired`/`last_fired_minute` deduplication must remain identical. Profile replacement or external data reload must invalidate any canonical cache.
VERIFY: existing malformed/healing and clock-boundary tests remain green. Add scaling tests for N canonical rules proving a no-fire 1 Hz check performs no per-rule dict copies after canonicalization and a dirty tick is O(N), not O(N²). Validate timestamp persistence through save/reload and collision suppression remains exact.

[P2] [PERF-006] STRONGLY EVIDENCED WASTE src/fastprompter/core/sound_manager.py::_prune_scaled_cache_dir
EVIDENCE: the prune routine snapshots `entries`, then checks `len(entries) <= _SCALED_CACHE_MAX_FILES` inside its eviction loop but never decreases that length when a file is successfully removed; only `total` bytes are decremented. Therefore once file count exceeds the cap while byte usage is otherwise legal, the file-count condition remains false for the entire loop and every removable old entry is deleted instead of only the excess. Exact execution of the live function with PyQt imports stubbed, `MAX_FILES=3`, huge byte budget, zero grace and five old one-byte WAVs produced `before_count=5`, `after_count=0`; correct oldest-first file-count pruning would retain three. The function runs at SoundManager startup and after writing a new scaled WAV, and the source documents the winsound fallback as the shipped-build path.
ISSUE: crossing the file-count threshold causes a cache cliff rather than bounded LRU-like eviction. Useful scaled WAVs are discarded en masse, forcing later playback to recreate and rewrite them; repeated threshold crossings create avoidable CPU/disk churn precisely where the cache was meant to prevent per-sound scaling work.
OPTIMIZE: maintain a live remaining-file count and decrement it only after successful removal; stop as soon as both byte and file budgets are satisfied. Continue respecting `protect` and grace-period entries; temporary over-budget state is acceptable when every otherwise-evictable candidate is protected/recent.
GUARDRAIL: never delete the currently protected just-written file or a WAV inside the asynchronous-playback grace window. Preserve oldest-first eviction, byte-budget enforcement and best-effort handling of filesystem errors.
VERIFY: with five old files and cap=3, exactly the two oldest removable files are deleted and three remain. Add mixed byte+count pressure, protected-file, young-file and failed-remove cases. Repeated insertion just over the cap must stabilize near the configured budget rather than collapsing the cache to one/few files.

PERFORMANCE_DONE_WHEN: PERF-001 through PERF-006 are repaired with regressions proving off-GUI/coalesced capacity-correct Sync-Project discovery, constant-time stale timer advancement, exact arithmetic ProductivityTimer catch-up with bounded resume notifications, linear block-metadata restoration, O(N) interval scheduling without recurring canonical-copy churn, and precise scaled-WAV count eviction; the 170 relevant pure tests remain green, compileall remains clean, W2-004 discovery correctness is preserved, and GUI/winsound performance regressions pass in a PyQt6-capable Windows environment.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.