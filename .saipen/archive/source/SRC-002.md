# FastPrompter — Audit Handoff

RUN_ID: acb-mtdl6q6l-c74f68708fad4c19898f
GENERATED_AT: 2026-08-29T03:13:25.535743
PROJECT_NAME: FastPrompter
TARGET: supplied FastPrompter_28.08.26-T07-33-57(4).zip, fully extracted and audited as FastPrompter 0.8.60; audit role/contract from attached instruction :contentReference[oaicite:0]{index=0}
BASELINE: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive contains a non-clean working tree (564 porcelain entries); supplied live files, including uncommitted changes, were treated as authoritative rather than HEAD
SAIPEN_CONTEXT: STALE - .saipen/STATE.md says phase DONE / board empty while .saipen/BOARD.md contains active TODO repairs; used only as orientation, not authority
TOTAL_TICKETS: 18
STATUS: AUDIT_ALL_3: COMPLETE

## Summary of Wave Audits
- Wave 1 (Core): 7 tickets (baseline: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab)
- Wave 2 (Second Wave): 4 tickets (baseline: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab)
- Wave 3 (Performance): 7 tickets (baseline: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab)

---
## 01 — AUDIT CORE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-29T02:40:24+03:00
WAVE: AUDIT CORE
TARGET: supplied FastPrompter_28.08.26-T07-33-57(4).zip, fully extracted and audited as FastPrompter 0.8.60; audit role/contract from attached instruction :contentReference[oaicite:0]{index=0}
BASELINE: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - archive contains a non-clean working tree (564 porcelain entries); supplied live files, including uncommitted changes, were treated as authoritative rather than HEAD
SAIPEN_CONTEXT: STALE - .saipen/STATE.md says phase DONE / board empty while .saipen/BOARD.md contains active TODO repairs; used only as orientation, not authority
AUDIT_SCOPE: entry/lifecycle orientation; SQLite state save/backup/restore/profile transitions; backup concurrency and shutdown/drain ownership; restore UI orchestration; nested-silo filesystem merge journal and undo durability; interval default/preset contract; directly relevant regression tests
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: PyQt6, markdown, and pynput are declared project dependencies but absent in this sandbox; GUI-dependent collection cannot run (for example tests/test_audit_core_tickets.py and tests/test_mt9141_audit.py fail collection with ModuleNotFoundError: No module named 'PyQt6')
VERIFIED_INSTEAD: all 280 Python files under src/tests/tests_smoke passed byte-level compile() syntax checking; focused backup/restore/startup pytest ran 22 passed + 1 genuine project failure; broader state/profile/backup subset ran 88 passed before 5 PyQt6 environment failures; three focused threaded replays independently reproduced authorization TOCTOU, destination in-flight undercount, and false publication callback
STATUS: AUDIT_CORE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT

[P1] [CORE-001] src/fastprompter/core/state.py::_backup_atomically / _backup_publication_authorized
EVIDENCE: _backup_atomically checks authorize() at state.py:830, but _backup_publication_authorized releases _BACKUP_LOCK when its call returns at state.py:916-917; os.replace(tmp, dest_path) then occurs later at state.py:833 with no coordinator lock held. A focused replay blocked exactly at os.replace after authorize returned True, called _revoke_backup_generation(), confirmed authorization had become False, released the replace, and observed dest_still_old=False: the revoked worker still overwrote the previous backup. tests/test_second_wave_core003.py:128-167 does not exercise this interleaving: it revokes before the outer authorization guard, so the real publication path is never entered.
DEFECT: generation/revocation authorization and publication are two separate critical sections. Revocation, restore drain, profile retirement, or a newer generation can occur after the final check but before os.replace, allowing a stale backup worker to publish into a destination it no longer owns.
REPAIR: make authorization + final publication one indivisible coordinator operation. Build and validate the candidate outside _BACKUP_LOCK, then acquire _BACKUP_LOCK, evaluate _backup_publication_authorized_locked(key, generation), and perform the destination os.replace before releasing that same lock. On denial discard the exact candidate without touching the existing destination. Apply the same primitive to both periodic and startup publishers.
VERIFY: barrier immediately before the real final replace; authorize generation G, concurrently revoke or advance to G+1, then release barrier and assert the old destination remains byte-identical and the candidate family is removed. Also prove an unchanged authorized generation publishes successfully. Exercise both _schedule_periodic_backup and _start_safety_snapshot_async.

[P1] [CORE-002] src/fastprompter/core/state.py::_BACKUP_INFLIGHT / _schedule_periodic_backup / _start_safety_snapshot_async / _drain_db_backup
EVIDENCE: periodic workers set _BACKUP_INFLIGHT[dest_key] = True at state.py:1002-1003 and unconditionally pop that destination at 1044-1047. Startup workers independently set the same boolean destination key at 1747-1750 and pop it at 1774-1776. The registry therefore cannot represent two physical workers targeting the same database. A focused replay started two periodic workers for the same db_path under distinct profile keys: while both ran the destination key was True; after worker A exited, A popped the destination key while worker B remained active; _drain_db_backup(db, timeout=.05) then returned True while B was still in _BACKUP_INFLIGHT under its profile key. Profile switching only revokes the old generation at state.py:1672-1678 and can therefore leave an old physical worker overlapping a later re-entry/startup worker.
DEFECT: destination worker ownership is represented as a boolean rather than a count/token set. The first finishing worker can erase another worker's registration, making restore/shutdown believe the destination is drained while a SQLite backup connection is still active.
REPAIR: separate profile-level coalescing state from destination-level physical-worker tracking. Track every physical periodic/startup worker for a destination with a refcount or unique token set; register before thread start and retire only that worker's token in finally. _drain_db_backup must return True only when the destination's physical-worker set/count reaches zero.
VERIFY: run two overlapping periodic workers for one destination, release only the first and assert drain still times out; release the second and assert drain succeeds. Repeat with one startup worker plus one periodic worker. Include profile A -> B -> A re-entry and shutdown drain cases.

[P1] [CORE-003] src/fastprompter/main.py::FastPrompter.restore_db
EVIDENCE: restore_db successfully saves and quiesces the watcher, then closes self.state.conn at main.py:13095-13100. Only afterward it calls _drain_db_backup(db_path) at 13108-13111 and completely ignores the function's boolean result. _drain_db_backup explicitly returns False on timeout at state.py:935-958. A timeout also leaves _BACKUP_DRAINING[key] and _BACKUP_REVOKED[key] set because those entries are cleared only on the successful branch at 952-956. Despite False, restore_db immediately enters restore_database(path, db_path) at main.py:13112-13113.
DEFECT: restore treats a fail-closed quiesce API as fire-and-forget. A timed-out backup may still hold/read the database while restore begins replacing it, especially problematic on Windows file locking; with CORE-001 it can additionally publish stale recovery state. Because the live connection was already closed, the application also destroys its easiest clean-abort state before knowing backup quiescence succeeded, and a timed-out drain leaves future backup admission latched.
REPAIR: after watcher quiesce but before closing the live SQLite connection, require _drain_db_backup(db_path) is True. On False, do not call restore_database, keep/recover the live connection, resume the watcher, and return a user-visible refusal. Add an explicit safe cancelled-drain transition for this abort path: advance/invalidate the old generation before clearing the draining admission barrier so old workers stay unauthorized while future generations can resume. Only close the live connection and begin restore after a proven successful drain.
VERIFY: block a real backup worker longer than the drain timeout and invoke restore: assert restore_database is never called, the live DB bytes are unchanged, the existing/reopened connection remains usable, watcher runtime resumes, and a later backup generation can be scheduled after the blocked worker retires. Then release the worker and prove the normal restore path still succeeds.

[P1] [CORE-004] src/fastprompter/ui/snippet_ops_mixin.py::_merge_journal_* / src/fastprompter/main.py::make_silo_child / _write_undo_file
EVIDENCE: _merge_journal_write persists only path records plus done flags at snippet_ops_mixin.py:51-65; it stores no child/parent or logical transaction identity. _reconcile_merge_journal(root, data) receives loaded authoritative data but never inspects it: lines 101-136 unconditionally reverse every surviving moved file back to its original child path. make_silo_child mutates silo_children at main.py:16762-16777, physically moves files, queues an undo save with merge_journal_root at 16786-16811, and only then calls mark_dirty at 16812. Undo persistence has a 1-second debounce at main.py:12405-12433, while primary SQLite autosave runs every 10 seconds at 909-911/4684-4687. _write_undo_file clears the merge journal immediately after the undo JSON os.replace succeeds at 12553-12567. Thus the ordinary timing can clear the only recovery journal roughly nine seconds before the hierarchy is durably saved to SQLite. The opposite ordering is also possible if the DB is force-saved first: a crash then leaves a surviving journal which startup blindly rolls back even though SQLite already committed the nesting.
DEFECT: crash recovery is bound to durability of secondary undo history rather than the authoritative SQLite hierarchy transaction. There are two opposite split-brain windows: undo/journal retirement before DB commit leaves DB saying child-owned while files remain under parent with no journal; DB commit before journal retirement leaves DB saying parent-owned while restart moves files back to child.
REPAIR: make SQLite hierarchy state the transaction authority. Extend the merge journal with enough stable logical identity to determine the intended child/parent relationship. Do not retire it merely because undo JSON landed. On startup, inspect the loaded silo_children state: if nesting is durably committed, keep/finish the files in the parent and retire the journal; if nesting is not committed, reverse completed moves to the child. Undo history may retain the move ledger for Ctrl+Z but must not decide crash commit/rollback.
VERIFY: fault-inject each boundary: (1) files moved + undo published + process death before SQLite hierarchy save => restart reverses files and retains pre-nest ownership; (2) SQLite hierarchy committed + death before journal deletion => restart keeps files in parent and retires journal; (3) undo writer fails while SQLite commits => recovery still follows SQLite. Test partial 1-of-N moves and idempotent repeated reconciliation.

[P2] [CORE-005] src/fastprompter/core/state.py::_backup_atomically / _schedule_periodic_backup
EVIDENCE: when final authorization rejects publication, _backup_atomically removes its candidate and returns None at state.py:830-832. Successful publication also has no distinct return value. _schedule_periodic_backup calls the helper at 1026 and then unconditionally sets published=True at 1027; on_published therefore runs at 1033-1036 even when no destination swap happened. A focused replay using the real _backup_atomically made the outer authorization pass and the helper's final authorization fail: AUTH_CALLS=2, BAK_EXISTS=False, CALLBACK=['published']. FastPrompterState._on_backup_published then stamps _last_backup_time_by_profile at state.py:2095-2102, and subsequent saves suppress refresh for 60 seconds at 2301-2313.
DEFECT: the helper has no publication-result contract, so an intentionally rejected stale worker is reported as a successful backup. The old profile's throttle advances even though its .bak was not refreshed, suppressing a legitimate retry after rapid profile re-entry.
REPAIR: make _backup_atomically return an explicit publication result, e.g. True only after os.replace succeeds and False on authorization denial; exceptions remain failures. _schedule_periodic_backup must set published and invoke on_published only from that True result. Update callers/tests so success cannot be inferred merely from absence of an exception.
VERIFY: force outer authorization True and final authorization False using the real helper; assert destination unchanged/absent, callback count zero, and throttle timestamp unchanged. Positive publication must callback exactly once. Re-enter the profile after a denied worker and prove the next eligible save can refresh .bak.

[P2] [CORE-006] src/fastprompter/core/state.py::_backup_atomically / restore_database
EVIDENCE: _backup_atomically allocates a unique SQLite candidate at state.py:820, but success/failure cleanup handles only that main tmp path at 830-835; it never removes tmp + "-wal" or tmp + "-shm". The focused restore suite produced a real failure: tests/test_restore.py::TestRestoreDatabase::test_successful_restore_replaces_and_round_trips failed after a successful restore because glob(live + ".prerestore-*") selected an orphan `live.db.prerestore-<uuid>.tmp.bak-<uuid>.tmp-wal`, which SQLite then reported as lacking the application tables. Result: 22 passed, 1 failed. restore_database already explicitly removes the separate restore candidate's sidecars at state.py:1486-1493, demonstrating that SQLite temp-family sidecars are expected artifacts, but the shared _backup_atomically helper does not perform equivalent cleanup.
DEFECT: successful or rejected atomic backups can leak SQLite WAL/SHM siblings belonging to their temporary candidate. Besides filesystem debris, those files match recovery-artifact prefixes and can be mistaken for actual pre-restore safety snapshots, as the existing test concretely demonstrates.
REPAIR: centralize candidate-family cleanup inside _backup_atomically. In a finally/controlled success path, remove only tmp + "-wal" and tmp + "-shm" belonging to the exact unique candidate on success, authorization denial, and exception; never touch dest_path's live sidecars. Preserve the published main destination.
VERIFY: rerun the currently failing restore round-trip. Repeatedly back up a WAL-mode source on success, forced validation failure, and authorization denial, then assert no `<candidate>-wal`/`<candidate>-shm` survives and every discovered `prerestore-*` main artifact validates as a FastPrompter DB.

[P2] [CORE-007] src/fastprompter/ui/timer_dialog.py::DEFAULT_INTERVAL_RULES / _interval_show_presets_menu
EVIDENCE: all four canonical DEFAULT_INTERVAL_RULES use volume 1.0 at timer_dialog.py:134-199, and core/default_profile.py:607-663 independently installs the default 24-hour interval schedule at volume 1.0. The Defaults tooltip still promises "(0.05 volume)" at timer_dialog.py:2590-2592 and the preset action still says "Default 24h Chime (Genie / NewDay / Owl @ 0.05)" at 2957. Choosing it deep-copies DEFAULT_INTERVAL_RULES at 2968-2969, therefore actually installing volume 1.0. Other explicit presets below it genuinely use 0.05 and are not the mismatch.
DEFECT: the UI advertises a 5% default chime volume while the selected/reset default is 100%. This is a concrete user-visible contract error, particularly unpleasant for an audio notification feature because the discrepancy is twenty-fold.
REPAIR: make the default preset label and Defaults tooltip reflect the canonical default volume (currently 1.0), preferably deriving display text from the same canonical default rather than duplicating the numeric value. Leave presets intentionally configured to 0.05 unchanged.
VERIFY: assert the displayed Default 24h Chime volume equals the volume actually installed by both Reset and Default preset selection; assert every canonical default rule remains 1.0 and the Workday/Hourly/Pomodoro/Night Owl 0.05 presets remain unchanged.

CORE_DONE_WHEN: authorization check and backup replace are atomic against generation/revocation; every physical destination worker is independently tracked and drain cannot succeed early; restore refuses and cleanly recovers from a failed drain before closing/replacing the live DB; merge crash recovery follows durable SQLite hierarchy rather than undo-file timing; denied backup publication cannot fire success/throttle callbacks; atomic-backup temp WAL/SHM families are always retired; default interval UI reports the actual 1.0 volume; focused concurrency/crash/restore regressions pass, the existing restore sidecar failure is green, and the full declared-dependency test suite is run in an environment providing PyQt6/markdown/pynput.

---
## 02 — AUDIT SECOND WAVE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-29T02:50:59+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNSPECIFIED (ACB_CHAIN_RECEIPT: second-mtdllez3-4ee1ff7d8037)
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: second
WAVE_INDEX: 2
WAVE_COUNT: 3
WAVE: AUDIT SECOND WAVE
TARGET: supplied FastPrompter_28.08.26-T07-33-57(4).zip, fully extracted and audited as FastPrompter 0.8.60
BASELINE: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - supplied archive contains 564 porcelain-status entries, so live supplied files including uncommitted state were treated as authoritative
SAIPEN_CONTEXT: STALE - .saipen/STATE.md reports phase DONE / board empty while .saipen/BOARD.md contains unresolved TODO work including migration, codec and sync-map defects; used only as orientation
AUDIT_SCOPE: startup schema migration transaction boundaries; structured settings encode/decode symmetry; missed-timer restart persistence; project-sync persisted-key validation and rescan failure containment; initial-boot versus profile-switch normalization; focused state/migration/codec/project-sync/restore regressions; prior-wave backup finding overlap
TEST_STATUS: TEST_FAILED
TEST_LIMITATION: PyQt6, markdown and pynput are declared dependencies but unavailable in this sandbox; focused command `PYTHONPATH=src pytest -q tests/test_state_migration.py tests/test_state_codec.py tests/test_project_sync.py tests/test_restore.py` hit `ModuleNotFoundError: No module named 'PyQt6'` in the GUI-dependent recovery-consumer test. The same run also contains one genuine project failure in restore temp-sidecar handling already diagnosed as CORE-006.
VERIFIED_INSTEAD: focused suite completed with 89 passed / 2 failed; one failure is environment-only and one is prior-wave CORE-006. Custom real-SQLite replays reproduced destructive migration commit before row validation and missed_timer_ids type loss; codec/free_slots replay reproduced malformed project-sync key propagation and ValueError; boot-normalizer replay reproduced category-wide hierarchy loss from one malformed member; all 280 Python files under src/tests/tests_smoke passed byte-level compile() syntax checking without writing project files
STATUS: SECOND_WAVE: COMPLETE
TICKETS: 4
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: src/fastprompter/core/state.py migration dispatcher, v0->v1 merge, loader-row validation, structured codec registry/normalizers, settings encoder/loader and state save path; src/fastprompter/core/project_sync.py::free_slots; src/fastprompter/main.py initial silo-state normalization, _normalise_int_keys, _load_missed_ids/_persist_missed_ids, _rescan_project_sync; relevant tests in test_state_migration.py, test_state_codec.py, test_project_sync.py and test_restore.py; .saipen orientation and Git baseline
COVERAGE_DEFERRED: full PyQt6 GUI/event-loop startup, live profile-switch UI execution, native pynput/hotkey behavior and markdown-rendering paths could not execute in this sandbox; backup authorization/drain/temp-sidecar roots already covered by CORE-001/CORE-002/CORE-003/CORE-005/CORE-006 were not redundantly re-ticketed
CROSS_WAVE_REFERENCES: CORE-006 directly explains the genuine restore test failure observed again in this wave and remains a prior-wave repair, not a new W2 ticket. CORE-001/CORE-002/CORE-003 cover backup publication/drain/restore lifecycle defects adjacent to this wave's lifecycle scope and were inspected only for overlap. No W2 ticket below duplicates those roots.
RESIDUAL_UNCERTAINTY: W2-004 full FastPrompter GUI startup/profile-switch parity could not be runtime-executed because importing fastprompter.main requires unavailable PyQt6; its persistence boundary and destructive boot expression were nevertheless independently reproduced from the live codec output. W2-002 persistence loss is runtime-proven at the SQLite layer, while direct execution of _load_missed_ids is likewise GUI-import limited.

[P1] [W2-001] src/fastprompter/core/state.py::_migrate_v0_to_v1 / _migrate_schema / _assert_loader_rows / FastPrompterState.init_db
EVIDENCE: _merge_legacy_into_v2 reads every legacy `(slot, content)` at state.py:658-659 and inserts a non-colliding slot verbatim at 684-688 without validating slot type or the loader's 0..99 invariant. _migrate_v0_to_v1 then drops `temp_presets` at 711-714 / `archive_temp_presets` at 716-719, records `PRAGMA user_version = 1` at 733 and verifies only structural schema requirements at 736. _migrate_schema commits this transaction at 770-778. The row-level invariant is checked only later during init_db at 1819-1828, after the migration has already committed. Three real SQLite startup replays with v0 `temp_presets.slot` values -1, 100 and `"oops"` all raised DatabaseOverflowError only after migration; reopening each failed database showed `user_version=1`, legacy `temp_presets` absent, and the invalid row permanently resident in `temp_presets_v2` respectively as `('Code', -1, 'BAD')`, `('Code', 100, 'BAD')`, and `('Code', 'oops', 'BAD')`.
DEFECT: migration's claimed transactional postcondition covers table/column shape but not the row invariants required by the immediately following loader. A malformed legacy row therefore allows the destructive migration to commit, drop the legacy source table and bump the schema version, after which startup refuses the newly-created v1 database. The rollback guarantee at state.py:746-750 is bypassed because the condition proving the migrated database unusable is evaluated outside the migration transaction.
REPAIR: enforce loader-readable row invariants while the migration transaction is still open and before COMMIT. The smallest consistent change is to run the applicable row-validity postcondition after `_migrate_v0_to_v1` and before state.py:778, raising MigrationError on an unplaceable invalid temp/archive slot so `_migrate_schema` rolls the entire v0->v1 transaction back. If invalid legacy slots are deliberately relocated instead, relocation must be deterministic into legal 0..99 slots and validated before legacy tables are dropped/transaction committed. Do not merely move the existing post-startup exception earlier while still committing first.
VERIFY: construct separate unversioned databases containing legacy temp and archive rows at -1, 100 and a non-integer slot. For every case that cannot be legally relocated, initialization must fail with migration failure while `PRAGMA user_version` remains 0, legacy tables and original rows remain intact, and no committed v1 partial state survives. Also prove a normal legal v0 fixture still migrates to v1, removes legacy tables only after successful validation, and preserves every snippet.

[P1] [W2-002] src/fastprompter/core/state.py::_JSON_SETTINGS / _STRUCTURED_CODECS / _encode_setting_value / FastPrompterState.init_db; src/fastprompter/main.py::_persist_missed_ids / _load_missed_ids
EVIDENCE: `_persist_missed_ids` stores `sorted(missed)` as a Python list in `self.data["missed_timer_ids"]` and marks settings dirty at main.py:1355-1361. `missed_timer_ids` is absent from `_JSON_SETTINGS` at state.py:23-78 and absent from `_STRUCTURED_CODECS` at 152-282. `_encode_setting_value` therefore falls through to `str(value)` at 519-523. On reload, init_db only structured-decodes registered keys at 1832-1840, so this row returns as a raw string. A real SQLite save/reopen replay produced `raw_sql=("['timer-123']",)` and reloaded `missed_timer_ids` as `str "['timer-123']"`. `_load_missed_ids` at main.py:1337-1353 accepts only list/tuple/set, so that reloaded string produces an empty missed-ID set.
DEFECT: write and read contracts disagree about a deliberately persisted piece of notification state. Every non-empty missed-timer set is serialized with Python repr rather than structured JSON and becomes unusable after restart/profile reload. Consequently an unacknowledged enabled one-shot timer can lose its persisted missed-attention state exactly across the restart/crash boundary the field is intended to survive.
REPAIR: register `missed_timer_ids` as a structured list in both `_JSON_SETTINGS` and `_STRUCTURED_CODECS`, with `legacy_ast=True` so already-written `"['id']"` rows can be recovered and rewritten canonically. Add key-specific member normalization that retains only non-empty strings. Keep the codec registry/write-side set invariant intact rather than adding another one-off parser in main.py.
VERIFY: save `{"timer-123"}` through the real state save path, inspect SQLite and require canonical JSON `["timer-123"]`, reopen and require a list, then run `_load_missed_ids` with a matching enabled one-shot timer and require the ID survives. Verify malformed JSON and valid JSON of the wrong top-level type fall back to `[]`; mixed members retain only non-empty strings; a legacy single-quoted `"['timer-123']"` row recovers via literal parsing and the next clean save rewrites canonical JSON.

[P2] [W2-003] src/fastprompter/core/state.py::_normalize_per_category_store / _decode_structured_setting; src/fastprompter/core/project_sync.py::free_slots; src/fastprompter/main.py::_rescan_project_sync
EVIDENCE: `project_sync_map_all` is declared only as a per-category `dict` at state.py:323. Its normalizer at 358-395 validates mapping VALUES as safe relative paths at 381-393 but never validates or canonicalizes mapping KEYS. Flat `project_sync_map` likewise has a structured dict codec but no slot-key normalizer. Real codec replays therefore preserved `{'not-a-slot':'a.md','0':'b.md'}`, `{'-1':'a.md','100':'b.md','1':'c.md'}`, and simultaneous aliases `{'01':'a.md','1':'b.md'}`. `project_sync.free_slots` immediately evaluates `{int(k) for k in mapping}` at project_sync.py:333-349; the first malformed case reproduced `ValueError: invalid literal for int() with base 10: 'not-a-slot'`. `_rescan_project_sync` calls this function at main.py:6563-6566 inside one broad `try`, whose catch at 6586-6588 merely debug-logs and aborts the rescan. It also computes `free_count = len(presets) - len(mapping)` at 6530-6536 before canonicalization, so out-of-range or duplicate logical aliases can distort capacity even when every key happens to parse.
DEFECT: persisted sync-map values are treated as security/correctness-sensitive filesystem data, but their slot identities cross the recovery boundary unvalidated. A single non-integer key can terminate the entire rescan and prevent otherwise valid new files from binding; negative/out-of-range keys and aliases such as `"01"`/`"1"` remain competing representations of one logical slot and can corrupt capacity/allocation decisions.
REPAIR: establish one slot-map key normalizer used for both `project_sync_map` and each category member of `project_sync_map_all`: accept only keys representing logical integer slots 0..99, convert them to one canonical string form, deterministically resolve/drop duplicate aliases, and quarantine malformed/out-of-range entries with a warning while retaining safe independent entries. Keep `free_slots` defensive as a second boundary so malformed in-memory/undo data cannot raise even if it bypasses persistence decoding.
VERIFY: codec tests for `not-a-slot`, -1, 100, `"01"` plus `"1"`, mixed valid/invalid entries and wrong member types must leave only canonical unique 0..99 keys. `free_slots` must never raise on malformed mappings and must allocate consistently from the sanitized logical claims. Drive `_rescan_project_sync` with a persisted malformed key plus a new eligible file and prove the invalid entry cannot abort the rescan or prevent the valid file from receiving a legal binding.

[P2] [W2-004] src/fastprompter/main.py initial startup silo normalization vs _normalise_int_keys profile-switch normalization
EVIDENCE: initial startup normalizes `silo_last_edited_all` at main.py:1007-1018 with one category-wide dict comprehension inside a single try/except; any malformed key or value causes `norm[c] = {}`, discarding every valid recency record in that category. It does the same for `silo_children_all` at 1038-1049: one malformed parent key, malformed child collection or one non-integer child causes the entire category hierarchy to become `{}`. The structured codec only validates that each category value itself is a dict (state.py:303-325, 358-395), so malformed inner members can reach this startup path. A replay using the live codec with `{"Code":{"0":[1,2],"bad":[3],"4":[5,"badchild",6]}}` decoded the nested mapping unchanged; executing the exact startup normalization expression yielded `{'Code': {}}`. In contrast, profile-switch code explicitly calls `_normalise_int_keys("silo_last_edited_all")` and `_normalise_int_keys("silo_children_all")` at main.py:9489-9493. That shared helper at 15894-15940 converts each parent key and each hierarchy child independently, skipping only the malformed member while retaining valid siblings.
DEFECT: startup and profile switch duplicate the same normalization responsibility with materially different error semantics. A partially malformed persisted category is tolerated member-by-member after a profile switch but catastrophically flattened during process startup. This turns one corrupt member into loss of unrelated valid hierarchy/recency state, and subsequent normal saves can make the in-memory loss durable.
REPAIR: remove the category-wide boot comprehensions as a competing implementation. Reuse one pure canonical normalizer for both initial boot and profile switch, preserving every independently valid key/value/child and dropping only the malformed member. Maintain aliasing requirements by mutating the existing nested maps in place where needed; do not introduce a third normalization path.
VERIFY: persist `silo_last_edited_all={"Code":{"0":"10","bad":"20","3":"30"}}` and `silo_children_all={"Code":{"0":[1,2],"bad":[3],"4":[5,"badchild",6]}}`. Initial startup and profile switch must produce identical normalized results: recency `{0:10,3:30}` and hierarchy `{0:[1,2],4:[5,6]}` with only malformed members removed. Save/reopen that state and prove valid survivors remain. Add a parity test asserting boot and profile-switch normalization use the same helper/result so the two lifecycle boundaries cannot drift again.

SECOND_WAVE_DONE_WHEN: v0->v1 migration cannot commit/drop legacy state before all loader-required row invariants pass; missed_timer_ids round-trips through SQLite as a structured list including legacy-row recovery; project-sync slot-map keys are canonical bounded identities and malformed state cannot abort rescan/allocation; startup and profile switch share member-isolating int-key/hierarchy normalization; focused migration/codec/project-sync regressions pass; prior CORE-006 restore failure is repaired under its original ticket; and GUI-dependent parity checks are run successfully in an environment providing PyQt6 plus the project's declared runtime dependencies.

---
## 03 — AUDIT PERFORMANCE
PROJECT_NAME: FastPrompter
DATE_TIME: 2026-08-29T03:11:17+03:00
CAMPAIGN_PROFILE: quick3
CAMPAIGN_PROFILE_VERSION: 1.0.0
CAMPAIGN_RUN_ID: UNSPECIFIED (ACB_CHAIN_RECEIPT: performance-mtdm0cmu-d462b876051d)
CAMPAIGN_MANIFEST_SHA256: c01ec812cf5952fdab101d5e7bc83c8251e0d9c21bb0ab06e426f5d58baedff2
WAVE_ID: performance
WAVE_INDEX: 3
WAVE_COUNT: 3
WAVE: AUDIT PERFORMANCE / STABILITY / EFFECTIVENESS
TARGET: supplied FastPrompter_28.08.26-T07-33-57(4).zip, fully extracted and audited as FastPrompter 0.8.60
BASELINE: FastPrompter_28.08.26-T07-33-57(4).zip sha256:a8acbfd47ce6fd89f30be8706499aacd2552323d41ea553f6e4de50283d886ab
PREVIOUS_WAVE_SHA256: NONE
GIT_CONTEXT: PRESENT main@3773172a3775c7ba14c42d7db52601d71160f279 - supplied archive contains 564 porcelain-status entries; live supplied files including uncommitted state were treated as authoritative
SAIPEN_CONTEXT: STALE - .saipen/STATE.md reports DONE/empty while .saipen/BOARD.md still carries T-1121..T-1138 TODOs; live source was checked independently and several older performance repairs are already present, so only remaining verified gaps are ticketed
AUDIT_SCOPE: Sync-Project discovery/filtering and watcher dispatch; editor extraction/save/cache paths; silo block-metadata restoration; repeating-timer catch-up; interval-rule scheduler/top-bar hot paths; ProductivityTimer elapsed catch-up; File Container result-cache and thumbnail/resource bounds; existing performance regression tests and pure core suites
TEST_STATUS: TEST_PARTIAL
TEST_LIMITATION: PyQt6 is unavailable in the sandbox. `PYTHONPATH=src pytest -q tests/test_perf001_noop_save.py tests/test_w2_005_interval_top_bar.py` failed during collection with `ModuleNotFoundError: No module named 'PyQt6'`, preventing live Qt event-loop/save/render performance verification.
VERIFIED_INSTEAD: `PYTHONPATH=src pytest -q tests/test_state.py tests/test_project_sync.py tests/test_timers.py tests/test_pomodoro.py` => 198 passed in 1.00s; all 280 Python files under src/tests/tests_smoke passed byte-level compile() syntax verification; exact pure-code benchmarks reproduced 100k-entry Sync discovery CPU cost, linear stale-timer catch-up, quadratic block membership, quadratic interval writeback, ProductivityTimer 1000-phase truncation, and byte-unbounded cache-key retention
STATUS: PERFORMANCE: COMPLETE
TICKETS: 7
HANDOFF: IMPLEMENTATION_AGENT
COVERAGE_INSPECTED: src/fastprompter/main.py::_apply_external_sync/_on_sync_dir_changed/_rescan_project_sync/save_data_to_db/_flush_live_editor/_update_active_silo_ui/_editor_text_snapshot/restore_silo_state/_restore_centered_blocks/_restore_aligned_blocks/_interval_notifs/_check_interval_notifs/_interval_top_bar_candidate/_check_timers/_tick_productivity; src/fastprompter/core/project_sync.py::is_text_file/match_exclude/scan_folder/write_text_file; src/fastprompter/core/timers.py::Timer.advance/collect_due; src/fastprompter/core/pomodoro.py::ProductivityTimer.tick; src/fastprompter/ui/editor.py::collect_view_metadata; src/fastprompter/ui/file_container.py::_dir_size/folder_summary/_purge_cache/thumbnail LRU/backlog; relevant state/project-sync/timer/pomodoro/performance regression tests
COVERAGE_DEFERRED: real PyQt GUI-thread frame/latency measurements, QTextDocument/QListWidget native costs, Windows QFileSystemWatcher event multiplicity, NTFS/HDD traversal timings, and live thumbnail decode pressure could not be measured because PyQt6/native GUI facilities are absent; static/resource paths were still inspected
CROSS_WAVE_REFERENCES: CORE-001/002/003/005/006 backup publication/drain/restore faults and W2-001..004 migration/codec/sync-key/startup-normalization faults remain separate and were not duplicated here. Existing prior performance repairs were confirmed in live code: hide uses durable dirty-scoped save, `_rescan_project_sync` has bounded retained discovery, editor snapshots are revision-aware, live-line cache is coverage-aware, recursive watcher arming runs asynchronously, hidden date-label visual work is gated, thumbnail backlog/LRU cardinality is capped, and file-result cache cardinalities are capped.
RESIDUAL_UNCERTAINTY: synthetic timings are machine-specific but expose algorithmic scaling independent of disk latency; real project-tree scans can only be slower once filesystem I/O is added. PERF-002's duplicate Qt extraction is statically proven by the live call chain but could not be dynamically counted without PyQt6. PERF-007's memory figures are structural retained-object estimates rather than an observed production RSS peak.

[P1] [PERF-001] PROVEN BOTTLENECK src/fastprompter/main.py::_apply_external_sync + src/fastprompter/core/project_sync.py::scan_folder/is_text_file
EVIDENCE: `_on_sync_dir_changed` sets `_sync_dir_changed=True` and starts the same 350ms main-window QTimer used for external sync at main.py:6177-6181; that QTimer is constructed as `QTimer(self)` and connected directly to `_apply_external_sync` at 1202-1205, so its callback executes on the GUI thread. For every directory-structure event `_apply_external_sync` calls `ps.scan_folder(...)` at 6321-6325 with NO `limit`, despite `_rescan_project_sync` already having a capacity-aware bounded call at 6520+ using the free-slot count. `scan_folder` still walks every directory/file at project_sync.py:214-229 even when a retained-result limit is supplied; `is_text_file` first evaluates the full exclusion matcher at 144 before making the cheaper extension/include decision at 146-166. DEFAULT_EXCLUDE contains dozens of component/wildcard patterns and DEFAULT_INCLUDE dozens of tokens. An exact benchmark of the live `scan_folder` with a synthetic 100,000-entry directory containing only irrelevant `.bin` names and no physical disk reads took 3.298s with `limit=None` and 3.242s even with `limit=5`; the retained-result bound therefore does not bound discovery CPU. Instrumenting the live `is_text_file` confirmed 10,000 irrelevant `.bin` paths still invoked `match_exclude` 10,000 times. `scan_folder(limit=0)` also returns the full result because `keep == 0` is used for both `None` and zero; a two-file replay returned `['a.txt', 'b.txt']` for limit zero. Atomic Sync writes use temp+`os.replace` at project_sync.py:303-323, so ordinary filesystem activity can itself produce parent-directory change notifications and repeatedly enter this path.
ISSUE: directory discovery remains a complete synchronous tree walk with expensive per-entry filtering on the GUI thread. The earlier bounded-heap repair only reduces retained-result memory in `_rescan_project_sync`; it does not protect the live directory-event path, does not avoid work when there are zero available slots, and does not reduce the dominant irrelevant-file filter cost. Large repositories can therefore freeze the UI for seconds on a routine directory event.
OPTIMIZE: separate mapped-file reconciliation from new-file discovery. Compute actual free binding capacity first; when capacity is zero, perform mapped-file existence handling but skip new-file discovery entirely. Otherwise capture immutable root/include/exclude/max-size/recursive/free-K plus a discovery generation and run `scan_folder(..., limit=K)` on a dedicated worker with one physical scan in flight and at most one newest pending request. Apply only a result whose generation/root/profile/category are still current. Inside `scan_folder`, distinguish `limit=None` from `limit=0`, precompute include extension/special-basename sets once, reject trivially non-included files before evaluating expensive exclusion wildcards, while retaining directory-pruning excludes before descent.
GUARDRAIL: preserve file-only batches performing zero discovery scans; preserve mapped-file deletion/invalidation semantics; preserve the lexicographically smallest K eligible paths exactly (the live bounded implementation was independently checked: limit 7 equalled the first seven entries of the unlimited sorted result); preserve special basename includes, exclude semantics, max_bytes checks, containment, and 100-slot identity rules; stale worker results must never bind files after root/profile/category change.
VERIFY: add a 20k+ mixed-tree test proving zero-capacity directory events do not invoke discovery; K-capacity discovery returns exactly `legacy_unbounded_result[:K]`; irrelevant extensions do not enter the file-level exclude matcher; `limit=0` returns `[]`; rapid directory-event generations result in only first+newest physical scans and only newest publication. In a Qt-capable run, block the discovery worker over a large synthetic tree and assert the GUI heartbeat/event loop remains responsive throughout.

[P1] [PERF-002] STRONGLY EVIDENCED WASTE src/fastprompter/main.py::save_data_to_db/_flush_live_editor/_update_active_silo_ui
EVIDENCE: save_data_to_db explicitly claims one whole-document extraction at main.py:4811-4815, but on a fresh edit with `_last_cached_text is None` it calls `self.text_area.toPlainText()` at 4818, then passes that string to `_flush_live_editor(current_text)` at 4844. When silo text actually changed, `_flush_live_editor` calls `_update_active_silo_ui()` with NO `raw` argument at 13014-13017. `_update_active_silo_ui` therefore falls through to another `self.text_area.toPlainText()` at 4640-4641. An AST call-chain check over the live source confirmed exactly: save -> `_flush_live_editor` with one argument at line 4844; flush -> `_update_active_silo_ui` with zero arguments at line 13017; UI fallback -> `toPlainText` at line 4641. Every text edit invalidates `_last_cached_text` at 17580-17587, while the normal editor cache debounce is 800-2500ms at 17609-17620; `hide_and_save` invokes save synchronously at 17708-17714, so typing followed quickly by hiding/focus-loss is a normal route through the duplicate extraction.
ISSUE: the save path has two competing document-read routes. A fresh edited document can be materialized twice on the GUI thread during the same authoritative save, doubling O(document) copying and allocation precisely on latency-sensitive hide/manual/finalize actions. The revision-keyed `_editor_text_snapshot` infrastructure already exists but this path bypasses it.
OPTIMIZE: make the authoritative save acquire its uncached text through `_editor_text_snapshot()` rather than a raw `toPlainText()` call, then propagate that exact immutable `current_text` through `_flush_live_editor`. Change the changed-silo branch to `_update_active_silo_ui(raw=current_text)`. Do not re-extract the document anywhere downstream for fingerprinting, persistence, label/line-count rendering, Sync push, or immediate same-revision consumers.
GUARDRAIL: retain `_on_text_changed` revision/cache invalidation so a save can never use pre-edit text; `_last_cached_text` may still be used only when its existing invalidation contract proves it current; preserve snippet-mode and archive-mode ownership behavior; UI label, line count, hash-title detection and active-silo coloring must remain byte-for-byte based on the exact saved string.
VERIFY: under PyQt, instrument `QPlainTextEdit.toPlainText`: fresh edit followed immediately by `save_data_to_db` must call it exactly once; another same-revision consumer/save must require zero new whole-document extractions where the cached snapshot is valid; edit again then save must add exactly one. Assert persisted SQLite text, active silo button title/line count, capture_silo_state fingerprint and Sync payload all equal that single captured snapshot.

[P2] [PERF-003] PROVEN BOTTLENECK src/fastprompter/main.py::restore_silo_state/_restore_centered_blocks
EVIDENCE: folded view-state is persisted as a list. On every silo restoration, `restore_silo_state` walks every QTextBlock and evaluates `b.text().strip() in folded` at main.py:2077-2087. `_restore_centered_blocks`, called during every ordinary silo switch at 15020-15023, independently walks every block and evaluates `block.text() in centered` where `centered` is a JSON list at 11384-11404. `_restore_aligned_blocks` immediately beside it already uses a dict lookup (`store.get(block.text())` at 11349-11350), demonstrating the intended constant-time membership pattern. A pure Python replay of the same list-membership shape measured 10,000 blocks against 10,000 saved strings at 0.379804s versus 0.000417s after one `set()` conversion, roughly 900x in that run; 5,000x5,000 took 0.096s versus 0.000151s. Actual Qt block traversal/format mutation adds cost on top.
ISSUE: restoration performs O(B×M) string comparisons where B is document blocks and M is saved folded/centered metadata. Large structured prompts therefore pay quadratic CPU on silo switches/reloads. Two separate restoration paths repeat the same collection-shape mistake.
OPTIMIZE: keep the persisted JSON/list formats for compatibility, but convert valid folded and centered entries to sets once at the start of each restoration and use set membership while walking blocks. If normalization is centralized, expose a small helper producing the membership set without changing stored ordering or serialization.
GUARDRAIL: duplicate block text semantics must remain unchanged: every document block whose text matches a saved centered/folded anchor must still receive the operation; preserve `.strip()` specifically for folded anchors and exact text matching for centered blocks; preserve FOLD_BIT checks and existing alignment/fold formatting behavior.
VERIFY: functional regression with duplicate block texts must produce identical folded/centered blocks before and after optimization. Add a large synthetic 10k-block/10k-metadata performance test instrumenting membership/lookup count and require linear-scale lookups rather than B×M comparisons. Run real silo switching under Qt and confirm restored metadata is exact.

[P2] [PERF-004] PROVEN BOTTLENECK src/fastprompter/core/timers.py::Timer.advance / collect_due
EVIDENCE: fixed-period interval/daily/weekly repeats build a timedelta at timers.py:421-426 and then repeatedly execute `while self.target <= now: self.target += step` at 434-435. `collect_due` invokes this synchronously for every due timer at 630-638, and `_check_timers` calls `collect_due` from the GUI's 1Hz timer at main.py:2741-2760. The code's own contract says a stale repeating timer should jump to the future instead of replaying every missed window, yet it still iterates once per missed recurrence to calculate that jump. Exact live-method timing for a 1-minute interval stale from 2020 was 0.0314s after one year, 0.1616s after five years and 0.3191s after ten years for ONE timer; multiple stale timers accumulate linearly on the GUI tick. Existing `test_interval_never_lands_in_the_past_after_a_long_sleep` covers only a 5-hour period stale by two days, so it protects result correctness but not asymptotic behavior.
ISSUE: fixed-period recurrence catch-up is O(number of missed intervals), even though the only required result is the first target strictly after `now`. Dormant profiles, restored old databases, clock jumps, or many short-period timers can turn the scheduler's one-second GUI callback into a visible stall.
OPTIMIZE: for REPEAT_INTERVAL, REPEAT_DAILY and REPEAT_WEEKLY, when `target <= now`, calculate the number of missed fixed steps arithmetically: `steps = floor((now - target) / step) + 1`, then advance once by `step * steps`. Leave monthly/yearly anchor-aware routines separate because calendar clamping is not a fixed timedelta recurrence.
GUARDRAIL: result must always be strictly greater than `now`; a target already in the future must remain unchanged; recurrence must remain on its original interval/grid; `fired=False` semantics stay identical; collect_due must still return each due Timer only once and must not synthesize notifications for missed occurrences; monthly/yearly anchor and Feb-29 behavior must remain untouched.
VERIFY: compare arithmetic and legacy-loop results across randomized moderate interval/daily/weekly fixtures including exact-boundary `target == now`; add 1-minute timers stale by 1, 10 and 100 years and verify runtime does not scale with missed recurrence count; run the existing timer suite unchanged and retain the long-sleep grid assertions.

[P2] [PERF-005] PROVEN BOTTLENECK src/fastprompter/main.py::_interval_notifs/_check_interval_notifs/_interval_top_bar_candidate
EVIDENCE: the application has a permanent 1Hz `date_timer` connected to both `_update_date_label` and `_check_timers` at main.py:1177-1180. `_check_timers` invokes `_check_interval_notifs` every second even while the main window is hidden at 2741-2754. When visible, `_update_date_label` additionally calls `_update_timer_label` at 1334-1335, whose interval candidate calls `_interval_notifs` at 1617-1623, so the canonicalizer can run twice per visible second. `_interval_notifs` traverses every rule and `_heal_interval_rule` begins from `dict(rule)`, producing fresh dict copies even for already-canonical rules at 2588-2619. On a dirty scheduler tick, because those are copies, `_check_interval_notifs` writes timestamps back by nested loops over every healed rule and every raw rule at 2697-2714: O(N²). It also sorts `candidates` by the enumeration index at 2682-2685 even though candidates were appended in that same order. TimerDialog `_interval_new` simply appends at timer_dialog.py:3035-3055 and establishes no count bound. Running the exact `_heal_interval_rule`, `_interval_notifs` and `_check_interval_notifs` function bodies extracted from the live source produced: canonical no-fire heal 5,000 rules = 0.0081s; dirty tick with 1,000 rules = 0.0238s, 3,000 = 0.1878s, 5,000 = 0.6335s. Only one notification fired in each dirty benchmark; the growth comes from housekeeping.
ISSUE: scheduler state is copied O(N) every second and reconciliation becomes O(N²) exactly when interval boundaries fire. The same canonicalization responsibility is paid repeatedly rather than at load/mutation boundaries, and a dirty tick can occupy more than half of the application's one-second GUI cadence for sufficiently large imported/user-created rule sets.
OPTIMIZE: maintain one canonical rule list rather than returning detached copies on every read. Heal/canonicalize when interval data is loaded, replaced, imported, or edited; already-canonical reads should return the live canonical dicts without allocation. If detached healing must remain, build one `id -> original` map and write back in O(N), never nested-search. Eliminate the redundant candidate sort because enumeration order already defines topmost priority. Optionally pass a single canonical rule snapshot through scheduler/top-bar work occurring in the same tick rather than healing twice.
GUARDRAIL: preserve malformed-field healing; duplicate IDs still collapse with first occurrence winning; persisted canonicalization must still mark settings dirty when actual healing occurs; topmost rule priority and lower-priority collision suppression must remain identical; elapsed-mode first-sample initialization and clock-mode `last_fired_minute` deduplication must remain durable.
VERIFY: add N=1k/5k/10k scheduler instrumentation and require dirty and non-dirty work to scale linearly; count `_heal_interval_rule`/dict-copy calls during ten unchanged seconds and require no repeated canonical-copy churn; verify simultaneous clock candidates still fire only the first and advance suppression timestamps on lower candidates; top-bar countdown and hidden-window firing behavior must remain unchanged.

[P2] [PERF-006] PROVEN BOTTLENECK src/fastprompter/core/pomodoro.py::ProductivityTimer.tick + src/fastprompter/main.py::_tick_productivity
EVIDENCE: `ProductivityTimer.tick` promises real elapsed-time correctness and explicitly says a long stall may cross multiple phases at pomodoro.py:161-167, but its catch-up loop is hard-limited to 1000 iterations at 178-190 with no handling of residual elapsed time after iteration 1000. An exact replay with work=1s, break=1s and `tick(2500)` returned 1,000 ended phases, recorded only 500 completed work cycles, and left the timer at a fresh work phase; the mathematically correct completed cycle count is 1,250. With the shipped defaults (45m30s work + 15m30s break), a 30-day elapsed tick processes exactly 1,000 phases = 500 cycles = 1,830,000 seconds = 21.1806 days, silently discarding the remaining 8.8194 days. `_tick_productivity` receives every returned phase and calls `_notify_productivity` once per item at main.py:2193-2199, so the same long catch-up can also trigger up to 1,000 sound/tray notification calls in a single GUI tick. Existing tests cover a 150-second three-phase stall and exact remainder but do not cross the 1000-phase guard.
ISSUE: the loop guard converts a performance defense into silent clock-state corruption. Before the cap it also makes catch-up cost and notification count proportional to the number of missed phases. A sufficiently long suspend/stall therefore both hammers the UI with historical phase notifications and leaves the timer days behind real elapsed time.
OPTIMIZE: consume the current partial phase normally, then fast-forward whole work+break cycles arithmetically when breaks are enabled, incrementing `completed_cycles` for every mathematically completed work phase, and process only the final residual phase(s) iteratively. Bound UI event replay separately from state advancement: ordinary short stalls must retain the existing exact ended-phase list, while very large catch-up must update state/alarm_phase exactly without generating an unbounded historical notification stream; emit at most a compact/latest catch-up notification for the skipped bulk. For breaks-disabled mode preserve the existing stop-after-work behavior rather than cycling.
GUARDRAIL: all current short-stall tests must remain byte-for-byte valid, including `tick(150) == [work, break, work]`; paused/idle/non-positive elapsed values remain no-ops; `completed_cycles`, phase and remaining must be exact after arbitrary elapsed time; alarm_pending/alarm_phase must describe the most recent completed phase; repeat_alarm=False semantics and break-disabled pause semantics must not change.
VERIFY: add elapsed values crossing 999, 1000, 1001 and 100,000 phases and compare final phase/remaining/completed_cycles against a mathematical oracle; the 30-day default replay must account for all 30 days, not 21.18; a 1s/1s `tick(2500)` must yield completed_cycles=1250 with exact residual state; instrument `_notify_productivity` and prove huge catch-up cannot dispatch O(number-of-missed-phases) tray/sound calls.

[P2] [PERF-007] STRONGLY EVIDENCED WASTE src/fastprompter/ui/file_container.py::_dir_size/folder_summary cache keys
EVIDENCE: `_dir_size` advertises a 2,000-file traversal cap at file_container.py:356-360, but before the capped walk it executes an unbounded `sorted(os.listdir(path))` and stores the ENTIRE resulting filename tuple inside the cache key at 363-368. `_dir_size_cache` is capped only by entry count (256) at 387-391. `folder_summary` independently stores `tuple(sorted(names))` in each `_folder_summary_cache` key at 416-423; that cache is capped at 64 entries at 456-479. Thus the cardinality caps do not bound retained bytes. A Python retained-object sizing replay using representative 15-character filenames measured a 100,000-name tuple plus its strings at approximately 6.1 MiB; 64 such keys are approximately 390.6 MiB and 256 approximately 1,562.5 MiB before cache values/dict overhead. Even 10,000 names retain ~0.61 MiB per signature. Old signatures remain until the cache exceeds its entry threshold; being expired does not remove them while under the threshold. `folder_summary` is dispatched on QThreadPool workers from main.py:4315-4344, so this is primarily long-session RSS/resource pressure rather than a GUI-thread freeze.
ISSUE: the recent hard-cardinality cache repair bounds dictionary length but not cache memory. Filename inventories are embedded directly into keys, so a handful of very large folders or repeated changed signatures can retain hundreds of megabytes of strings; `_dir_size` also pays a full unbounded directory listing/sort merely to decide whether its deliberately capped 2,000-file result can be reused.
OPTIMIZE: replace retained filename tuples with a fixed-size directory signature. Keep canonical path as the identity and store a compact direct-listing generation such as directory stat metadata plus TTL, or a fixed-size digest if exact add/remove signature semantics are required. `folder_summary` may still enumerate names when recomputing the actual summary, but the cache key/value must not retain those names after computation. `_dir_size` should be able to validate a hot cache entry without listing/sorting an arbitrarily large directory merely to construct the key.
GUARDRAIL: preserve the current 2-second staleness bound; direct add/remove changes should invalidate promptly where the filesystem exposes a directory generation/mtime; nested changes may retain the already-documented TTL behavior; keep existing 64/256 entry caps and cancellation of the capped recursive size walk; no cache optimization may make folder summary correctness security-sensitive.
VERIFY: add a synthetic large-directory test producing 100k names and assert cache keys retain O(1) signature bytes rather than O(filename count); insert 64/256 large-folder signatures and verify retained key payload stays bounded independently of total filenames; preserve current cache-hit and expiry tests, then mutate direct entries and confirm a fresh summary appears within the documented invalidation/TTL contract.

PERFORMANCE_DONE_WHEN: Sync-Project directory discovery is capacity-aware, latest-only and off the GUI thread with cheap candidate filtering and correct zero-limit semantics; a fresh edited-document save performs one whole-document extraction and forwards that snapshot through UI/persistence; folded/centered restoration uses linear-time membership; fixed-period timers jump stale intervals arithmetically; interval-rule scheduling/canonicalization/writeback is O(N) without per-second copy churn; ProductivityTimer accounts for arbitrary elapsed time without a 1000-phase correctness cliff or notification storm; File Container cache keys retain fixed-size signatures rather than filename inventories; all pure regression suites remain green and the corresponding Qt event-loop/GUI latency tests pass in an environment with PyQt6.

---
ALL_3_DONE_WHEN: 3/3 waves validated and combined into canonical handoff package.