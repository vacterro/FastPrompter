# ASP Board

## Current BLOCKED (depends on T-569 green suite)
| ID | Status | Description |
|---|---|---|
| T-566 | BLOCKED (T-569) | Delete `toggle_header_line()` — dead code (only tests call it). Changes fuzz test RNG sequence, header tests fail downstream. |
| T-568 | BLOCKED (T-569) | Bug: icons squashed/vanish on theme change. Root: `_apply_header_density` applies only when tier FLIPS, not by current width. Fix attempted + reverted (dense/ultra lists overlap, `apply_button_size()` resets font inflating 960→1047). Needs stable suite first. |
| T-569 | P0 BLOCKER | `tests_smoke` RED at HEAD (5/190 failed, clean tree). Passes differ by run (195 vs 190) — segfault at TEARDOWN after 100% progress. Same `setExtraSelections` re-entrancy family. All other BLOCKED tickets wait on this. |
| T-570 | P1 (depends T-569) | Segfault at suite TEARDOWN (Qt crash, no Python traceback). 100% progress reached, final count sometimes unprinted. Fix teardown crash before trusting pass counts. |

## Current TODO — bugs & gaps (by severity)
| ID | Sev | Description |
|---|---|---|
| T-555 | P1 | Bug (user): document jumps to top on click. NOT reproduced. Instrumented: `mousePressEvent` now logs full traceback, `_watch_scroll_reset` logs call stack on scroll slam >200→0. Next occurrence leaves evidence in `%TEMP%/fastprompter.log`. |
| T-562 | P1 | Review aa70c09 (third-party uncommitted work: Ctrl+E centering, text-align combo, FlowLayout justification, Tab focus, page buttons). Three findings already fixed (T-563..T-565). Deeper review: the FlowLayout justification and text-align combo may have edge cases beyond what was caught. |
| T-200 | P2 | `editor.py` `_checkbox_at_pos()` catches all `Exception` — blocks event propagation, buggy coordinate math silently skips checkboxes. |
| T-201 | P2 | `editor.py` `mousePressEvent` Ctrl+bullet calls `beginEditBlock` but NOT `endEditBlock` on early Ctrl+None match. |
| T-202 | P2 | `editor.py` `keyPressEvent` QKeySequence comparison may fail on non-US keyboard layouts. |
| T-203 | P2 | `editor.py` `paintEvent` creates QPainter per frame + iterates all blocks (checkboxes, folds, code-copy, ts-glyphs, rules, zebra) — heavy per-frame work. |
| T-204 | P2 | `search_mixin.py` `find_text()` wrap-around uses `setTextCursor` triggering `cursorPositionChanged` side-effects. |
| T-205 | P2 | `search_mixin.py` `replace_text()` compares `selectedText()` (\u2029) with `search_input.text()` (\n) — fails on multi-line selections. |
| T-206 | P2 | `formatting_mixin.py` `clear_formatting()` calls `cache_current_text()` re-reading entire document — stalls UI on large docs. |
| T-207 | P2 | `formatting_mixin.py` `insert_add_line()` calls `endEditBlock` without matching `beginEditBlock`. |
| T-208 | P2 | `formatting_mixin.py` `toggle_bullet_conversion()` missing `beginEditBlock`/`endEditBlock`. |
| T-210 | P2 | `editor.py` `wheelEvent` uses `angleDelta().y()` which returns 0 on some trackpads — Ctrl+wheel zoom silently fails. |
| T-211 | P2 | `pie_menu.py` QuickListWidget uses `pynput.keyboard.Listener` global hook — Esc works across apps while pie menu open. |
| T-212 | P2 | `editor.py` `_toggle_checkboxes()` separate QTextCursor per block inside beginEditBlock — per-block cursors bypass atomic undo grouping. |
| T-213 | P2 | `editor.py` `mousePressEvent` sets pressed-state flags without try/finally — exception leaves stale state. |
| T-216 | P2 | `formatting_mixin.py` `apply_format()` calculates start/end from whitespace-stripped selection, inserts text — final `setPosition` after `endEditBlock` references stale coordinates. |
| T-217 | P2 | `editor.py` `keyPressEvent` bare `except` swallows keyboard exceptions. |
| T-218 | P2 | `portable_backup.py` runs inside `state.py` DB write lock — slow file I/O blocks all DB writes up to 60s. |
| T-219 | P2 | `snippet_panel.py` `DraggableButton` `_dragging` flag never reset if drag fails silently. |
| T-220 | P2 | `editor.py` middle-click `clear_temp` without checking silo active — could clear wrong silo during tab switch race. |
| T-232 | P2 | `window_mixin.py` `toggle_visibility()` uses `self.isActiveWindow()` for frameless Tool windows — returns False on some Win32 configs. |
| T-238 | P2 | `theme_mixin.py` `apply_theme()` calls `_begin_batch_update` which accesses `self.left_panel.grab()` — if `left_panel` not yet created, AttributeError. |
| T-240 | P2 | `scaling_mixin.py` `_set_unified_scale()` forces `_refresh_settings_cache()` which is a no-op stub. |
| T-259 | P2 | `snippet_ops_mixin.py` `save_snippet_as_number()` double-calls `cancel_editing()`. |
| T-295 | P2 | `tests_smoke` shares ONE module-scoped `win` fixture across 100+ tests — state leaks, test order affects results. |
| T-299 | P2 | `editor.py` keyPressEvent hardcoded Ctrl+B/I/U at line intercept — user rebinding still sees hardcoded block fire. |
| T-307 | P2 | `state.py` `switch_profile()` closes old DB without `save_data_to_db()` — unsaved changes lost on profile switch. |
| T-311 | P2 | `backup_dialog.py` `export_silos()` exports only current tab — UI says "Export All Silos" but only one category saved. |
| T-400..T-408 | Wave i18n | All DONE — see archive. |
| T-405 | P4 | Slim `translations.py` `_DATA` — once pack RU absorbs 5 legacy-only keys, can shrink to just those + reverse-map. |
| T-349 | P3 | Smoke test `test_header_ultra_mode_fits_portrait_sliver` fails in full suite (passes isolation) — module-scoped `win` fixture pollution. |
| T-350 | RESOLVED | Orphan `.pyc` in `tools/i18n/__pycache__/` — .pyc gone, empty dir remains. |
| T-351 | RESOLVED | Orphan `.pyc` in `tools/__pycache__/` — .pyc gone, empty dir remains. |
| T-355 | P4 | Build artifacts in tree 426MB (was 28MB). `build/` contains EXE, intermediates. Clean up or .gitignore. |
| T-357 | P4 | Unsorted imports (ruff I001) across 10+ files. |
| T-359 | P5 | Ambiguous `l` variable in `drop_overlay.py` (ruff E741). |
| T-360 | P5 | Trailing whitespace in `toolbar_reorder.py`, `trash_dialog.py` (ruff W291). |
| T-361 | P5 | Unnecessary `'r'` mode in `trash_dialog.py` (ruff UP015). |

## Deep audit backlog (T-200..T-330) — P3-P5, triaged but unfixed
| ID | Sev | File | Description |
|---|---|---|---|
| T-214 | P3 | formatting_mixin.py | `insert_old_add_line()` calls `self.mark_dirty()` twice (duplicate). |
| T-215 | P3 | editor.py | `Alt+Backspace` catches ALL exceptions silently — PreviousWord could fail at doc start. |
| T-221 | P3 | sound_manager.py | `QSoundEffect` fallback: `_play_winsound()` has no volume control. |
| T-222 | P3 | sound_manager.py | `_players` dict grows unbounded — each `play()` creates QSoundEffect, never cleaned. |
| T-223 | P3 | help_dialog.py | HTML f-strings with `tr()` — hotkey strings containing `<>&` not escaped. |
| T-224 | P3 | backup_dialog.py | `export_silos()` reads ALL silo texts into memory — OOM on 100×500K each. |
| T-225 | P3 | formatting_mixin.py | `simple_markdown_to_html()` `html.escape()` BEFORE `markdown()` — double-escapes code blocks. |
| T-226 | P3 | tests/test_main.py | Uses `except Exception: pass` for fallback renderer — syntax error passes silently. |
| T-227 | P3 | tests/test_pie_menu.py | Mocks QCursor+QTimer globally — other tests get broken imports. |
| T-228 | P3 | config.py | `extract_border_color()` regex missing `\s*` before hex — fails on `border: 3px solid #abc`. |
| T-229 | P3 | textfit.py | `clip_safe_width()` hardcodes `pixel_size=11`, `bold=True` — wrong after scale changes. |
| T-230 | P3 | state.py | `init_db()` inline backup BEFORE migration — migration failure kills .bak. |
| T-231 | P3 | window_mixin.py | `place_window()` checks `cb_lock_cursor.isChecked()` before nullable import — AttributeError. |
| T-233 | P3 | window_mixin.py | `show_window()` starts 30s topmost timer — window can slip below others for 30s. |
| T-234 | P3 | window_mixin.py | `toggle_focus_mode()` saves full state but NOT `_pre_focus_sidebar_visible`. |
| T-235 | P3 | window_mixin.py | `toggle_sidebar_position()` re-inserts widgets into splitter — children get reparented. |
| T-236 | P3 | theme_mixin.py | `_get_custom_colors()` `str(raw)` on dict creates new cache key every call. |
| T-237 | P3 | theme_mixin.py | `change_preview_mode()` Source View sets `highlighter.setDocument(None)` — never re-attached. |
| T-239 | P3 | scaling_mixin.py | `apply_scaled_ui()` `findChildren(QPushButton)` includes ALL children — override snippet/file btn sizes. |
| T-241 | P3 | hotkey_mixin.py | `_apply_tooltips()` assigns `lang = getattr(...)` twice (lines 33 and 43). |
| T-242 | P3 | logging.py | `exception_hook()` uses `__import__('datetime')` — same pattern as T-163. |
| T-243 | P3 | hotkeys.py | `_resolve_vk()` could also raise `ValueError` on null result. |
| T-244 | P3 | drop_overlay.py | `zone_at()` binary: `editor_link` at bottom — user expects copy at top. |
| T-245 | P3 | drop_overlay.py | `zone_at()` text: right zones narrow on small viewports — buttons un-tappable. |
| T-246 | P3 | window_mixin.py | `enforce_topmost()` comment says 2s polling, actual timer is 30s. |
| T-247 | P3 | window_mixin.py | `toggle_sidebar_position()` no guard for `splitter` — crashes if called before `init_ui()`. |
| T-248 | P3 | window_mixin.py | `apply_sidebar_position()` uses `ast.literal_eval` — no validation result is `list[int, int]`. |
| T-249 | P3 | window_mixin.py | `set_lock_state()` saves `geometry()` — minimized returns 0,0,0,0 — locks to zero. |
| T-250 | P3 | theme_mixin.py | `apply_font()` misses try/except for `highlighter.update_base_size()`. |
| T-251 | P3 | markdown_highlighter.py | `highlightBlock()` re-compiles link regex inline — wasteful per-match. |
| T-252 | P3 | markdown_highlighter.py | Underline `__[^_\n]+__` excludes `_` inside — `__some_var__` won't match. |
| T-253 | P3 | markdown_highlighter.py | `_CODE_KEYWORDS` has lowercase+uppercase variants — lowercase are dead code. |
| T-254 | P3 | markdown_highlighter.py | Link anchor `setFormat` called twice per match — confuses Qt format cache. |
| T-255 | P3 | themes.py | `generate_custom_theme()` mutates caller's dict via `c.setdefault(k, v)`. |
| T-256 | P3 | themes.py | `Vintage Classic` has only 4 `preset_colors` vs 10 in all others. |
| T-257 | P3 | themes.py | `Dark 2 (OLED)`: both `bg_main` and `bg_text` are `#000000` — editor invisible. |
| T-258 | P3 | snippet_ops_mixin.py | `backup_silo_to_files()` re-imports `os` despite top-level import. |
| T-260 | P3 | snippet_ops_mixin.py | `del_silo()` uses `hasattr(self, "_remap_silo_indices")` — fragile duck-typing. |
| T-261 | P3 | snippet_ops_mixin.py | `convert_to_snippet()` doesn't call `cancel_editing()` — button text stays "Update". |
| T-262 | P3 | snippet_ops_mixin.py | `trash_silo()` imports `datetime` inside body — style inconsistency. |
| T-263 | P3 | help_dialog.py | Hotkey keys may not exist in `data` for older configs — no validation. |
| T-264 | P3 | backup_dialog.py | `backup_database()` opens connections without try/finally — leak on partial failure. |
| T-265 | P3 | backup_dialog.py | File dialogs lack `ignore_focus_loss` guards — could trigger auto-hide. |
| T-266 | P3 | resizers.py | `EdgeResizer` mouse events don't call `super()` — bypasses Qt event chain. |
| T-267 | P4 | tools/build.py | `build_with_nuitka()` calls `os.chdir(project_root)` — global side effect. |
| T-268 | P4 | tools/release.py | If GitHub API release fails, `rel` is None then `.get("assets")` raises AttributeError. |
| T-269 | P4 | tools/fix_remaining_bugs.py | Hardcodes `PROJECT` path to dev machine — won't run elsewhere. |
| T-270 | P4 | tools/fix_remaining_bugs.py | `fix_main_py()` replaces exact multiline string — likely mismatched, silent failure. |
| T-271 | P4 | settings.py | HotkeySettingsDialog `__init__` calls `unregister_all_hotkeys()` — crash mid-init leaves hotkeys off. |
| T-272 | P4 | settings.py | ColorConfigDialog defines 14-entry default dict 3 times (inline, fallback, reset) — maintenance hazard. |
| T-273 | P4 | settings.py | `ColorConfigDialog` imports `QColorDialog` at module bottom (after class body). |
| T-274 | P4 | settings.py | HotkeySettingsDialog imports QScrollArea+QTabWidget inside method body. |
| T-275 | P4 | settings.py | `reset_defaults()` guards `app_binds` with `getattr(self, ..., [])` — always set in `__init__`, dead code. |
| T-276 | P4 | file_container.py | `refresh()` imports `datetime` inside for-loop body. |
| T-277 | P4 | file_container.py | `build_template_folders()` — trailing comma creates `self.folder` via `exist_ok=True`; `..` escapes root. |
| T-278 | P4 | file_container.py | `_pick_import()` / `_pick_import_folder()` no `_modal_guard()`. |
| T-279 | P4 | file_container.py | `_export_all()` calls `getExistingDirectory` without `_modal_guard()`. |
| T-280 | P4 | file_container.py | `save_clipboard_as_file()` calls `QInputDialog.getText` without `_modal_guard()`. |
| T-281 | P4 | file_container.py | `open_for()` calls `os.makedirs(folder, exist_ok=True)` without error handling. |
| T-282 | P4 | tests/test_formatting_mixin.py | Patches `markdown.markdown` at module level — global side-effect across test modules. |
| T-283 | P4 | tests/test_search_mixin.py | Sets `sys.modules["PyQt6"] = MagicMock()` at module level. |
| T-284 | P4 | tests/test_pie_menu.py | Same global-leak as T-283. |
| T-285 | P4 | deploy.ps1 | Uses `git push --force-with-lease` after rebase conflict — could overwrite remote. |
| T-287 | P4 | translations.py | Both `'Always on Top'` (lower o) and `'Always On Top'` (upper O) — case inconsistency, one is dead. |
| T-288 | P4 | tests/test_main.py | Duplicates fallback renderer as `_fallback_markdown_to_html()` — diverges from real code. |
| T-290 | P4 | drop_overlay.py | Single-letter `l` variable (ruff E741) — confusable with `1`. |
| T-291 | RESOLVED | backup_dialog.py | Re-imports `os` and `datetime` inside methods — cleaned by H-04 sweep. |
| T-292 | P4 | file_container.py | `_FileList.startDrag()` accesses `currentItem().icon()` without null check — AttributeError. |
| T-293 | P4 | file_container.py | `_icon_for()` imports `QFileInfo` inside except handler — waste on every icon-less file. |
| T-294 | P4 | file_container.py | `import_paths()` doesn't recreate `self.folder` if deleted externally. |
| T-296 | P4 | settings.py | HotkeyWidget `keyPressEvent` uses `chr(key)` — NumLock-off sends different codes. |
| T-298 | P4 | editor.py | `_refresh_checkbox_flag()` 200-block scan — docs >200 blocks have checkboxes past 200 never rendered. |
| T-300 | P4 | editor.py | `paintEvent` accesses `self.main_win.data` without checking if `main_win` deleted — RuntimeError. |
| T-301 | P4 | editor.py | `line_number_area_mouse_press_event` iterates from `doc.begin()` instead of first visible block. |
| T-302 | P4 | editor.py | `_ts_glyph_rect()` creates QTextCursor per stamped line during paintEvent — per-frame overhead. |
| T-303 | P4 | ipc_server.py | `IpcServer.setup()` calls `sys.exit(0)` on listen failure with no error message. |
| T-304 | P4 | ipc_server.py | `_handle_command()` accepts plain `"SHOW"` — bypasses token auth. |
| T-305 | P4 | ipc_server.py | `_get_token()` writes token without file locking — two processes overwrite each other. |
| T-306 | P4 | state.py | `_save_data_to_db_locked()` runs backup INSIDE write lock — file I/O blocks all DB ops. |
| T-308 | P4 | state.py | `_export_md_backup()` uses `os.path.expanduser("~/.fastprompter")` — wrong in portable mode. |
| T-309 | P5 | CHANGELOG.md | Documents up to v0.7.0 (i18n wave). Missing: settings tabs, overflow menu, undo integrity, timer system, hashtags, cursors, compact picker, line heat, FancyZones, silo nesting, ~100 bug fixes. |
| T-312 | P4 | hotkey_mixin.py | `_register_single()` catches ALL exceptions silently — invalid hotkey gives no feedback. |
| T-313 | P4 | main.py | `change_profile()` rebuilds `silo_docs` only for active category — other tabs stale. |
| T-314 | P4 | theme_mixin.py | `apply_theme()` sets stylesheets without `sip.isdeleted` checks — RuntimeError on teardown. |
| T-315 | P4 | theme_mixin.py | `change_preview_mode()` calls `highlighter.setDocument(None)` without `sip.isdeleted`. |
| T-316 | P4 | backup_dialog.py | BackupDialog copies `styleSheet()` once — stale after theme switch. |
| T-317 | P4 | theme_mixin.py | `clear_custom_fonts()` never calls `QFontDatabase.removeApplicationFont()` — leaks font registration. |
| T-318 | P4 | theme_mixin.py | `load_custom_font()` stores font IDs in `data["custom_font_ids"]` — meaningless after restart. |
| T-319 | P4 | main.py | `closeEvent` calls `save_data_to_db(force=True)` without checking `self.state.conn` — silent data loss. |
| T-320 | P4 | main.py | `_apply_header_density()` calls `cat_combo.setStyleSheet("")` — resets themed combo style. |
| T-321 | P4 | main.py | `data_redo_stack` has no memory cap (like T-168) — grows unbounded. |
| T-322 | P4 | main.py | `resizeEvent` when locked silently discards resize — no visual feedback. |
| T-323 | P4 | main.py | `setup_single_instance_server()` called BEFORE `self.state = FastPrompterState()` — IPC/SHOW crash. |
| T-324 | P4 | main.py | `simulate_ctrl_v()` uses `GetAsyncKeyState` without sync — key state globally out of sync. |
| T-325 | P4 | main.py | `setup_exception_hook()` writes crash.log in append mode with no rotation. |
| T-326 | P4 | main.py | `quit_app()` sets `conn = None` BEFORE `QApplication.quit()` — closeEvent save_data silently lost. |
| T-327 | P4 | paths.py | `get_data_dir()` doesn't handle `os.makedirs()` failure — no AppData fallback. |
| T-328 | P4 | paths.py | `_detect_base_dir()` returns filesystem root when no anchor found — wrong paths. |
| T-329 | P4 | FastPrompter.pyw | Writes crash.log with `"w"` mode (overwrite) while main.py uses `"a"` (append) — pre-init crash overwrites. |
| T-330 | P4 | FastPrompter.pyw | Hardcodes `--product-version=0.6.0` — not synced with pyproject.toml. |
| T-331 | P4 | resizers.py | `self.target_rect` typed `QRect` but init `None` — `QRect(None)` crashes on synthesized move before press. |
| T-332 | P4 | ipc_server.py | `nextPendingConnection()` returns `QLocalSocket | None` — no None check, AttributeError. |
| T-333 | P4 | hotkey_filter.py | `nativeEventFilter` signature doesn't match parent — violates Liskov, breaks on PyQt6 upgrades. |
| T-334 | P4 | logging.py | `traceback.format_exception(exctype, value, tb)` — `tb` typed `object | None`, expects `TracebackType | None`. |
| T-335 | P4 | sound_manager.py | `QSoundEffect = None` conflicts with type annotation — mypy assignment error. |
| T-336 | P4 | window_mixin.py (systemic) | 93 mypy `attr-defined` errors — mixin pattern confuses type checker. |

## HUNT findings 21.07 — ctrl_e_center persistence (recorded, NOT fixed)
| ID | Sev | Description |
|---|---|---|
| H-310 | P4 | `centered_blocks` list grows unbounded — never pruned except on header revert. O(doc_blocks × list_length) scan. Cap at 1000. |
| H-311 | P4 | `centered_blocks` leaks stale entries — deleted headers stay in list. Zero functional harm but accumulates orphans. |
| H-312 | P5 | Toggling `ctrl_e_center` OFF does not clear `centered_blocks`. After OFF→save→reload, old headers re-centred. Needs user opinion. |
| H-313 | P5 | Root-level orphan scripts: `_fix_vi.py`, `_translate.py`, `tmp_vi_missing.txt`, `ruff_output.json`, `ruff_output.txt`, `ruff.json`. Move to `tools/` or delete. |
| H-314 | RESOLVED | `__pycache__` in project root: orphan .pyc gone, dir empty. |

## Needs human decision
| ID | Status | Description |
|---|---|---|
| C-001 | AWAIT HUMAN | `i18n_build_scripts/` (113 untracked one-off scripts) — zero refs from src/ or tests/. Either delete or move under `tools/i18n/`. |
| C-002 | AWAIT HUMAN | `.saipen_backup/` at repo root (untracked snapshot 18.07) — breaks RFC §1.7 (protocol files not in project). Confirm ownership then delete. |

## NOT STARTED — wishlist backlog (needs spec or separate pass)
- **Timer / "limit reset" system** — clickable clock → manager for per-platform reset timers, sound/volume/repeat presets, static or "temperature" colour. (Big one: new dialog, persistence, scheduler, notification plumbing.)
- **Toolbar dockable to all 4 sides** (settings-driven) — QHBoxLayout throughout; vertical docking = real layout rework.
- **Line temperature system** — recent-edit heat colouring per line/block. Default OFF. *(LH-01..04 done, this was already implemented — check if still pending)*
- **Silo drop onto a child re-parents into that hierarchy** — rather than requiring exact drop on the parent.
- **Italic/regular toggle next to the collapse button** — rather than global Settings toggle.
- **Per-silo selectable state** — cursor position? selection? scroll? all three? Needs a spec.

## ARCHIVE — all DONE waves and sweeps
### Wave i18n (19.07) — all DONE
T-400..T-408: i18n pack injection, proxy delegation, language selector, tests, FULL-UI retranslation, ded language, view combo bug, flags. Verified: 461+104 PASS.

### Wave v0.6.0 (17.07) — all DONE
T-130..T-139: Settings rethink, button moves, snippet arrows, folder template editor, bindable shortcuts, search pass, README refresh, backup+ship, global hotkeys fix.

### Wave v0.6.5 (17.07) — all DONE
T-140..T-156: tr() format-string bug, QPainter leaks (×2), untranslated QMessageBox (×5), dead code, timer race, tr() tooltips, topmost_timer, _apply_settings_language, bare excepts, WindowFlags opt, translation sweep (54 keys), parallel bug hunt (17 bugs), static analysis (43 bugs tagged).

### Wave v0.6.6 (17-18.07) — DONE
T-160..T-194, T-209, T-310: folder_summary crash, print() in prod, 26 tooltips, __import__ datetime, undo data race, backup conn leak, hide_and_save sync I/O, silo count estimate, undo no cap, bullet tooltip, thumb_cache, redundant getattr, lazy import, undo history per profile, _live_folder_sync double read, _update_files_button lazy import, change_profile hardcoded tab, folder_summary I/O per hover. (Remaining T-200..T-330 TODO — kept in Current TODO.)

### Wave v0.6.2 user request list (18.07) — DONE
T-200/201/202/203: boot crash, tick hover, line-number cycle, Alt+D local. T-210..T-218: moved to later waves.

### Static-analysis audit (18.07) — all WONTFIX/DONE
T-168/175/190/199 WONTFIX-PERF, T-177/178/180 WONTFIX-SMELL, T-181/183/187/195 WONTFIX-THEORY.

### HUNT sweep 18.07 — DONE
T-210 (verified), T-211 (FIXED 3 endEditBlock bugs + AST regression), T-212 (removed 40 noise tags). H-301..H-306 (all FIXED by antigravity + opus: undo corrupt, undo bloat, del_category leak, archive collision, trash-log restart, archive paths).

### Recent Ad-Hoc Fixes (18.07) — all DONE
F-001: Ctrl+E regex fix + legacy migration. F-002: refresh/fold button overlap. F-003: sidebar buttons to top toolbar.

### T-340..T-348 (18-19.07) — all DONE
Sidebar buttons to header, color box insert fix (T-341), checkbox strikethrough (T-342), Ctrl+Shift+C shortcut (T-343), hotkey audit (T-344), code fence full-width bg (T-345), Ctrl+V file link (T-346), Ctrl+click open link (T-347), italic/underline configurable (T-348).

### CLEAN sweep (18.07) — human review C-001/C-002 pending (kept above)
C-001 (i18n_build_scripts), C-002 (.saipen_backup).

### RECOVERY + HUNT 21.07 — all DONE
R-01..R-05: P0s restored, theme wave, markdown escape+Ctrl+V+touchpad, line-drag+quote+priority-fit, generate_custom_theme mutation fix.

### CRITICAL for next agent — resolved
T-521: PUSHED to remote (origin, branch restore-2026-07-21). T-520: ROOT CAUSE FOUND & FIXED (test_toolbar_button_can_move_back_across_gaps corrupted shared fixture). Downgraded to P3.

### HUNT sweep 21.07 — all FIXED
H-01 (fancy_zones QCursor crash), H-02 (_fix_tooltip.py deleted), H-03 (3 bare except narrowed), H-04 (unused/duplicate imports), H-05 (31 translatations.py dupe keys resolved). Verified: 465 unit + 118 smoke.

### Feature: FancyZones (21.07) — DONE
F-01: Full interactive picker, 7 layouts, Ctrl+Q compact (later replaced by T-560). Settings grid rows/cols.

### Wishlist rounds 3-5 (21.07) — all DONE
W-01..W-14: Overflow menu, user code font, per-silo view state, middle-click line cycle, ~~ none accumulation, snippets panel toggle, quote un-stranding (P0), layout-independent hotkeys, one-line quote folds, hover line wash, Ctrl+E stamps only first header, heat+LH, SI-01..05 (silo identity remapping), F-10/F-11/SD-01/SD-02/HT-01 (files & tints), SP-01..03 (settings vertical space), C-10..C-13 (crash reports).

### Undo/redo integrity (21.07) — all DONE
U-01..U-04: typing after formatting undo merge fix, edit_block() context manager, AST regression guard, 8 runtime tests.

### Timer system (21.07) — all DONE
TM-01..TM-10: duration parser, Qt-free timer model, timer dialog, top-bar countdown, description, toast popup, test button, edit/pause/snooze, snooze bug fix, sound volume guard. Verified: 503 unit + 148 smoke.

### Settings UI compaction (21.07) — all DONE
S-01..S-05: FlowLayout, QTabWidget (4 tabs), clock own tab, appearance row flattened, 1848→287px (6.4×). Verified: 503 + 150.

### Line heat system (21.07) — all DONE
LH-01..LH-04: QTextBlockUserData, per-silo reconnect, same bucket system as recency, skip >2000 blocks. Default OFF.

### Silo identity & ordering (21.07) — all DONE
SI-01..SI-05: silo_view_state_all remap, archive remap, _SILO_INDEX_STATE registry, guard test, attribute aliasing fix.

### Files, dup, tints (21.07) — all DONE
F-10/F-11 (empty folder cleanup, phantom entries), SD-01/SD-02 (Duplicate + New Child Silo), HT-01 (tunable hover/heat palette, Settings).

### Settings vertical space (21.07) — all DONE
SP-01..SP-03: QTabWidget max height, per-tab size policy, heightForWidth flow cap. Window tab 415→205, Editor 415→210. Editor gained 233px.

### Crash reports (21.07) — all DONE
C-10 (pinned drop ValueError), C-11 (threading.excepthook + Qt message handler), C-12 (stress test — no crash), C-13 (paintEvent direct call AV fixed).

### Batch: tint, fold, heat, X-01..X-05 (21.07) — all DONE
X-01 (setExtraSelections inside paintEvent — likely heavy-doc crash), X-02 (tint selections → block paint), X-03 (wrapped-line tint via block rect), X-04 (---/***/___ ends header fold), X-05 (heat persists restart).

### T-540..T-572 batch (21.07) — mostly DONE
T-540..T-554, T-556..T-565, T-567, T-571, T-572: all DONE. T-555 (jump bug — kept in TODO). T-566 (BLOCKED). T-568 (BLOCKED). T-569 (P0 BLOCKER). T-570 (P1 BLOCKED).

### HUNT sweep 20.07 — partly addressed
T-352 (dupe keys — FIXED by H-05), T-353 (.git — replaced by T-521), T-354 (_fix_tooltip — DELETED by H-02), T-355 (build dir — still TODO), T-356 (bare except — FIXED by H-03), T-357 (import sorts — still TODO), T-358 (unused imports — FIXED by H-04), T-359 (l variable — still TODO), T-360 (whitespace — still TODO), T-361 (r mode — still TODO). Remaining TODO kept in Current TODO above.
